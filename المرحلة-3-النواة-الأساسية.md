# 01-إعداد-بيئة-التطوير

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/01-إعداد-بيئة-التطوير.md
**التبعيات:** المرحلة-2-التصميم-المعماري/ACCEPTANCE_CRITERIA_PHASE2.md

## 🎯 الغرض والهدف
تحديد وتوثيق جميع الخطوات اللازمة لإعداد بيئة التطوير (Development Environment) وبنية المشروع الأساسية (Project Structure) لبدء تنفيذ النواة الأساسية.

## 📋 المتطلبات الأساسية
قبول تسليمات المرحلة 2 (التصميم المعماري).

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/ACCEPTANCE_CRITERIA_PHASE2.md` - يمثل نقطة البداية للمرحلة 3.

### الملفات اللاحقة  
- `02-هيكلة-ملفات-المشروع.md` - لتفصيل هيكل الملفات بعد الإعداد.
- `03-تهيئة-نظام-الأنواع-الموحدة.md` - لتنفيذ نظام الأنواع الموحدة.

## 🏗 البنية التفصيلية
### 1. الأدوات والتقنيات (Tools and Technologies)
| الأداة/التقنية | الإصدار | الغرض |
| :--- | :--- | :--- |
| **TypeScript** | أحدث مستقر | لضمان الأمان النوعي (Type Safety). |
| **React** | أحدث مستقر | لتطوير واجهة المستخدم (Popup, Options). |
| **Vite** | أحدث مستقر | لبناء المشروع (Bundling) بكفاءة وسرعة. |
| **ESLint/Prettier** | أحدث مستقر | لفرض معايير الكود (CODING_STANDARDS). |
| **WebAssembly** | N/A | لتشغيل نماذج STT المحلية (مثل Whisper). |

### 2. إعداد بيئة المشروع (Project Setup)
**الوصف:** استخدام Vite لإنشاء مشروع TypeScript/React، وتكوينه ليكون إضافة متصفح (Browser Extension).
*   **ملف `manifest.json`:** يجب تكوينه لدعم `Service Worker` و `Content Script` و `Popup` و `Options Page`.
*   **تكوين TypeScript:** يجب أن يكون صارمًا (Strict Mode) لضمان أعلى مستوى من الأمان النوعي.

### 3. تثبيت التبعيات (Dependencies Installation)
**الوصف:** تثبيت جميع الحزم الأساسية المطلوبة للنواة.
*   **React/ReactDOM:** لمكونات الواجهة الأمامية.
*   **IndexedDB Wrapper:** لتسهيل التفاعل مع IndexedDB (Storage Manager).
*   **WebAssembly Loader:** لتحميل نماذج STT.

## ⚙️ آلية العمل
يتم تنفيذ هذه الخطوات لإنشاء بيئة عمل موحدة وفعالة لجميع المطورين، مما يضمن أن الكود المكتوب يتبع أعلى معايير الجودة والأداء.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم تشغيل أمر `npm run dev`، ويتم بناء الإضافة بنجاح وتحميلها في المتصفح للاختبار.
*   **حالة 2:** يتم تشغيل ESLint، ويتم التحقق من أن جميع الملفات تتبع `CODING_STANDARDS`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 02-هيكلة-ملفات-المشروع

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/02-هيكلة-ملفات-المشروع.md
**التبعيات:** 01-إعداد-بيئة-التطوير.md, المرحلة-2-التصميم-المعماري/01-هيكل-النظام-الشامل.md

## 🎯 الغرض والهدف
تحديد الهيكل التنظيمي لملفات المشروع (Project Structure) والمجلدات، بما يتوافق مع التصميم المعماري (طبقات النظام والمكونات)، لضمان سهولة التنقل والصيانة.

## 📋 المتطلبات الأساسية
ملف `01-إعداد-بيئة-التطوير.md` لتأكيد إعداد البيئة، و `01-هيكل-النظام-الشامل.md` لتحديد المكونات.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-إعداد-بيئة-التطوير.md` - يمثل الخطوة الأولى في التنفيذ.
- `المرحلة-2-التصميم-المعماري/01-هيكل-النظام-الشامل.md` - يوفر الأساس الهيكلي.

### الملفات اللاحقة  
- `03-تهيئة-نظام-الأنواع-الموحدة.md` - يتم وضع ملفات الأنواع في هذا الهيكل.
- `04-تنفيذ-نظام-الرسائل-والأحداث.md` - يتم وضع ملفات نظام الرسائل في هذا الهيكل.

## 🏗 البنية التفصيلية
### 1. هيكل المجلدات الرئيسي
```
/src
├── assets/             # الموارد الثابتة (أيقونات، صور، خطوط)
├── components/         # مكونات React المشتركة (لـ Popup و Options)
├── core/               # طبقة منطق الأعمال (Service Worker)
│   ├── managers/       # وحدات الإدارة الرئيسية (SettingsManager, ResourceManager)
│   ├── services/       # الخدمات الأساسية (EncryptionService, FallbackManager)
│   └── adapters/       # واجهات التكيف مع الخدمات الخارجية (GoogleTranslateAdapter)
├── ui/                 # طبقة العرض (Presentation Layer)
│   ├── popup/          # منطق وواجهة Popup
│   ├── options/        # منطق وواجهة Options Panel
│   └── content/        # منطق وواجهة Content Script (VideoDetector, OverlayManager)
├── types/              # الأنواع والواجهات الموحدة (common.ts)
├── utils/              # الدوال المساعدة المشتركة
├── manifest.json       # ملف تكوين الإضافة
└── index.html          # ملفات HTML الأساسية (Popup, Options)
```

### 2. هيكل مجلد `core/managers`
**الوصف:** يحتوي على المكونات الرئيسية التي تم تصميمها في المرحلة 2.
*   `SettingsManager.ts`
*   `ResourceManager.ts`
*   `SecurityManager.ts`
*   `ErrorManager.ts`
*   `TranslationOrchestrator.ts`

## ⚙️ آلية العمل
يتم استخدام هذا الهيكل لضمان أن كل جزء من الكود يقع في مكانه الصحيح وفقًا لمسؤوليته (SRP)، مما يسهل على المطورين الجدد فهم المشروع.

## 🔍 حالات الاستخدام
*   **حالة 1:** عند البحث عن منطق حفظ الإعدادات، يتم التوجه مباشرة إلى `core/managers/SettingsManager.ts`.
*   **حالة 2:** عند البحث عن مكون واجهة المستخدم لزر التشغيل، يتم التوجه إلى `ui/popup/components/PlayButton.tsx`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 03-تهيئة-نظام-الأنواع-الموحدة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/03-تهيئة-نظام-الأنواع-الموحدة.md
**التبعيات:** 02-هيكلة-ملفات-المشروع.md, المرحلة-2-التصميم-المعماري/06-تصميم-الواجهات-والأنواع-الموحدة.md

## 🎯 الغرض والهدف
تنفيذ جميع الواجهات (Interfaces) والأنواع (Types) الموحدة التي تم تصميمها في المرحلة 2 في ملف `src/types/common.ts`، لضمان الأمان النوعي والاتساق عبر جميع المكونات.

## 📋 المتطلبات الأساسية
ملف `02-هيكلة-ملفات-المشروع.md` لتحديد موقع الملف، و `06-تصميم-الواجهات-والأنواع-الموحدة.md` لتحديد المحتوى.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `02-هيكلة-ملفات-المشروع.md` - يحدد أن الملف سيتم وضعه في `src/types/common.ts`.
- `المرحلة-2-التصميم-المعماري/06-تصميم-الواجهات-والأنواع-الموحدة.md` - يوفر المحتوى التفصيلي للأنواع.

### الملفات اللاحقة  
- `04-تنفيذ-نظام-الرسائل-والأحداث.md` - يستخدم هذه الأنواع في هيكل الرسائل.
- `05-تنفيذ-وحدة-إدارة-التخزين.md` - يستخدم هذه الأنواع لتخزين البيانات.

## 🏗 البنية التفصيلية
### 1. تنفيذ الواجهات الأساسية (Core Interfaces)
**الوصف:** تنفيذ الواجهات التي تحدد العقود البرمجية للمكونات الرئيسية.
```typescript
// src/types/common.ts

export interface ITranslationEngine {
  id: string;
  name: string;
  translate(request: TranslationRequest): Promise<TranslationResult>;
}

export interface ISTTEngine {
  id: string;
  name: string;
  process(audio: AudioSegment): Promise<STTResult>;
}

export interface IStorageManager {
  get<T>(key: string): Promise<T | null>;
  set<T>(key: string, value: T): Promise<void>;
}
```

### 2. تنفيذ أنواع البيانات (Data Types)
**الوصف:** تنفيذ الأنواع التي تمثل البيانات المتداولة في النظام.
```typescript
// src/types/common.ts

export type LanguageCode = string; // e.g., 'en', 'ar'

export interface TranslationRequest {
  text: string;
  sourceLang: LanguageCode;
  targetLang: LanguageCode;
  requestId: string;
}

export interface TranslationResult {
  originalText: string;
  translatedText: string;
  engineId: string;
  latency: number; // in milliseconds
}

export interface AppConfig {
  sourceLang: LanguageCode;
  targetLang: LanguageCode;
  defaultEngineId: string;
  overlaySettings: {
    fontSize: number;
    fontColor: string;
    position: 'top' | 'bottom';
  };
}
```

## ⚙️ آلية العمل
يتم إنشاء ملف `src/types/common.ts` وتضمين جميع الأنواع والواجهات الموحدة فيه، مما يضمن أن جميع المكونات تستخدم نفس التعريفات، ويتم التحقق من ذلك بواسطة TypeScript.

## 🔍 حالات الاستخدام
*   **حالة 1:** عند استدعاء دالة `translate` في أي محرك ترجمة، يضمن TypeScript أن المدخل هو `TranslationRequest` والمخرج هو `TranslationResult`.
*   **حالة 2:** عند الوصول إلى إعدادات التطبيق، يضمن TypeScript أن الهيكل يتبع `AppConfig`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 04-تنفيذ-نظام-الرسائل-والأحداث

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/04-تنفيذ-نظام-الرسائل-والأحداث.md
**التبعيات:** 03-تهيئة-نظام-الأنواع-الموحدة.md, المرحلة-2-التصميم-المعماري/05-تصميم-نظام-الرسائل-والأحداث.md

## 🎯 الغرض والهدف
تنفيذ نظام الرسائل والأحداث (Event-Driven Architecture) الذي يتيح التواصل الآمن وغير المتزامن بين مكونات الإضافة المختلفة، بناءً على التصميم المحدد في المرحلة 2.

## 📋 المتطلبات الأساسية
ملف `03-تهيئة-نظام-الأنواع-الموحدة.md` لتحديد هيكل الرسالة، و `05-تصميم-نظام-الرسائل-والأحداث.md` لتحديد آليات التواصل.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `03-تهيئة-نظام-الأنواع-الموحدة.md` - يوفر النوع `Message` الموحد.
- `المرحلة-2-التصميم-المعماري/05-تصميم-نظام-الرسائل-والأحداث.md` - يوفر التصميم التفصيلي للنظام.

### الملفات اللاحقة  
- `05-تنفيذ-وحدة-إدارة-التخزين.md` - تستخدم هذه الوحدة نظام الرسائل للتواصل مع `Options Panel`.
- `06-تنفيذ-وحدة-إدارة-الإعدادات.md` - تستخدم هذه الوحدة نظام الرسائل لنشر تحديثات الإعدادات.

## 🏗 البنية التفصيلية
### 1. تنفيذ هيكل الرسالة الموحد
**الوصف:** إنشاء ملف `src/types/messages.ts` يحتوي على تعريف `Message` وجميع أنواع الإجراءات (Actions) والأحداث (Events).

```typescript
// src/types/messages.ts
export type MessageAction = 'TRANSLATE_REQUEST' | 'SAVE_SETTINGS' | 'START_CAPTURE' | 'STOP_CAPTURE';
export type MessageEvent = 'TRANSLATION_READY' | 'SETTINGS_UPDATED' | 'ERROR_OCCURRED';

export interface Message<T = any> {
  type: 'REQUEST' | 'RESPONSE' | 'EVENT';
  action: MessageAction | MessageEvent;
  payload?: T;
  requestId?: string;
  error?: string;
}
```

### 2. تنفيذ `MessageBroker`
**الوصف:** إنشاء فئة `MessageBroker` في `src/core/services/MessageBroker.ts` لتغليف وظائف `chrome.runtime.sendMessage` و `chrome.runtime.onMessage`.

```typescript
// src/core/services/MessageBroker.ts
class MessageBroker {
  // إرسال رسالة طلب إلى Service Worker
  static sendRequest<T, R>(action: MessageAction, payload: T): Promise<R> {
    // ... تنفيذ chrome.runtime.sendMessage
  }

  // إرسال حدث إلى جميع المكونات
  static sendEvent<T>(event: MessageEvent, payload: T): void {
    // ... تنفيذ chrome.runtime.sendMessage
  }

  // الاستماع إلى الرسائل في Service Worker
  static onMessage(handler: (message: Message) => Promise<Message | void>): void {
    // ... تنفيذ chrome.runtime.onMessage.addListener
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `MessageBroker` كواجهة موحدة للتواصل بين جميع أجزاء الإضافة، مما يضمن أن جميع الرسائل تتبع الهيكل الموحد وتتم معالجتها بشكل غير متزامن.

## 🔍 حالات الاستخدام
*   **حالة 1:** `Popup` يستدعي `MessageBroker.sendRequest('SAVE_SETTINGS', config)` لحفظ الإعدادات.
*   **حالة 2:** `Translation Orchestrator` يستدعي `MessageBroker.sendEvent('TRANSLATION_READY', result)` لإرسال الترجمة إلى `Content Script`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 05-تنفيذ-وحدة-إدارة-التخزين

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/05-تنفيذ-وحدة-إدارة-التخزين.md
**التبعيات:** 03-تهيئة-نظام-الأنواع-الموحدة.md, المرحلة-2-التصميم-المعماري/13-تصميم-وحدة-إدارة-التخزين.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة التخزين (`StorageManager`) التي توفر واجهة موحدة للوصول إلى آليات التخزين المختلفة (IndexedDB، Local Storage) وتطبيق مخطط التخزين المحدد.

## 📋 المتطلبات الأساسية
ملف `03-تهيئة-نظام-الأنواع-الموحدة.md` لتحديد واجهة `IStorageManager`، و `13-تصميم-وحدة-إدارة-التخزين.md` لتحديد مخطط التخزين.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `03-تهيئة-نظام-الأنواع-الموحدة.md` - يوفر واجهة `IStorageManager`.
- `المرحلة-2-التصميم-المعماري/13-تصميم-وحدة-إدارة-التخزين.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `06-تنفيذ-وحدة-إدارة-الإعدادات.md` - تستخدم هذه الوحدة لتخزين الإعدادات.
- `07-تنفيذ-وحدة-الأمان-والخصوصية.md` - تستخدم هذه الوحدة لتخزين البيانات المشفرة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `StorageManager`
**الوصف:** إنشاء فئة `StorageManager` في `src/core/managers/StorageManager.ts` تنفذ واجهة `IStorageManager`.

```typescript
// src/core/managers/StorageManager.ts
import { IStorageManager } from '../../types/common';

class StorageManager implements IStorageManager {
  private indexedDB: IndexedDBAdapter;
  private localStorage: LocalStorageAdapter;

  constructor() {
    this.indexedDB = new IndexedDBAdapter(); // للبيانات الكبيرة (Cache, Archive)
    this.localStorage = new LocalStorageAdapter(); // للبيانات الصغيرة (Settings)
  }

  async get<T>(key: string): Promise<T | null> {
    if (key === 'AppConfig') {
      return this.localStorage.get(key);
    }
    return this.indexedDB.get(key);
  }

  async set<T>(key: string, value: T): Promise<void> {
    if (key === 'AppConfig') {
      return this.localStorage.set(key, value);
    }
    return this.indexedDB.set(key, value);
  }
  
  // ... وظائف أخرى مثل archiveTranslation
}
```

### 2. تنفيذ `IndexedDBAdapter` و `LocalStorageAdapter`
**الوصف:** تنفيذ محولات التخزين الفعلية التي تتعامل مع واجهات برمجة تطبيقات المتصفح.

*   **`IndexedDBAdapter.ts`:** مسؤول عن تهيئة قاعدة البيانات، وفتح المخازن (Stores) مثل `TranslationCache` و `Archive`.
*   **`LocalStorageAdapter.ts`:** مسؤول عن التعامل مع `localStorage` مع معالجة الأخطاء (مثل تجاوز الحجم).

## ⚙️ آلية العمل
يتم استخدام `StorageManager` كطبقة تجريد (Abstraction Layer) تخفي تفاصيل التخزين عن باقي مكونات النظام، مما يسهل التبديل بين آليات التخزين في المستقبل.

## 🔍 حالات الاستخدام
*   **حالة 1:** `SettingsManager` يستدعي `StorageManager.get<AppConfig>('AppConfig')` لجلب الإعدادات.
*   **حالة 2:** `Translation Orchestrator` يستدعي `StorageManager.set('TranslationCache', result)` لتخزين نتيجة الترجمة مؤقتًا.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 06-تنفيذ-وحدة-إدارة-الإعدادات

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/06-تنفيذ-وحدة-إدارة-الإعدادات.md
**التبعيات:** 05-تنفيذ-وحدة-إدارة-التخزين.md, المرحلة-2-التصميم-المعماري/19-تصميم-وحدة-إدارة-الإعدادات.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة الإعدادات (`SettingsManager`) التي تتعامل مع تحميل، حفظ، والتحقق من صحة إعدادات المستخدم، وتستخدم `StorageManager` لتخزين البيانات.

## 📋 المتطلبات الأساسية
ملف `05-تنفيذ-وحدة-إدارة-التخزين.md` لتوفير آلية التخزين، و `19-تصميم-وحدة-إدارة-الإعدادات.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `05-تنفيذ-وحدة-إدارة-التخزين.md` - يوفر واجهة التخزين.
- `المرحلة-2-التصميم-المعماري/19-تصميم-وحدة-إدارة-الإعدادات.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `07-تنفيذ-وحدة-الأمان-والخصوصية.md` - تستخدم هذه الوحدة لتشفير مفاتيح API في الإعدادات.
- `08-تنفيذ-وحدة-إدارة-الموارد.md` - تستخدم هذه الوحدة إعدادات اللغة لتحميل الموارد.

## 🏗 البنية التفصيلية
### 1. تنفيذ `SettingsManager`
**الوصف:** إنشاء فئة `SettingsManager` في `src/core/managers/SettingsManager.ts`، وتوفير وظائف `load`, `save`, و `get` للإعدادات.

```typescript
// src/core/managers/SettingsManager.ts
import { AppConfig } from '../../types/common';
import { StorageManager } from './StorageManager';
import { DefaultSettings } from '../config/DefaultSettings'; // ملف جديد للإعدادات الافتراضية

class SettingsManager {
  private currentConfig: AppConfig;
  private storageManager: StorageManager;

  constructor(storageManager: StorageManager) {
    this.storageManager = storageManager;
    this.currentConfig = DefaultSettings; // تهيئة بالإعدادات الافتراضية
  }

  async loadSettings(): Promise<AppConfig> {
    const storedConfig = await this.storageManager.get<AppConfig>('AppConfig');
    if (storedConfig) {
      // دمج الإعدادات المخزنة مع الافتراضية لضمان التوافق مع الإصدارات الجديدة
      this.currentConfig = { ...DefaultSettings, ...storedConfig };
    }
    return this.currentConfig;
  }

  async saveSettings(newConfig: AppConfig): Promise<void> {
    // 1. التحقق من صحة الإعدادات (SettingsValidator)
    // 2. تشفير مفاتيح API (SecurityManager)
    // 3. حفظ الإعدادات
    await this.storageManager.set('AppConfig', newConfig);
    this.currentConfig = newConfig;
    // 4. نشر حدث SETTINGS_UPDATED (MessageBroker)
  }

  getSettings(): AppConfig {
    return this.currentConfig;
  }
}
```

### 2. تنفيذ `DefaultSettings`
**الوصف:** إنشاء ملف `src/core/config/DefaultSettings.ts` يحتوي على الإعدادات الافتراضية.

## ⚙️ آلية العمل
يتم استخدام `SettingsManager` كمركز تحكم للإعدادات، مما يضمن أن جميع المكونات تعمل دائمًا بأحدث الإعدادات الصالحة.

## 🔍 حالات الاستخدام
*   **حالة 1:** عند تغيير المستخدم للغة الهدف، يتم استدعاء `saveSettings`، ويتم تحديث `currentConfig` ونشر حدث `SETTINGS_UPDATED`.
*   **حالة 2:** `Translation Orchestrator` يستدعي `getSettings()` للحصول على اللغة الهدف الحالية قبل بدء الترجمة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 07-تنفيذ-وحدة-الأمان-والخصوصية

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/07-تنفيذ-وحدة-الأمان-والخصوصية.md
**التبعيات:** 06-تنفيذ-وحدة-إدارة-الإعدادات.md, المرحلة-2-التصميم-المعماري/14-تصميم-وحدة-الأمان-والخصوصية.md

## 🎯 الغرض والهدف
تنفيذ وحدة الأمان والخصوصية (`SecurityManager`)، مع التركيز على تشفير وفك تشفير مفاتيح API باستخدام `Web Crypto API` لضمان تلبية المتطلبات غير الوظيفية للأمان (NFR3.0).

## 📋 المتطلبات الأساسية
ملف `06-تنفيذ-وحدة-إدارة-الإعدادات.md` الذي يتطلب تشفير مفاتيح API قبل الحفظ، و `14-تصميم-وحدة-الأمان-والخصوصية.md` لتحديد آلية التشفير.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `06-تنفيذ-وحدة-إدارة-الإعدادات.md` - يتطلب تشفير مفاتيح API.
- `المرحلة-2-التصميم-المعماري/14-تصميم-وحدة-الأمان-والخصوصية.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `08-تنفيذ-وحدة-إدارة-الموارد.md` - لا يوجد ارتباط مباشر.
- `09-تنفيذ-وحدة-إدارة-الأخطاء.md` - يتم تسجيل أخطاء التشفير عبر هذه الوحدة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `SecurityManager`
**الوصف:** إنشاء فئة `SecurityManager` في `src/core/managers/SecurityManager.ts` لتغليف وظائف التشفير.

```typescript
// src/core/managers/SecurityManager.ts
class SecurityManager {
  private static readonly KEY_ALIAS = 'app-encryption-key';
  private encryptionKey: CryptoKey | null = null;

  async init(): Promise<void> {
    // 1. جلب مفتاح التشفير من التخزين أو توليده
    // 2. استخدام Web Crypto API لتوليد مفتاح AES-GCM
    // 3. تخزين المفتاح (بشكل آمن)
  }

  async encrypt(data: string): Promise<string> {
    if (!this.encryptionKey) throw new Error('Encryption key not initialized.');
    // استخدام AES-GCM لتشفير البيانات
    // إرجاع البيانات المشفرة كـ Base64
  }

  async decrypt(encryptedData: string): Promise<string> {
    if (!this.encryptionKey) throw new Error('Encryption key not initialized.');
    // فك تشفير البيانات المشفرة
  }

  // وظيفة لتشفير مفاتيح API في كائن الإعدادات
  async encryptApiKeys(config: AppConfig): Promise<AppConfig> {
    const encryptedConfig = { ...config };
    if (config.apiKeys.google) {
      encryptedConfig.apiKeys.google = await this.encrypt(config.apiKeys.google);
    }
    // ... تشفير مفاتيح API الأخرى
    return encryptedConfig;
  }
}
```

### 2. آلية التشفير
**الوصف:** استخدام `crypto.subtle.generateKey` لتوليد مفتاح سري، و `crypto.subtle.encrypt` و `crypto.subtle.decrypt` باستخدام خوارزمية `AES-GCM`.

## ⚙️ آلية العمل
يتم تهيئة `SecurityManager` عند بدء تشغيل `Service Worker`، ويتم استخدامه بواسطة `SettingsManager` لتشفير مفاتيح API قبل حفظها وفك تشفيرها قبل استخدامها في الاتصال بالطرف الثالث.

## 🔍 حالات الاستخدام
*   **حالة 1:** `SettingsManager` يستدعي `SecurityManager.encryptApiKeys(config)` قبل تمرير الإعدادات إلى `StorageManager`.
*   **حالة 2:** `ThirdPartyManager` يستدعي `SecurityManager.decrypt(encryptedKey)` قبل استخدام مفتاح API في طلب خارجي.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 08-تنفيذ-وحدة-إدارة-الموارد

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/08-تنفيذ-وحدة-إدارة-الموارد.md
**التبعيات:** 07-تنفيذ-وحدة-الأمان-والخصوصية.md, المرحلة-2-التصميم-المعماري/15-تصميم-وحدة-إدارة-الموارد.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة الموارد (`ResourceManager`) التي تتعامل مع تحميل الموارد الثابتة (مثل ملفات اللغات، نماذج STT المحلية) وإدارة التخزين المؤقت لها بكفاءة، مع مراقبة استهلاك الذاكرة.

## 📋 المتطلبات الأساسية
ملف `07-تنفيذ-وحدة-الأمان-والخصوصية.md` (لا يوجد ارتباط مباشر، لكن التسلسل مهم)، و `15-تصميم-وحدة-إدارة-الموارد.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/15-تصميم-وحدة-إدارة-الموارد.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `09-تنفيذ-وحدة-إدارة-الأخطاء.md` - يتم تسجيل أخطاء تحميل الموارد عبر هذه الوحدة.
- `10-تنفيذ-وحدة-التسجيل-والمراقبة.md` - يتم استخدام هذه الوحدة لتسجيل مقاييس الأداء.

## 🏗 البنية التفصيلية
### 1. تنفيذ `ResourceManager`
**الوصف:** إنشاء فئة `ResourceManager` في `src/core/managers/ResourceManager.ts` لتغليف وظائف تحميل الموارد والتخزين المؤقت.

```typescript
// src/core/managers/ResourceManager.ts
class ResourceManager {
  private cache: Cache; // استخدام Cache API للتخزين المؤقت
  private memoryMonitor: MemoryMonitor;

  constructor() {
    this.memoryMonitor = new MemoryMonitor();
    // تهيئة Cache API
  }

  async loadResource(resourcePath: string): Promise<Response> {
    // 1. التحقق من وجود المورد في Cache
    // 2. إذا لم يوجد، تحميله باستخدام fetch
    // 3. تخزينه في Cache
    // 4. إرجاع الاستجابة
  }

  async unloadResource(resourcePath: string): Promise<void> {
    // إزالة المورد من Cache وذاكرة النظام (إذا كان نموذج WebAssembly)
  }

  getMemoryUsage(): number {
    // استخدام performance.memory (إذا كان متاحًا) أو تقنية أخرى
    return this.memoryMonitor.getCurrentUsage();
  }
}

// src/core/services/MemoryMonitor.ts
class MemoryMonitor {
  getCurrentUsage(): number {
    // تنفيذ منطق مراقبة الذاكرة
    return 0; // القيمة الافتراضية
  }
}
```

### 2. استراتيجية التحميل والتفريغ
**الوصف:** تطبيق استراتيجية التحميل عند الطلب والتفريغ التلقائي.
*   **التحميل عند الطلب:** يتم استدعاء `loadResource` فقط عندما يطلب مكون آخر المورد.
*   **التفريغ التلقائي:** يتم استخدام مؤقت (Timer) لاستدعاء `unloadResource` للموارد غير المستخدمة بعد فترة زمنية محددة.

## ⚙️ آلية العمل
يتم استخدام `ResourceManager` لضمان أن النظام يعمل بكفاءة عالية ويستهلك الحد الأدنى من الموارد، مما يدعم متطلب الأداء (NFR1.0).

## 🔍 حالات الاستخدام
*   **حالة 1:** `STT Unit` يطلب ملف لغة، و `ResourceManager` يقوم بتحميله من التخزين المؤقت أو الشبكة.
*   **حالة 2:** `MemoryMonitor` يطلق تحذيرًا داخليًا عندما يتجاوز استهلاك الذاكرة 40 ميجابايت.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 09-تنفيذ-وحدة-إدارة-الأخطاء

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/09-تنفيذ-وحدة-إدارة-الأخطاء.md
**التبعيات:** 08-تنفيذ-وحدة-إدارة-الموارد.md, المرحلة-2-التصميم-المعماري/16-تصميم-وحدة-إدارة-الأخطاء.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة الأخطاء (`ErrorManager`)، بما في ذلك تعريف الاستثناءات المخصصة (Custom Exceptions) وتوفير آلية مركزية لمعالجة الأخطاء.

## 📋 المتطلبات الأساسية
ملف `08-تنفيذ-وحدة-إدارة-الموارد.md` (للتسلسل)، و `16-تصميم-وحدة-إدارة-الأخطاء.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/16-تصميم-وحدة-إدارة-الأخطاء.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `10-تنفيذ-وحدة-التسجيل-والمراقبة.md` - يتم استخدام هذه الوحدة لتسجيل الأخطاء.
- `11-تنفيذ-وحدة-إدارة-الإشعارات.md` - يتم استخدام هذه الوحدة لإبلاغ المستخدم بالأخطاء.

## 🏗 البنية التفصيلية
### 1. تعريف الاستثناءات المخصصة
**الوصف:** إنشاء ملف `src/types/exceptions.ts` يحتوي على فئات الاستثناءات المخصصة التي ترث من `Error`.

```typescript
// src/types/exceptions.ts
export class BaseAppError extends Error {
  constructor(message: string, public code: number = 500) {
    super(message);
    this.name = this.constructor.name;
  }
}

export class TranslationEngineError extends BaseAppError {
  constructor(message: string) {
    super(message, 503);
  }
}

export class AudioCaptureError extends BaseAppError {
  constructor(message: string) {
    super(message, 400);
  }
}

export class SecurityError extends BaseAppError {
  constructor(message: string) {
    super(message, 401);
  }
}
```

### 2. تنفيذ `ErrorManager`
**الوصف:** إنشاء فئة `ErrorManager` في `src/core/managers/ErrorManager.ts` لتغليف وظائف معالجة الأخطاء.

```typescript
// src/core/managers/ErrorManager.ts
import { BaseAppError } from '../../types/exceptions';

class ErrorManager {
  handleError(error: Error): void {
    if (error instanceof BaseAppError) {
      // 1. تسجيل الخطأ (Logging Unit)
      // 2. إبلاغ المستخدم (Notification Unit)
      // 3. محاولة الاسترداد (FallbackManager)
    } else {
      // التعامل مع أخطاء النظام غير المتوقعة
    }
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `ErrorManager` في جميع أنحاء النظام لضمان أن الأخطاء يتم اعتراضها ومعالجتها وتسجيلها بشكل موحد، مما يزيد من موثوقية النظام (NFR2.0).

## 🔍 حالات الاستخدام
*   **حالة 1:** عند فشل الاتصال بمحرك الترجمة، يتم إطلاق `new TranslationEngineError('Connection failed')`، ويقوم `ErrorManager` بمعالجته.
*   **حالة 2:** يتم استخدام `try...catch` في جميع الدوال الرئيسية، ويتم تمرير أي خطأ ملتقط إلى `ErrorManager.handleError(e)`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 10-تنفيذ-وحدة-التسجيل-والمراقبة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/10-تنفيذ-وحدة-التسجيل-والمراقبة.md
**التبعيات:** 09-تنفيذ-وحدة-إدارة-الأخطاء.md, المرحلة-2-التصميم-المعماري/17-تصميم-وحدة-التسجيل-والمراقبة.md

## 🎯 الغرض والهدف
تنفيذ وحدة التسجيل والمراقبة (`Logging Unit`) التي توفر واجهة موحدة لتسجيل الأحداث بمستويات مختلفة (DEBUG, INFO, WARN, ERROR) وتخزينها مؤقتًا.

## 📋 المتطلبات الأساسية
ملف `09-تنفيذ-وحدة-إدارة-الأخطاء.md` لتلقي الأخطاء، و `17-تصميم-وحدة-التسجيل-والمراقبة.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `09-تنفيذ-وحدة-إدارة-الأخطاء.md` - يرسل الأخطاء إلى هذه الوحدة للتسجيل.
- `المرحلة-2-التصميم-المعماري/17-تصميم-وحدة-التسجيل-والمراقبة.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `11-تنفيذ-وحدة-إدارة-الإشعارات.md` - لا يوجد ارتباط مباشر.
- `12-تنفيذ-وحدة-التكامل-مع-المتصفح.md` - يتم استخدام هذه الوحدة لتسجيل أحداث المتصفح.

## 🏗 البنية التفصيلية
### 1. تنفيذ `Logger`
**الوصف:** إنشاء فئة `Logger` في `src/core/services/Logger.ts` لتغليف وظائف التسجيل.

```typescript
// src/core/services/Logger.ts
type LogLevel = 'DEBUG' | 'INFO' | 'WARN' | 'ERROR';

class Logger {
  private logBuffer: LogEntry[] = [];
  private readonly MAX_LOGS = 100;

  log(level: LogLevel, message: string, context?: any): void {
    const timestamp = new Date().toISOString();
    const logEntry = { timestamp, level, message, context };

    // 1. إضافة السجل إلى المخزن المؤقت
    this.logBuffer.push(logEntry);
    if (this.logBuffer.length > this.MAX_LOGS) {
      this.logBuffer.shift(); // إزالة أقدم سجل
    }

    // 2. إرسال السجل إلى console (للتطوير)
    switch (level) {
      case 'ERROR':
        console.error(`[${timestamp}] [${level}] ${message}`, context);
        break;
      case 'WARN':
        console.warn(`[${timestamp}] [${level}] ${message}`, context);
        break;
      case 'INFO':
        console.info(`[${timestamp}] [${level}] ${message}`, context);
        break;
      case 'DEBUG':
        console.log(`[${timestamp}] [${level}] ${message}`, context);
        break;
    }
  }

  getLogs(): LogEntry[] {
    return this.logBuffer;
  }
}
```

### 2. تكامل `PerformanceLogger`
**الوصف:** استخدام `Logger` لتسجيل مقاييس الأداء بشكل دوري.
*   **مثال:** `Logger.log('DEBUG', 'Memory Usage', { usage: ResourceManager.getMemoryUsage() });`

## ⚙️ آلية العمل
يتم استخدام `Logger` في جميع أنحاء النظام لتسجيل الأحداث، مما يوفر سجلاً تاريخيًا مفيدًا لتصحيح الأخطاء وتحليل الأداء.

## 🔍 حالات الاستخدام
*   **حالة 1:** `SettingsManager` يستدعي `Logger.log('INFO', 'Settings saved successfully')` بعد حفظ الإعدادات.
*   **حالة 2:** `ErrorManager` يستدعي `Logger.log('ERROR', error.message, error)` عند وقوع خطأ حرج.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 11-تنفيذ-وحدة-إدارة-الإشعارات

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/11-تنفيذ-وحدة-إدارة-الإشعارات.md
**التبعيات:** 10-تنفيذ-وحدة-التسجيل-والمراقبة.md, المرحلة-2-التصميم-المعماري/18-تصميم-وحدة-إدارة-الإشعارات.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة الإشعارات (`NotificationManager`) التي تتعامل مع إرسال الإشعارات إلى المستخدم، سواء كانت إشعارات نظام أو إشعارات داخلية، بطريقة غير مزعجة.

## 📋 المتطلبات الأساسية
ملف `10-تنفيذ-وحدة-التسجيل-والمراقبة.md` (للتسلسل)، و `18-تصميم-وحدة-إدارة-الإشعارات.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/18-تصميم-وحدة-إدارة-الإشعارات.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `12-تنفيذ-وحدة-التكامل-مع-المتصفح.md` - يتم استخدام هذه الوحدة لتحديث أيقونة الإضافة.
- `13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md` - يتم استخدام هذه الوحدة لإبلاغ المستخدم بفشل التكامل.

## 🏗 البنية التفصيلية
### 1. تنفيذ `NotificationManager`
**الوصف:** إنشاء فئة `NotificationManager` في `src/core/managers/NotificationManager.ts` لتغليف وظائف الإبلاغ.

```typescript
// src/core/managers/NotificationManager.ts
type NotificationType = 'CRITICAL' | 'WARNING' | 'INFO';

class NotificationManager {
  private notificationQueue: NotificationEntry[] = [];
  private readonly DEBOUNCE_TIME = 2000; // 2 ثانية بين الإشعارات

  sendNotification(type: NotificationType, title: string, message: string): void {
    // 1. إضافة الإشعار إلى الطابور
    const newNotification = { type, title, message, timestamp: Date.now() };
    this.notificationQueue.push(newNotification);

    // 2. معالجة الطابور
    this.processQueue();
  }

  private processQueue(): void {
    if (this.notificationQueue.length === 0) return;

    const notification = this.notificationQueue.shift();
    if (!notification) return;

    // 3. تحديد القناة (نظام أو داخلي)
    if (notification.type === 'CRITICAL') {
      this.sendSystemNotification(notification.title, notification.message);
    } else {
      this.sendInAppNotification(notification.title, notification.message);
    }

    // 4. جدولة المعالجة التالية
    setTimeout(() => this.processQueue(), this.DEBOUNCE_TIME);
  }

  private sendSystemNotification(title: string, message: string): void {
    // استخدام chrome.notifications.create
  }

  private sendInAppNotification(title: string, message: string): void {
    // إرسال رسالة عبر MessageBroker إلى Popup/Options لعرض الإشعار
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `NotificationManager` لضمان أن المستخدم يتم إبلاغه بالأحداث المهمة والأخطاء بطريقة واضحة وغير مزعجة، مع استخدام طابور لمنع الإفراط في الإشعارات.

## 🔍 حالات الاستخدام
*   **حالة 1:** `ErrorManager` يستدعي `NotificationManager.sendNotification('CRITICAL', 'خطأ في الترجمة', 'فشل الاتصال بمحرك Google')`.
*   **حالة 2:** `SettingsManager` يستدعي `NotificationManager.sendNotification('INFO', 'تم الحفظ', 'تم حفظ الإعدادات بنجاح')`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 12-تنفيذ-وحدة-التكامل-مع-المتصفح

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/12-تنفيذ-وحدة-التكامل-مع-المتصفح.md
**التبعيات:** 11-تنفيذ-وحدة-إدارة-الإشعارات.md, المرحلة-2-التصميم-المعماري/21-تصميم-وحدة-التكامل-مع-المتصفح.md

## 🎯 الغرض والهدف
تنفيذ وحدة التكامل مع المتصفح (`BrowserManager`) التي تدير وظائف المتصفح الخاصة بالإضافة (الأيقونة، قائمة السياق، الأذونات) وتوفر واجهة موحدة لخدمات المتصفح.

## 📋 المتطلبات الأساسية
ملف `11-تنفيذ-وحدة-إدارة-الإشعارات.md` (للتسلسل)، و `21-تصميم-وحدة-التكامل-مع-المتصفح.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/21-تصميم-وحدة-التكامل-مع-المتصفح.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md` - لا يوجد ارتباط مباشر.
- `14-تنفيذ-وحدة-التحكم-بالفيديو.md` - يتم استخدام هذه الوحدة لتحديد علامات تبويب الفيديو النشطة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `BrowserManager`
**الوصف:** إنشاء فئة `BrowserManager` في `src/core/managers/BrowserManager.ts` لتغليف وظائف المتصفح.

```typescript
// src/core/managers/BrowserManager.ts
class BrowserManager {
  private iconManager: IconManager;
  private contextMenuManager: ContextMenuManager;

  constructor() {
    this.iconManager = new IconManager();
    this.contextMenuManager = new ContextMenuManager();
    this.contextMenuManager.init(); // تهيئة قائمة السياق عند بدء التشغيل
  }

  updateIcon(state: 'active' | 'inactive' | 'error'): void {
    this.iconManager.update(state);
  }

  async getActiveTabId(): Promise<number | null> {
    // استخدام chrome.tabs.query للحصول على علامة التبويب النشطة
  }

  async requestPermission(permission: string): Promise<boolean> {
    // استخدام chrome.permissions.request
  }
}

// src/core/services/IconManager.ts
class IconManager {
  update(state: 'active' | 'inactive' | 'error'): void {
    // استخدام chrome.action.setIcon لتغيير أيقونة الإضافة
  }
}

// src/core/services/ContextMenuManager.ts
class ContextMenuManager {
  init(): void {
    // استخدام chrome.contextMenus.create لإنشاء عناصر القائمة
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `BrowserManager` كواجهة موحدة للتفاعل مع واجهات برمجة تطبيقات المتصفح، مما يضمن أن الإضافة تتكامل بسلاسة مع بيئة المتصفح.

## 🔍 حالات الاستخدام
*   **حالة 1:** `Translation Orchestrator` يستدعي `BrowserManager.updateIcon('active')` عند بدء الترجمة.
*   **حالة 2:** عند النقر على عنصر في قائمة السياق، يتم إرسال رسالة إلى `Service Worker`، ويتم معالجتها بواسطة `BrowserManager`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md
**التبعيات:** 12-تنفيذ-وحدة-التكامل-مع-المتصفح.md, المرحلة-2-التصميم-المعماري/20-تصميم-وحدة-التكامل-مع-الطرف-الثالث.md

## 🎯 الغرض والهدف
تنفيذ وحدة التكامل مع الطرف الثالث (`ThirdPartyManager`)، مع التركيز على إنشاء واجهات التكيف (Adapters) لمحركات الترجمة السحابية (مثل Google Translate) وتطبيق آلية تحديد المعدل (Rate Limiting).

## 📋 المتطلبات الأساسية
ملف `12-تنفيذ-وحدة-التكامل-مع-المتصفح.md` (للتسلسل)، و `20-تصميم-وحدة-التكامل-مع-الطرف-الثالث.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/20-تصميم-وحدة-التكامل-مع-الطرف-الثالث.md` - يوفر التصميم التفصيلي للوحدة.
- `07-تنفيذ-وحدة-الأمان-والخصوصية.md` - لتشفير مفاتيح API.

### الملفات اللاحقة  
- `14-تنفيذ-وحدة-التحكم-بالفيديو.md` - لا يوجد ارتباط مباشر.
- `15-تنفيذ-وحدة-الترجمة-المركزية.md` - تستخدم هذه الوحدة لتنفيذ الترجمة الفعلية.

## 🏗 البنية التفصيلية
### 1. تنفيذ `ThirdPartyManager`
**الوصف:** إنشاء فئة `ThirdPartyManager` في `src/core/managers/ThirdPartyManager.ts` لتغليف وظائف الاتصال بالخدمات الخارجية.

```typescript
// src/core/managers/ThirdPartyManager.ts
import { ITranslationEngine, TranslationRequest, TranslationResult } from '../../types/common';
import { GoogleTranslateAdapter } from '../adapters/GoogleTranslateAdapter';
import { DeepLAdapter } from '../adapters/DeepLAdapter';

class ThirdPartyManager {
  private engines: Map<string, ITranslationEngine> = new Map();

  constructor() {
    // تهيئة محركات الترجمة
    this.engines.set('google', new GoogleTranslateAdapter());
    this.engines.set('deepl', new DeepLAdapter());
  }

  getEngine(engineId: string): ITranslationEngine | undefined {
    return this.engines.get(engineId);
  }

  async translate(request: TranslationRequest): Promise<TranslationResult> {
    const engine = this.getEngine(request.engineId);
    if (!engine) {
      throw new Error(`Engine ${request.engineId} not found.`);
    }

    // تطبيق Rate Limiter قبل الاستدعاء
    // ...

    return engine.translate(request);
  }
}
```

### 2. تنفيذ واجهات التكيف (Adapters)
**الوصف:** إنشاء ملفات التكيف في `src/core/adapters/` لكل محرك ترجمة.
*   **`GoogleTranslateAdapter.ts`:** ينفذ واجهة `ITranslationEngine` ويحتوي على منطق الاتصال بـ Google Translate API، بما في ذلك فك تشفير مفتاح API.

## ⚙️ آلية العمل
يتم استخدام `ThirdPartyManager` كطبقة تجريد للتعامل مع جميع الخدمات الخارجية، مما يضمن أن منطق الأعمال لا يتأثر بالتفاصيل الخاصة بكل محرك ترجمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** `Translation Orchestrator` يستدعي `ThirdPartyManager.translate(request)` لتنفيذ الترجمة.
*   **حالة 2:** إذا تجاوز محرك Google Translate حد المعدل، يتم إطلاق خطأ يتم التقاطه بواسطة `ErrorManager`.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 14-تنفيذ-وحدة-التحكم-بالفيديو

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/14-تنفيذ-وحدة-التحكم-بالفيديو.md
**التبعيات:** 13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md, المرحلة-2-التصميم-المعماري/07-تصميم-وحدة-التحكم-بالفيديو.md

## 🎯 الغرض والهدف
تنفيذ وحدة التحكم بالفيديو (`VideoController`) التي تقع في `Content Script`، وهي مسؤولة عن اكتشاف عنصر الفيديو النشط في الصفحة، وإدارة حالته (تشغيل/إيقاف مؤقت)، وتوفير واجهة موحدة للتفاعل معه.

## 📋 المتطلبات الأساسية
ملف `13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md` (للتسلسل)، و `07-تصميم-وحدة-التحكم-بالفيديو.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/07-تصميم-وحدة-التحكم-بالفيديو.md` - يوفر التصميم التفصيلي للوحدة.

### الملفات اللاحقة  
- `15-تنفيذ-وحدة-الترجمة-المركزية.md` - تستخدم هذه الوحدة حالة الفيديو لبدء/إيقاف الترجمة.
- `16-تنفيذ-وحدة-التقاط-الصوت.md` - تستخدم هذه الوحدة عنصر الفيديو كمصدر للصوت.

## 🏗 البنية التفصيلية
### 1. تنفيذ `VideoController`
**الوصف:** إنشاء فئة `VideoController` في `src/ui/content/VideoController.ts` للتعامل مع عنصر الفيديو.

```typescript
// src/ui/content/VideoController.ts
class VideoController {
  private videoElement: HTMLVideoElement | null = null;

  constructor() {
    this.detectVideoElement();
  }

  private detectVideoElement(): void {
    // البحث عن عنصر <video> النشط في DOM
    this.videoElement = document.querySelector('video:not([muted]):not([paused])');
    if (this.videoElement) {
      // إضافة مستمعي الأحداث (play, pause, seek)
      this.videoElement.addEventListener('play', this.handlePlay);
      this.videoElement.addEventListener('pause', this.handlePause);
    }
  }

  isPlaying(): boolean {
    return !!this.videoElement && !this.videoElement.paused;
  }

  getCurrentTime(): number {
    return this.videoElement ? this.videoElement.currentTime : 0;
  }

  // وظائف التحكم (Play, Pause)
  play(): void {
    this.videoElement?.play();
  }

  pause(): void {
    this.videoElement?.pause();
  }

  // معالجات الأحداث
  private handlePlay = () => {
    // إرسال حدث PLAY_EVENT إلى Service Worker
  };

  private handlePause = () => {
    // إرسال حدث PAUSE_EVENT إلى Service Worker
  };
}
```

## ⚙️ آلية العمل
يتم تهيئة `VideoController` عند تحميل `Content Script`، ويراقب حالة الفيديو النشط، ويرسل الأحداث إلى `Service Worker` لبدء أو إيقاف عملية الترجمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** المستخدم يضغط على زر التشغيل في مشغل الفيديو، و `VideoController` يرسل حدث `PLAY_EVENT`.
*   **حالة 2:** `Translation Orchestrator` يستدعي `VideoController.pause()` لإيقاف الفيديو مؤقتًا أثناء معالجة الترجمة (إذا لزم الأمر).

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 15-تنفيذ-وحدة-الترجمة-المركزية

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/15-تنفيذ-وحدة-الترجمة-المركزية.md
**التبعيات:** 14-تنفيذ-وحدة-التحكم-بالفيديو.md, المرحلة-2-التصميم-المعماري/08-تصميم-وحدة-الترجمة-المركزية.md

## 🎯 الغرض والهدف
تنفيذ وحدة الترجمة المركزية (`TranslationOrchestrator`) التي تقع في `Service Worker`، وهي النواة التي تنسق عملية الترجمة الكاملة (التقاط الصوت -> STT -> الترجمة -> العرض).

## 📋 المتطلبات الأساسية
ملف `14-تنفيذ-وحدة-التحكم-بالفيديو.md` (للتسلسل)، و `08-تصميم-وحدة-الترجمة-المركزية.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/08-تصميم-وحدة-الترجمة-المركزية.md` - يوفر التصميم التفصيلي للوحدة.
- `13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md` - تستخدم هذه الوحدة لتنفيذ الترجمة الفعلية.

### الملفات اللاحقة  
- `16-تنفيذ-وحدة-التقاط-الصوت.md` - يتم استخدام هذه الوحدة لالتقاط الصوت.
- `17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md` - يتم استخدام هذه الوحدة لاختيار محرك الترجمة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `TranslationOrchestrator`
**الوصف:** إنشاء فئة `TranslationOrchestrator` في `src/core/managers/TranslationOrchestrator.ts` لتنسيق العملية.

```typescript
// src/core/managers/TranslationOrchestrator.ts
class TranslationOrchestrator {
  private isRunning: boolean = false;
  private audioCaptureUnit: AudioCaptureUnit;
  private sttUnit: STTUnit;
  private thirdPartyManager: ThirdPartyManager;
  private settingsManager: SettingsManager;

  constructor(deps: any) {
    // حقن التبعيات (Dependency Injection)
    this.audioCaptureUnit = deps.audioCaptureUnit;
    this.sttUnit = deps.sttUnit;
    this.thirdPartyManager = deps.thirdPartyManager;
    this.settingsManager = deps.settingsManager;
  }

  startTranslation(): void {
    if (this.isRunning) return;
    this.isRunning = true;
    // 1. بدء التقاط الصوت (AudioCaptureUnit)
    this.audioCaptureUnit.startCapture();
    // 2. تحديث أيقونة المتصفح (BrowserManager)
  }

  stopTranslation(): void {
    if (!this.isRunning) return;
    this.isRunning = false;
    // 1. إيقاف التقاط الصوت (AudioCaptureUnit)
    this.audioCaptureUnit.stopCapture();
    // 2. تحديث أيقونة المتصفح (BrowserManager)
  }

  // معالج لنتائج STT
  async handleSTTResult(sttResult: STTResult): Promise<void> {
    if (!this.isRunning) return;

    const config = this.settingsManager.getSettings();
    const request: TranslationRequest = {
      text: sttResult.text,
      sourceLang: config.sourceLang,
      targetLang: config.targetLang,
      engineId: config.defaultEngineId, // سيتم استبداله بـ SmartEngineSelector
      requestId: sttResult.requestId,
    };

    try {
      // 1. اختيار المحرك الذكي (SmartEngineSelector)
      // 2. تنفيذ الترجمة (ThirdPartyManager)
      const translationResult = await this.thirdPartyManager.translate(request);
      // 3. إرسال النتيجة للعرض (MessageBroker)
    } catch (error) {
      // 4. معالجة الخطأ (ErrorManager)
    }
  }
}
```

## ⚙️ آلية العمل
يعمل `TranslationOrchestrator` كآلة حالة (State Machine) تتحكم في تدفق البيانات بين جميع الوحدات، مما يضمن التزامن والترتيب الصحيح للعمليات.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتلقى `Orchestrator` حدث `PLAY_EVENT` من `VideoController`، ويستدعي `startTranslation()`.
*   **حالة 2:** يتلقى `Orchestrator` نتيجة من `STTUnit`، ويقوم بترجمتها وإرسالها للعرض.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 16-تنفيذ-وحدة-التقاط-الصوت

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md
**التبعيات:** 15-تنفيذ-وحدة-الترجمة-المركزية.md, المرحلة-2-التصميم-المعماري/09-تصميم-وحدة-التقاط-الصوت.md

## 🎯 الغرض والهدف
تنفيذ وحدة التقاط الصوت (`AudioCaptureUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن الوصول إلى مصدر الصوت (سواء كان الميكروفون أو صوت النظام) وتقسيمه إلى مقاطع صوتية (Audio Segments) لإرسالها إلى وحدة STT.

## 📋 المتطلبات الأساسية
ملف `15-تنفيذ-وحدة-الترجمة-المركزية.md` الذي يستدعي وظائف البدء والإيقاف، و `09-تصميم-وحدة-التقاط-الصوت.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/09-تصميم-وحدة-التقاط-الصوت.md` - يوفر التصميم التفصيلي للوحدة.
- `15-تنفيذ-وحدة-الترجمة-المركزية.md` - يستدعي وظائف البدء والإيقاف.

### الملفات اللاحقة  
- `17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md` - لا يوجد ارتباط مباشر.
- `18-تنفيذ-وحدة-التعرف-على-الكلام.md` - يتم إرسال مقاطع الصوت إلى هذه الوحدة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `AudioCaptureUnit`
**الوصف:** إنشاء فئة `AudioCaptureUnit` في `src/core/services/AudioCaptureUnit.ts` لتغليف وظائف التقاط الصوت.

```typescript
// src/core/services/AudioCaptureUnit.ts
class AudioCaptureUnit {
  private mediaRecorder: MediaRecorder | null = null;
  private audioChunks: Blob[] = [];
  private readonly CHUNK_DURATION = 2000; // 2 ثانية لكل مقطع

  async startCapture(): Promise<void> {
    try {
      // 1. الحصول على مصدر الصوت (الميكروفون أو صوت علامة التبويب)
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      // 2. تهيئة MediaRecorder
      this.mediaRecorder = new MediaRecorder(stream);
      this.mediaRecorder.ondataavailable = this.handleDataAvailable;
      this.mediaRecorder.onstop = this.handleStop;
      // 3. بدء التسجيل وتقسيم المقاطع
      this.mediaRecorder.start(this.CHUNK_DURATION);
    } catch (error) {
      // إطلاق AudioCaptureError (ErrorManager)
    }
  }

  stopCapture(): void {
    if (this.mediaRecorder && this.mediaRecorder.state !== 'inactive') {
      this.mediaRecorder.stop();
    }
  }

  private handleDataAvailable = (event: BlobEvent) => {
    if (event.data.size > 0) {
      // 1. إرسال المقطع الصوتي إلى STTUnit
      // MessageBroker.sendEvent('AUDIO_CHUNK_READY', event.data);
    }
  };

  private handleStop = () => {
    // تنظيف الموارد
  };
}
```

## ⚙️ آلية العمل
تضمن هذه الوحدة التقاط الصوت في الوقت الفعلي وتقسيمه إلى مقاطع صغيرة، مما يدعم متطلب الترجمة في الوقت الفعلي (FR1.0).

## 🔍 حالات الاستخدام
*   **حالة 1:** `TranslationOrchestrator` يستدعي `startCapture()` لبدء عملية الترجمة.
*   **حالة 2:** كل 2 ثانية، يتم إطلاق حدث `AUDIO_CHUNK_READY` مع مقطع صوتي جديد.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 17-تنفيذ-وحدة-اختيار-المحرك-الذكي

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md
**التبعيات:** 16-تنفيذ-وحدة-التقاط-الصوت.md, المرحلة-2-التصميم-المعماري/10-تصميم-وحدة-اختيار-المحرك-الذكي.md

## 🎯 الغرض والهدف
تنفيذ وحدة اختيار المحرك الذكي (`SmartEngineSelector`) التي تقع في `Service Worker`، وهي مسؤولة عن اختيار محرك الترجمة الأمثل بناءً على مجموعة من المعايير (مثل اللغة، التكلفة، الأداء، التوفر).

## 📋 المتطلبات الأساسية
ملف `16-تنفيذ-وحدة-التقاط-الصوت.md` (للتسلسل)، و `10-تصميم-وحدة-اختيار-المحرك-الذكي.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/10-تصميم-وحدة-اختيار-المحرك-الذكي.md` - يوفر التصميم التفصيلي للوحدة.
- `13-تنفيذ-وحدة-التكامل-مع-الطرف-الثالث.md` - يوفر قائمة بمحركات الترجمة المتاحة.

### الملفات اللاحقة  
- `18-تنفيذ-وحدة-التعرف-على-الكلام.md` - لا يوجد ارتباط مباشر.
- `19-تنفيذ-وحدة-نظام-الخبراء.md` - يتم استخدام هذه الوحدة لتقييم جودة الترجمة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `SmartEngineSelector`
**الوصف:** إنشاء فئة `SmartEngineSelector` في `src/core/services/SmartEngineSelector.ts` لتغليف منطق الاختيار.

```typescript
// src/core/services/SmartEngineSelector.ts
import { AppConfig, LanguageCode } from '../../types/common';

class SmartEngineSelector {
  private engineMetrics: Map<string, EngineMetric> = new Map(); // مقاييس الأداء والتكلفة

  constructor() {
    // تهيئة المقاييس الأولية
  }

  async selectEngine(sourceLang: LanguageCode, targetLang: LanguageCode, config: AppConfig): Promise<string> {
    // 1. تصفية المحركات المتاحة (بناءً على مفاتيح API في config)
    const availableEngines = this.getAvailableEngines(config);

    // 2. تطبيق قواعد نظام الخبراء (Expert System)
    // مثال: إذا كانت اللغة الهدف هي الصينية، استخدم محرك X.
    const expertEngine = this.applyExpertRules(sourceLang, targetLang);
    if (expertEngine && availableEngines.includes(expertEngine)) {
      return expertEngine;
    }

    // 3. تطبيق قواعد الأداء والتكلفة
    // مثال: اختيار الأسرع والأرخص بين المتاحين.
    const bestEngine = this.getBestEngineByMetrics(availableEngines);

    // 4. Fallback: العودة إلى المحرك الافتراضي
    return bestEngine || config.defaultEngineId;
  }

  private getAvailableEngines(config: AppConfig): string[] {
    // ... منطق التحقق من مفاتيح API وتوفر المحركات
    return ['google', 'deepl'];
  }

  private applyExpertRules(sourceLang: LanguageCode, targetLang: LanguageCode): string | null {
    // ... منطق نظام الخبراء (سيتم تفصيله في المرحلة 6)
    return null;
  }

  private getBestEngineByMetrics(engines: string[]): string | null {
    // ... منطق اختيار الأفضل بناءً على latency و cost
    return engines[0] || null;
  }
}
```

## ⚙️ آلية العمل
يتم استدعاء `SmartEngineSelector` بواسطة `TranslationOrchestrator` قبل بدء عملية الترجمة، مما يضمن استخدام المحرك الأكثر ملاءمة لكل طلب.

## 🔍 حالات الاستخدام
*   **حالة 1:** إذا كانت الترجمة من الإنجليزية إلى اليابانية، يختار `Selector` محركًا معروفًا بدقته في هذه اللغات.
*   **حالة 2:** إذا كان محرك Google Translate غير متاح (بسبب Rate Limit)، يختار `Selector` محركًا احتياطيًا.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 18-تنفيذ-وحدة-التعرف-على-الكلام

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/18-تنفيذ-وحدة-التعرف-على-الكلام.md
**التبعيات:** 17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md, المرحلة-2-التصميم-المعماري/11-تصميم-وحدة-التعرف-على-الكلام.md

## 🎯 الغرض والهدف
تنفيذ وحدة التعرف على الكلام (`STTUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن استقبال مقاطع الصوت من `AudioCaptureUnit` ومعالجتها باستخدام محرك STT المختار (سحابي أو محلي) وتحويلها إلى نص.

## 📋 المتطلبات الأساسية
ملف `17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md` (للتسلسل)، و `11-تصميم-وحدة-التعرف-على-الكلام.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/11-تصميم-وحدة-التعرف-على-الكلام.md` - يوفر التصميم التفصيلي للوحدة.
- `16-تنفيذ-وحدة-التقاط-الصوت.md` - يرسل مقاطع الصوت إلى هذه الوحدة.

### الملفات اللاحقة  
- `19-تنفيذ-وحدة-نظام-الخبراء.md` - لا يوجد ارتباط مباشر.
- `DELIVERABLES_PHASE3.md` - لتحديد تسليمات المرحلة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `STTUnit`
**الوصف:** إنشاء فئة `STTUnit` في `src/core/managers/STTUnit.ts` لتغليف وظائف التعرف على الكلام.

```typescript
// src/core/managers/STTUnit.ts
import { ISTTEngine, AudioSegment, STTResult } from '../../types/common';
import { GoogleSTTAdapter } from '../adapters/GoogleSTTAdapter';
import { LocalWhisperEngine } from '../adapters/LocalWhisperEngine';

class STTUnit {
  private engines: Map<string, ISTTEngine> = new Map();
  private currentEngineId: string = 'google'; // الافتراضي

  constructor() {
    // تهيئة محركات STT
    this.engines.set('google', new GoogleSTTAdapter());
    this.engines.set('local', new LocalWhisperEngine());
  }

  async processAudio(audioSegment: AudioSegment): Promise<void> {
    const engine = this.engines.get(this.currentEngineId);
    if (!engine) {
      throw new Error(`STT Engine ${this.currentEngineId} not found.`);
    }

    try {
      // 1. تنفيذ عملية التعرف على الكلام
      const sttResult: STTResult = await engine.process(audioSegment);
      
      // 2. إرسال النتيجة إلى TranslationOrchestrator
      // TranslationOrchestrator.handleSTTResult(sttResult);
    } catch (error) {
      // 3. معالجة الخطأ (ErrorManager)
    }
  }

  setEngine(engineId: string): void {
    this.currentEngineId = engineId;
  }
}
```

### 2. تنفيذ واجهات التكيف (Adapters)
**الوصف:** إنشاء واجهات التكيف لمحركات STT.
*   **`GoogleSTTAdapter.ts`:** يتعامل مع Google Speech-to-Text API.
*   **`LocalWhisperEngine.ts`:** يتعامل مع نموذج Whisper الذي يعمل عبر WebAssembly (سيتم تفصيله في المرحلة 5).

## ⚙️ آلية العمل
يتم استخدام `STTUnit` كطبقة تجريد لتحويل الصوت إلى نص، مما يتيح التبديل السهل بين المحركات السحابية والمحلية.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتلقى `STTUnit` مقطع صوتي، ويستخدم `GoogleSTTAdapter` لتحويله إلى نص.
*   **حالة 2:** يتم تغيير المحرك إلى `local`، ويستخدم `LocalWhisperEngine` لمعالجة الصوت محليًا.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# 19-تنفيذ-وحدة-نظام-الخبراء

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/19-تنفيذ-وحدة-نظام-الخبراء.md
**التبعيات:** 18-تنفيذ-وحدة-التعرف-على-الكلام.md, المرحلة-2-التصميم-المعماري/12-تصميم-وحدة-نظام-الخبراء.md

## 🎯 الغرض والهدف
تنفيذ وحدة نظام الخبراء (`ExpertSystemUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تطبيق قواعد تحسين الترجمة (مثل المصطلحات المخصصة، قواعد النحو، التفضيلات السياقية) على النص المترجم.

## 📋 المتطلبات الأساسية
ملف `18-تنفيذ-وحدة-التعرف-على-الكلام.md` (للتسلسل)، و `12-تصميم-وحدة-نظام-الخبراء.md` لتحديد منطق الوحدة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-2-التصميم-المعماري/12-تصميم-وحدة-نظام-الخبراء.md` - يوفر التصميم التفصيلي للوحدة.
- `17-تنفيذ-وحدة-اختيار-المحرك-الذكي.md` - تستخدم هذه الوحدة قواعد الخبراء لاختيار المحرك.

### الملفات اللاحقة  
- `DELIVERABLES_PHASE3.md` - لتحديد تسليمات المرحلة.
- `ACCEPTANCE_CRITERIA_PHASE3.md` - لتحديد معايير القبول.

## 🏗 البنية التفصيلية
### 1. تنفيذ `ExpertSystemUnit`
**الوصف:** إنشاء فئة `ExpertSystemUnit` في `src/core/services/ExpertSystemUnit.ts` لتغليف وظائف نظام الخبراء.

```typescript
// src/core/services/ExpertSystemUnit.ts
import { TranslationResult } from '../../types/common';

class ExpertSystemUnit {
  private terminologyDB: TerminologyDB; // قاعدة بيانات المصطلحات (من StorageManager)

  constructor() {
    // تهيئة قاعدة بيانات المصطلحات
    this.terminologyDB = new TerminologyDB();
  }

  async applyPostProcessing(result: TranslationResult): Promise<TranslationResult> {
    let processedText = result.translatedText;

    // 1. تطبيق قواعد المصطلحات المخصصة
    const terms = await this.terminologyDB.getAllTerms();
    terms.forEach(term => {
      // استبدال المصطلح الأصلي بالترجمة المخصصة
      processedText = processedText.replace(new RegExp(term.original, 'g'), term.customTranslation);
    });

    // 2. تطبيق قواعد النحو والأسلوب (إذا لزم الأمر)
    // ...

    return { ...result, translatedText: processedText };
  }

  async getEngineSelectionRules(): Promise<EngineSelectionRule[]> {
    // إرجاع القواعد التي يستخدمها SmartEngineSelector
    return [];
  }
}
```

### 2. قاعدة بيانات المصطلحات
**الوصف:** يتم تخزين المصطلحات المخصصة في `StorageManager` (IndexedDB) لضمان استمراريتها.

## ⚙️ آلية العمل
يتم استدعاء `ExpertSystemUnit` بواسطة `TranslationOrchestrator` بعد الحصول على نتيجة الترجمة من المحرك، وقبل إرسالها للعرض، لضمان أعلى جودة للترجمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** المستخدم يحدد أن كلمة "Overlay" يجب أن تترجم إلى "طبقة التراكب" دائمًا، ويقوم `ExpertSystemUnit` بتطبيق هذه القاعدة.
*   **حالة 2:** `SmartEngineSelector` يستدعي `getEngineSelectionRules()` لتحديد المحرك الأمثل.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# ACCEPTANCE_CRITERIA_PHASE3

<!-- Metadata -->
**النوع:** معايير قبول
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/ACCEPTANCE_CRITERIA_PHASE3.md
**التبعيات:** DELIVERABLES_PHASE3.md

## 🎯 الغرض والهدف
تحديد المعايير الموضوعية التي يجب استيفاؤها لقبول تسليمات المرحلة الثالثة والموافقة على الانتقال إلى المرحلة الرابعة (نظام الترجمة متعدد المحركات).

## 📋 المتطلبات الأساسية
ملف `DELIVERABLES_PHASE3.md` لتحديد التسليمات التي سيتم تقييمها.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `DELIVERABLES_PHASE3.md` - لتحديد التسليمات التي سيتم تقييمها.

### الملفات اللاحقة  
- `SUCCESS_METRICS_PHASE3.md` - لتحديد مقاييس النجاح التي سيتم قياسها.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/01-تنفيذ-واجهة-محرك-الترجمة.md` (المرحلة 4) - يتم البدء في هذه المرحلة بعد استيفاء معايير القبول.

## 🏗 البنية التفصيلية
### 1. اكتمال الوثائق (19 ملفًا)
**الوصف:** يجب أن تكون جميع الملفات الـ 19 المذكورة في `DELIVERABLES_PHASE3.md` موجودة ومكتملة.
**الوظيفة:** ضمان عدم وجود وثائق تنفيذ مفقودة.
**العلاقات:** ترتبط بـ `structure_validator.py`.

### 2. اتساق التنفيذ مع التصميم
**الوصف:** يجب أن يتطابق الهيكل التنفيذي الموثق في هذه المرحلة مع التصميم المعماري المحدد في المرحلة 2.
**الوظيفة:** ضمان أن التنفيذ يتبع المخطط الهندسي.
**العلاقات:** ترتبط بـ `content_validator.py` (للتأكد من الإشارة إلى التصميم المعماري).

### 3. جاهزية النواة الأساسية
**الوصف:** يجب أن تكون جميع الوحدات الأساسية (التخزين، الإعدادات، الأمان، الرسائل) جاهزة للاستخدام من قبل الوحدات المتقدمة في المراحل التالية.
**الوظيفة:** التأكد من أن الأساس التقني سليم.
**العلاقات:** ترتبط بـ `04-تنفيذ-نظام-الرسائل-والأحداث.md` و `05-تنفيذ-وحدة-إدارة-التخزين.md`.

## ⚙️ آلية العمل
يتم استخدام أدوات التحقق (structure_validator.py و content_validator.py) لتقييم اكتمال الوثائق واتساقها.

## 🔍 حالات الاستخدام
*   **حالة 1:** إذا كانت واجهة `IStorageManager` غير موجودة في `src/types/common.ts`، فإن معيار جاهزية النواة الأساسية يفشل.
*   **حالة 2:** إذا كان ملف `07-تنفيذ-وحدة-الأمان-والخصوصية.md` لا يوضح استخدام `Web Crypto API` كما هو مطلوب في التصميم، فإن معيار الاتساق يفشل.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# DELIVERABLES_PHASE3

<!-- Metadata -->
**النوع:** تسليمات
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/DELIVERABLES_PHASE3.md
**التبعيات:** 19-تنفيذ-وحدة-نظام-الخبراء.md

## 🎯 الغرض والهدف
تحديد قائمة واضحة ومفصلة بالمنتجات النهائية (Deliverables) التي يجب تسليمها عند الانتهاء من المرحلة الثالثة (النواة الأساسية والبنية التحتية).

## 📋 المتطلبات الأساسية
جميع ملفات التنفيذ التي تم إنشاؤها في هذه المرحلة (19 ملفًا).

## 🔗 العلاقات والترابط
### الملفات السابقة
- `19-تنفيذ-وحدة-نظام-الخبراء.md` - آخر ملف تنفيذي تم إنشاؤه.

### الملفات اللاحقة  
- `ACCEPTANCE_CRITERIA_PHASE3.md` - لتحديد معايير قبول هذه التسليمات.
- `SUCCESS_METRICS_PHASE3.md` - لتحديد مقاييس النجاح.

## 🏗 البنية التفصيلية
### 1. وثائق التنفيذ (19 ملفًا)
**الوصف:** مجموعة كاملة من وثائق التنفيذ التي تم إنشاؤها في هذه المرحلة.
**الوظيفة:** توفير المخطط التفصيلي لتنفيذ النواة الأساسية.
**العلاقات:** ترتبط بـ `01-إعداد-بيئة-التطوير.md` حتى `19-تنفيذ-وحدة-نظام-الخبراء.md`.

### 2. البنية التحتية للكود (Code Infrastructure)
**الوصف:** الهيكل الأساسي للمشروع، بما في ذلك:
*   **هيكلة الملفات:** مجلدات `src/core` و `src/types` و `src/ui` كما هو محدد في `02-هيكلة-ملفات-المشروع.md`.
*   **الأنواع الموحدة:** ملفات `common.ts` و `messages.ts` و `exceptions.ts` التي تحتوي على جميع الواجهات والأنواع.
*   **النواة الأساسية:** تنفيذ جميع وحدات الإدارة والخدمات (Managers and Services) التي تم توثيقها في هذه المرحلة.

### 3. قائمة التحقق من الجودة (المرحلة 3)
**الوصف:** قائمة التحقق من الجودة المكتملة لهذه المرحلة.
**الوظيفة:** التأكد من أن جميع الملفات تتبع القوالب الموحدة وتم التحقق من ترابطها.
**العلاقات:** ترتبط بـ `quality_checklist.md` (ملف أساسي مشترك).

## ⚙️ آلية العمل
يتم تجميع جميع الملفات التي تم إنشاؤها في المرحلة 3 في مجلد واحد، ويتم التحقق من اكتمالها وصحتها قبل الانتقال إلى المرحلة التالية.

## 🔍 حالات الاستخدام
*   **حالة 1:** فريق التطوير يستخدم هذه الوثائق كمرجع وحيد لبدء كتابة الكود الفعلي لوحدات الترجمة و STT في المراحل التالية.
*   **حالة 2:** مدير المشروع يستخدم هذا الملف للتحقق من أن جميع وثائق التنفيذ جاهزة قبل بدء مرحلة الاختبار.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
# SUCCESS_METRICS_PHASE3

<!-- Metadata -->
**النوع:** مقاييس نجاح
**المرحلة:** 3
**المسار:** المرحلة-3-النواة-الأساسية/SUCCESS_METRICS_PHASE3.md
**التبعيات:** ACCEPTANCE_CRITERIA_PHASE3.md

## 🎯 الغرض والهدف
تحديد المقاييس الكمية والنوعية التي سيتم استخدامها لتقييم نجاح المرحلة الثالثة (النواة الأساسية والبنية التحتية).

## 📋 المتطلبات الأساسية
ملف `ACCEPTANCE_CRITERIA_PHASE3.md` لتحديد معايير القبول التي يجب قياسها.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `ACCEPTANCE_CRITERIA_PHASE3.md` - لتحديد معايير القبول التي يجب قياسها.

### الملفات اللاحقة  
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/01-تنفيذ-واجهة-محرك-الترجمة.md` (المرحلة 4) - يتم استخدام هذه المقاييس لتقييم جاهزية المرحلة 4.

## 🏗 البنية التفصيلية
### 1. مقاييس الجودة (Quality Metrics)
| المقياس | الهدف | الوصف |
| :--- | :--- | :--- |
| **اكتمال الوثائق** | 100% | يجب أن تكون جميع الملفات الـ 19 موجودة ومكتملة. |
| **نسبة الترابط** | > 95% | نسبة الروابط الداخلية الصحيحة بين الملفات. |
| **تغطية الواجهات** | 100% | يجب أن تكون جميع الواجهات المحددة في `06-تصميم-الواجهات-والأنواع-الموحدة.md` قد تم تنفيذها في `src/types`. |

### 2. مقاييس الكفاءة (Efficiency Metrics)
| المقياس | الهدف | الوصف |
| :--- | :--- | :--- |
| **جاهزية النواة** | 100% | يجب أن تكون جميع الوحدات الأساسية (Storage, Settings, Security, Messaging) جاهزة للاستخدام. |
| **الالتزام بالهيكل** | 100% | يجب أن يتبع هيكل الملفات الموثق في `02-هيكلة-ملفات-المشروع.md`. |

## ⚙️ آلية العمل
يتم استخدام هذه المقاييس لتقييم جودة التنفيذ الأساسي قبل الانتقال إلى تطوير الميزات المتقدمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** إذا كان هناك ملف واحد مفقود، فإن مقياس اكتمال الوثائق يفشل.
*   **حالة 2:** إذا لم يتم تنفيذ واجهة `ITranslationEngine` في `src/types/common.ts`، فإن مقياس تغطية الواجهات يفشل.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
