# وثيقة المرحلة الثامنة: التخزين وإدارة البيانات

---

## 1. 01-هيكل-قاعدة-البيانات

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/01-هيكل-قاعدة-البيانات.md
**التبعيات:** المرحلة-7-واجهة-المستخدم-والتجربة/20-تصميم-شاشة-إدارة-التحديثات-والإصدارات.md

### 🎯 الغرض والهدف
تحديد الهيكل المنطقي والفيزيائي لقاعدة البيانات المستخدمة لتخزين بيانات التطبيق، مع التركيز على الكفاءة والسرعة في الوصول إلى بيانات الترجمة والإعدادات.

### 📋 المتطلبات الأساسية
ملف `المرحلة-7-واجهة-المستخدم-والتجربة/20-تصميم-شاشة-إدارة-التحديثات-والإصدارات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `المرحلة-7-واجهة-المستخدم-والتجربة/20-تصميم-شاشة-إدارة-التحديثات-والإصدارات.md` - يوفر السياق العام لواجهة المستخدم.

**الملفات اللاحقة:**
- `02-نماذج-البيانات-والكائنات.md` - يحدد نماذج البيانات بناءً على هذا الهيكل.
- `DATABASE_SCHEMA_DESIGN.md` - يحدد تصميم مخطط قاعدة البيانات.

### 🏗 البنية التفصيلية
#### 1. اختيار نوع قاعدة البيانات
*   **النوع المقترح:** قاعدة بيانات NoSQL خفيفة الوزن (مثل IndexedDB للمتصفح أو SQLite/Realm للتطبيق المكتبي) لتخزين البيانات المحلية، مع خيار قاعدة بيانات سحابية (مثل MongoDB أو PostgreSQL) لتخزين البيانات المشتركة (مثل الإعدادات المخصصة للمستخدمين المسجلين).
*   **السبب:** السرعة في القراءة والكتابة، والمرونة في تخزين البيانات غير المهيكلة (مثل نتائج STT).

#### 2. الجداول/المجموعات الرئيسية
| الاسم (الجدول/المجموعة) | الغرض | الحقول الرئيسية |
| :--- | :--- | :--- |
| **User_Settings** | تخزين إعدادات المستخدم (اللغة، المحركات المفضلة، إلخ). | `user_id` (مفتاح أساسي)، `settings_json` |
| **Translation_Cache** | تخزين نتائج الترجمة المؤقتة لتسريع الأداء. | `source_hash` (مفتاح أساسي)، `target_text`، `timestamp` |
| **STT_Cache** | تخزين نتائج التعرف على الكلام المؤقتة. | `audio_hash` (مفتاح أساسي)، `recognized_text`، `timestamp` |
| **Custom_Terms** | تخزين المصطلحات المخصصة للمستخدمين. | `user_id`، `source_term`، `target_term` |
| **Error_Logs** | تخزين سجلات الأخطاء والتحذيرات. | `log_id` (مفتاح أساسي)، `error_code`، `details_json` |

#### 3. متطلبات الأداء
*   **زمن الاستجابة:** يجب أن يكون زمن استرجاع البيانات من الذاكرة المؤقتة أقل من 50 مللي ثانية.
*   **الحجم:** يجب أن تكون قاعدة البيانات المحلية قادرة على تخزين ما لا يقل عن 10000 إدخال في الذاكرة المؤقتة.

### ⚙️ آلية العمل
يتم التفاعل مع هذا الهيكل عبر وحدة إدارة التخزين (المرحلة 3) لضمان الاتساق والأمان.

---

## 2. 02-نماذج-البيانات-والكائنات

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/02-نماذج-البيانات-والكائنات.md
**التبعيات:** 01-هيكل-قاعدة-البيانات.md

### 🎯 الغرض والهدف
تحديد نماذج البيانات (Data Models) والكائنات (Objects) المستخدمة في النظام، لضمان الاتساق في تبادل البيانات بين الوحدات المختلفة (STT، الترجمة، الواجهة).

### 📋 المتطلبات الأساسية
ملف `01-هيكل-قاعدة-البيانات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `01-هيكل-قاعدة-البيانات.md` - يوفر سياق هيكل قاعدة البيانات.

**الملفات اللاحقة:**
- `03-إدارة-الحالة-والجلسات.md` - يعتمد على هذه النماذج لإدارة الحالة.
- `DATA_MODEL_SPECIFICATIONS.md` - يحدد مواصفات نماذج البيانات.

### 🏗 البنية التفصيلية
#### 1. نموذج نتيجة التعرف على الكلام (STTResult)
| الحقل | النوع | الوصف |
| :--- | :--- | :--- |
| `text` | `string` | النص المعترف به. |
| `confidence` | `number` | مستوى الثقة في النص (0.0 - 1.0). |
| `engine_used` | `string` | اسم محرك STT المستخدم. |
| `language_code` | `string` | رمز اللغة المعترف بها. |
| `speaker_id` | `string` | معرف المتحدث (إذا كان متاحًا). |
| `timestamp` | `number` | وقت بدء مقطع الصوت (بالمللي ثانية). |

#### 2. نموذج نتيجة الترجمة (TranslationResult)
| الحقل | النوع | الوصف |
| :--- | :--- | :--- |
| `source_text` | `string` | النص الأصلي الذي تم ترجمته. |
| `target_text` | `string` | النص المترجم. |
| `engine_used` | `string` | اسم محرك الترجمة المستخدم. |
| `source_lang` | `string` | رمز اللغة المصدر. |
| `target_lang` | `string` | رمز اللغة الهدف. |
| `quality_score` | `number` | درجة جودة الترجمة (مقدمة من نظام الخبراء). |

#### 3. نموذج حالة الجلسة (SessionState)
| الحقل | النوع | الوصف |
| :--- | :--- | :--- |
| `session_id` | `string` | معرف الجلسة الفريد. |
| `is_active` | `boolean` | حالة الجلسة (نشطة/غير نشطة). |
| `current_engine` | `string` | المحرك الحالي المستخدم للترجمة. |
| `stt_engine` | `string` | المحرك الحالي المستخدم لـ STT. |
| `user_settings` | `object` | نسخة من إعدادات المستخدم الحالية. |
| `last_activity` | `number` | آخر وقت نشاط للجلسة. |

### ⚙️ آلية العمل
يتم استخدام هذه النماذج بواسطة وحدة الأنواع الموحدة (المرحلة 3) لضمان أن جميع البيانات التي يتم تمريرها بين الوحدات تتبع هيكلاً متوقعًا.

---

## 3. 03-إدارة-الحالة-والجلسات

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/03-إدارة-الحالة-والجلسات.md
**التبعيات:** 02-نماذج-البيانات-والكائنات.md

### 🎯 الغرض والهدف
تحديد الآلية التي يتم بها إدارة حالة التطبيق (Application State) وحالة الجلسة (Session State)، بما في ذلك التغييرات، التخزين المؤقت، والمزامنة بين المكونات المختلفة.

### 📋 المتطلبات الأساسية
ملف `02-نماذج-البيانات-والكائنات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `02-نماذج-البيانات-والكائنات.md` - يوفر نماذج البيانات المستخدمة في الحالة.

**الملفات اللاحقة:**
- `04-تخزين-الترجمات-والنتائج.md` - يحدد كيفية تخزين البيانات الناتجة.
- `STATE_MANAGEMENT_STRATEGY.md` - يحدد استراتيجية إدارة الحالة.

### 🏗 البنية التفصيلية
#### 1. مكونات إدارة الحالة
*   **المخزن المركزي (Central Store):** مكان واحد لتخزين الحالة الكاملة للتطبيق (SessionState).
*   **المغيرات (Mutators):** وظائف نقية (Pure Functions) هي الطريقة الوحيدة لتغيير الحالة.
*   **المشتركون (Subscribers):** المكونات التي تستمع إلى التغييرات في الحالة وتتفاعل معها.

#### 2. دورة حياة الجلسة
1.  **البدء:** عند تشغيل التطبيق، يتم تحميل الحالة الافتراضية أو الحالة المحفوظة سابقًا من التخزين المحلي.
2.  **النشاط:** يتم تحديث الحالة عند كل حدث (مثل تغيير الإعدادات، وصول نتيجة STT، وصول نتيجة ترجمة).
3.  **الحفظ:** يتم حفظ الحالة تلقائيًا كل 5 ثوانٍ، أو عند إغلاق التطبيق.
4.  **الانتهاء:** عند تسجيل الخروج أو إغلاق التطبيق، يتم حفظ الحالة النهائية.

#### 3. مزامنة الحالة
*   **المزامنة المحلية:** يتم استخدام نظام رسائل (المرحلة 3) لإخطار المكونات بالتغييرات في الحالة.
*   **المزامنة السحابية (اختياري):** إذا كان المستخدم مسجلاً للدخول، يتم مزامنة الإعدادات الرئيسية مع التخزين السحابي (المرحلة 3/13).

### ⚙️ آلية العمل
تعتمد إدارة الحالة على مبدأ **تدفق البيانات أحادي الاتجاه (Unidirectional Data Flow)** لضمان إمكانية التنبؤ بالحالة وتتبع الأخطاء.

---

## 4. 04-تخزين-الترجمات-والنتائج

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/04-تخزين-الترجمات-والنتائج.md
**التبعيات:** 03-إدارة-الحالة-والجلسات.md

### 🎯 الغرض والهدف
تحديد كيفية تخزين نتائج التعرف على الكلام (STT) ونتائج الترجمة، بما في ذلك آليات التخزين المؤقت (Caching) لتقليل زمن الاستجابة وتكاليف استخدام واجهات البرمجة السحابية.

### 📋 المتطلبات الأساسية
ملف `03-إدارة-الحالة-والجلسات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `03-إدارة-الحالة-والجلسات.md` - يوفر سياق إدارة الجلسة التي يتم فيها تخزين النتائج.

**الملفات اللاحقة:**
- `05-نظام-النسخ-الاحتياطي.md` - يعتمد على هذا التخزين لعمل نسخ احتياطية.
- `cache-service-template.ts` - يحدد قالب الكود لخدمة التخزين المؤقت.

### 🏗 البنية التفصيلية
#### 1. آلية التخزين المؤقت (Caching)
*   **مفتاح التخزين المؤقت:** يتم إنشاء مفتاح فريد (Hash) لكل طلب STT ولكل طلب ترجمة بناءً على النص المصدر، اللغة المصدر، اللغة الهدف، والمحرك المستخدم.
*   **التحقق من التخزين المؤقت:** قبل إرسال أي طلب إلى محرك سحابي، يتم التحقق من وجود المفتاح في جدول `Translation_Cache` أو `STT_Cache`.
*   **مدة الصلاحية (TTL):** يتم تعيين مدة صلاحية للنتائج المؤقتة (مثلاً 7 أيام) لضمان تحديث البيانات بانتظام.

#### 2. تخزين نتائج STT
*   يتم تخزين نتائج STT في جدول `STT_Cache` باستخدام نموذج `STTResult`.
*   يتم تخزين النص المعترف به، ومستوى الثقة، والمحرك المستخدم.
*   يتم استخدام هذا التخزين المؤقت لتجنب إعادة معالجة نفس المقطع الصوتي.

#### 3. تخزين نتائج الترجمة
*   يتم تخزين نتائج الترجمة في جدول `Translation_Cache` باستخدام نموذج `TranslationResult`.
*   يتم تخزين النص المصدر، النص الهدف، والمحرك المستخدم.
*   يتم استخدام هذا التخزين المؤقت لتجنب إعادة ترجمة نفس الجملة.

### ⚙️ آلية العمل
تعتمد هذه الآلية على مبدأ **"لا تترجم أو تتعرف على ما تم ترجمته أو التعرف عليه بالفعل"** لتقليل التكاليف وتحسين الأداء.

---

## 5. 05-نظام-النسخ-الاحتياطي

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/05-نظام-النسخ-الاحتياطي.md
**التبعيات:** 04-تخزين-الترجمات-والنتائج.md

### 🎯 الغرض والهدف
تحديد إستراتيجية شاملة للنسخ الاحتياطي (Backup) واستعادة البيانات (Recovery) لضمان استمرارية الخدمة وحماية بيانات المستخدمين (الإعدادات، المصطلحات المخصصة).

### 📋 المتطلبات الأساسية
ملف `04-تخزين-الترجمات-والنتائج.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `04-تخزين-الترجمات-والنتائج.md` - يحدد البيانات التي يجب عمل نسخة احتياطية لها.

**الملفات اللاحقة:**
- `06-إدارة-البيانات-اللغوية.md` - يحدد كيفية إدارة البيانات اللغوية التي يجب حمايتها.
- `BACKUP_RECOVERY_PLAN.md` - يحدد خطة النسخ الاحتياطي والاستعادة.

### 🏗 البنية التفصيلية
#### 1. أنواع البيانات التي يتم نسخها احتياطيًا
*   **بيانات المستخدم الحساسة:** إعدادات المستخدم، المصطلحات المخصصة، سجلات الترخيص والاستخدام.
*   **بيانات النظام:** سجلات الأخطاء، سجلات المراقبة، تكوينات النظام الديناميكية.
*   **البيانات المؤقتة:** لا يتم عمل نسخة احتياطية لبيانات التخزين المؤقت (Cache) حيث يمكن إعادة إنشائها.

#### 2. إستراتيجية النسخ الاحتياطي
| النوع | التردد | الموقع | آلية الاستعادة |
| :--- | :--- | :--- | :--- |
| **كامل (Full)** | أسبوعيًا | تخزين سحابي آمن (S3 أو ما يعادله) | استعادة كاملة للنظام. |
| **تزايدي (Incremental)** | يوميًا | تخزين سحابي آمن | استعادة سريعة لأحدث التغييرات. |
| **محلي (Local)** | عند كل إغلاق للتطبيق | ملف محلي مشفر | استعادة سريعة للإعدادات المحلية. |

#### 3. آلية الاستعادة (Recovery)
*   **الاستعادة التلقائية:** عند فشل النظام، يتم محاولة استعادة أحدث نسخة احتياطية محلية تلقائيًا.
*   **الاستعادة اليدوية:** يمكن للمستخدم طلب استعادة بياناته من النسخة السحابية (يتطلب مصادقة).
*   **اختبار الاستعادة:** يجب إجراء اختبار دوري (شهريًا) لعملية الاستعادة لضمان فعاليتها.

### ⚙️ آلية العمل
يتم تنفيذ نظام النسخ الاحتياطي بواسطة وحدة إدارة التخزين (المرحلة 3) بالتعاون مع وحدة الأمان (المرحلة 9) لضمان تشفير البيانات.

---

## 6. 06-إدارة-البيانات-اللغوية

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/06-إدارة-البيانات-اللغوية.md
**التبعيات:** 05-نظام-النسخ-الاحتياطي.md

### 🎯 الغرض والهدف
تحديد كيفية إدارة وتخزين البيانات اللغوية الخاصة بالنظام، مثل قوائم اللغات المدعومة، المصطلحات المخصصة، وقواعد التحسين اللغوي.

### 📋 المتطلبات الأساسية
ملف `05-نظام-النسخ-الاحتياطي.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `05-نظام-النسخ-الاحتياطي.md` - يحدد كيفية حماية هذه البيانات.

**الملفات اللاحقة:**
- `07-تخزين-إعدادات-المستخدم.md` - يعتمد على هذه البيانات لتحديد خيارات المستخدم.
- `DATA_MANAGEMENT_POLICIES.md` - يحدد سياسات إدارة البيانات.

### 🏗 البنية التفصيلية
#### 1. قوائم اللغات المدعومة
*   **التخزين:** يتم تخزين قائمة اللغات المدعومة (رمز اللغة، الاسم المحلي، الاسم الإنجليزي) في ملف تكوين ثابت (Configuration File) يتم تحميله عند بدء التشغيل.
*   **التحديث:** يتم تحديث القائمة عبر آلية التكوين الديناميكي (المرحلة 10) دون الحاجة لإعادة تشغيل التطبيق.

#### 2. المصطلحات المخصصة (Custom Terminology)
*   **التخزين:** يتم تخزينها في جدول `Custom_Terms` (المرحلة 8/01).
*   **الهيكل:** `user_id`، `source_term`، `target_term`، `context` (اختياري).
*   **الاستخدام:** يتم استرجاعها بواسطة وحدة نظام الخبراء (المرحلة 6) لتطبيقها على نتائج الترجمة.

#### 3. قواعد التحسين اللغوي (Linguistic Enhancement Rules)
*   **الغرض:** قواعد لتحسين جودة الترجمة (مثل تصحيح الأخطاء الشائعة، التفضيلات النحوية).
*   **التخزين:** يتم تخزينها في ملفات JSON أو قاعدة بيانات مخصصة يتم تحميلها بواسطة وحدة نظام الخبراء.
*   **الإدارة:** يتم إدارتها وتحديثها مركزيًا.

### ⚙️ آلية العمل
تضمن هذه الإدارة أن النظام يستخدم أحدث البيانات اللغوية لتحقيق أعلى دقة وجودة في الترجمة.

---

## 7. 07-تخزين-إعدادات-المستخدم

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/07-تخزين-إعدادات-المستخدم.md
**التبعيات:** 06-إدارة-البيانات-اللغوية.md

### 🎯 الغرض والهدف
تحديد الآلية التفصيلية لتخزين واسترجاع إعدادات المستخدم، بما في ذلك الإعدادات المحلية (المتصفح/الجهاز) والإعدادات السحابية (للمستخدمين المسجلين).

### 📋 المتطلبات الأساسية
ملف `06-إدارة-البيانات-اللغوية.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `06-إدارة-البيانات-اللغوية.md` - يوفر البيانات اللغوية التي يمكن للمستخدم اختيارها.

**الملفات اللاحقة:**
- `08-نظام-الفهرسة-والبحث.md` - لا يوجد ترابط مباشر، لكنه يتبع التسلسل المنطقي.
- `STATE_MANAGEMENT_STRATEGY.md` - يعتمد على هذه الإعدادات لتحديد الحالة الأولية.

### 🏗 البنية التفصيلية
#### 1. أنواع الإعدادات
| النوع | الوصف | مكان التخزين الأساسي |
| :--- | :--- | :--- |
| **إعدادات الواجهة** | المظهر، حجم الخط، موضع الترجمة. | التخزين المحلي (Local Storage/IndexedDB) |
| **إعدادات المحرك** | المحركات المفضلة لـ STT والترجمة، مفاتيح API الشخصية. | التخزين المحلي (مشفر) والسحابي (مشفر) |
| **إعدادات اللغة** | اللغة المصدر والهدف الافتراضية. | التخزين المحلي والسحابي |
| **إعدادات الأداء** | جودة الصوت، زمن الاستجابة المطلوب. | التخزين المحلي |

#### 2. آلية التخزين والاسترجاع
*   **التخزين المحلي:** يتم استخدام IndexedDB أو Local Storage لتخزين الإعدادات بشكل فوري.
*   **التخزين السحابي:** يتم مزامنة الإعدادات الهامة (مثل المحركات المفضلة) مع جدول `User_Settings` في قاعدة البيانات السحابية (المرحلة 8/01) عند تسجيل الدخول.
*   **الأولوية:** عند بدء التشغيل، يتم تحميل الإعدادات المحلية، ثم يتم دمجها مع الإعدادات السحابية (إذا كان المستخدم مسجلاً للدخول)، مع إعطاء الأولوية للإعدادات السحابية في حالة التعارض.

#### 3. الأمان
يجب تشفير جميع الإعدادات التي تحتوي على معلومات حساسة (مثل مفاتيح API) قبل تخزينها محليًا أو سحابيًا (المرحلة 9).

### ⚙️ آلية العمل
تضمن هذه الآلية تجربة مستخدم متسقة عبر الأجهزة المختلفة.

---

## 8. 08-نظام-الفهرسة-والبحث

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/08-نظام-الفهرسة-والبحث.md
**التبعيات:** 07-تخزين-إعدادات-المستخدم.md

### 🎯 الغرض والهدف
تحديد نظام الفهرسة (Indexing) والبحث (Search) الفعال داخل البيانات المخزنة محليًا (مثل سجلات الترجمة، المصطلحات المخصصة) لتمكين المستخدم من العثور على المعلومات بسرعة.

### 📋 المتطلبات الأساسية
ملف `07-تخزين-إعدادات-المستخدم.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `07-تخزين-إعدادات-المستخدم.md` - يوفر سياق الإعدادات التي قد تؤثر على نتائج البحث.

**الملفات اللاحقة:**
- `09-تنظيف-وإدارة-البيانات.md` - يحدد كيفية تنظيف البيانات قبل فهرستها.
- `DATA_MANAGEMENT_POLICIES.md` - يحدد سياسات إدارة البيانات.

### 🏗 البنية التفصيلية
#### 1. البيانات المفهرسة
*   **المصطلحات المخصصة:** يجب فهرسة المصطلحات المصدر والهدف لتمكين البحث السريع.
*   **سجلات الترجمة:** يجب فهرسة النصوص المصدر والهدف في الذاكرة المؤقتة (Cache) لتمكين البحث في السجل.
*   **سجلات الأخطاء:** فهرسة رسائل الأخطاء لتسهيل عملية المراقبة.

#### 2. تقنيات الفهرسة
*   **الفهرسة النصية الكاملة (Full-Text Indexing):** استخدام تقنيات فهرسة متقدمة (مثل محرك بحث مدمج في IndexedDB أو SQLite) للبحث في النصوص الطويلة (سجلات الترجمة).
*   **الفهرسة الجزئية:** فهرسة الحقول الرئيسية فقط (مثل `user_id`، `engine_used`) لسرعة الاستعلام.

#### 3. متطلبات البحث
*   **البحث متعدد اللغات:** يجب أن يدعم نظام البحث البحث في النصوص المكتوبة بلغات مختلفة.
*   **البحث الغامض (Fuzzy Search):** دعم البحث الذي يسمح بالأخطاء الإملائية البسيطة أو الاختلافات في الكلمات.
*   **التصفية:** إمكانية تصفية نتائج البحث بناءً على التاريخ، اللغة، أو المحرك المستخدم.

### ⚙️ آلية العمل
يتم تحديث الفهارس بشكل غير متزامن (Asynchronously) بعد كل عملية تخزين لضمان عدم تأثيرها على أداء النظام في الوقت الفعلي.

---

## 9. 09-تنظيف-وإدارة-البيانات

<!-- Metadata -->
**النوع:** وثيقة إلزامية
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/09-تنظيف-وإدارة-البيانات.md
**التبعيات:** 08-نظام-الفهرسة-والبحث.md

### 🎯 الغرض والهدف
تحديد سياسات وإجراءات تنظيف البيانات (Data Cleaning) وإدارتها (Data Governance) لضمان جودة البيانات المخزنة، وتقليل حجم التخزين، والامتثال لمتطلبات الخصوصية.

### 📋 المتطلبات الأساسية
ملف `08-نظام-الفهرسة-والبحث.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `08-نظام-الفهرسة-والبحث.md` - يحدد البيانات التي يجب تنظيفها قبل فهرستها.

**الملفات اللاحقة:**
- `DATABASE_SCHEMA_DESIGN.md` - يحدد تصميم مخطط قاعدة البيانات.
- `data-cleaner-template.ts` - يحدد قالب الكود لمنظف البيانات.

### 🏗 البنية التفصيلية
#### 1. سياسات الاحتفاظ بالبيانات (Retention Policies)
| نوع البيانات | مدة الاحتفاظ | الإجراء بعد انتهاء المدة |
| :--- | :--- | :--- |
| **Translation_Cache** | 7 أيام | حذف تلقائي |
| **STT_Cache** | 7 أيام | حذف تلقائي |
| **Error_Logs** | 30 يومًا | أرشفة أو حذف تلقائي |
| **Custom_Terms** | حتى يقرر المستخدم حذفها | حذف يدوي بواسطة المستخدم |

#### 2. إجراءات التنظيف
*   **إزالة البيانات المكررة:** يجب التحقق من عدم وجود إدخالات مكررة في جداول التخزين المؤقت (Cache) بناءً على المفتاح الفريد (Hash).
*   **التحقق من صحة البيانات:** التحقق من أن جميع الحقول المطلوبة موجودة وأنواع البيانات صحيحة قبل التخزين.
*   **إخفاء الهوية (Anonymization):** يجب إخفاء هوية أي بيانات شخصية (مثل `user_id` في سجلات الأخطاء) قبل إرسالها إلى الخوادم السحابية للمراقبة.

#### 3. آلية التنفيذ
*   **الجدولة:** يتم تشغيل عملية التنظيف (Data Cleaner) تلقائيًا مرة واحدة يوميًا (أو عند بدء التشغيل إذا كان التطبيق مغلقًا لفترة طويلة).
*   **التنبيه:** يتم إرسال تنبيه (المرحلة 3/11) إذا فشلت عملية التنظيف أو إذا تجاوز حجم قاعدة البيانات المحلية حدًا معينًا.

### ⚙️ آلية العمل
تضمن هذه الإجراءات أن النظام يعمل بكفاءة مع بيانات نظيفة ومحدثة.

---

## 10. BACKUP_RECOVERY_PLAN

<!-- Metadata -->
**النوع:** مرجع تطوير
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/BACKUP_RECOVERY_PLAN.md
**التبعيات:** 05-نظام-النسخ-الاحتياطي.md

### 🎯 الغرض والهدف
توفير خطة تفصيلية للنسخ الاحتياطي والاستعادة (Backup and Recovery) بناءً على الإستراتيجية المحددة في الوثيقة الإلزامية، مع تحديد الأدوات والخطوات التقنية.

### 📋 المتطلبات الأساسية
ملف `05-نظام-النسخ-الاحتياطي.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `05-نظام-النسخ-الاحتياطي.md` - يوفر إستراتيجية النسخ الاحتياطي.

**الملفات اللاحقة:**
- `DATA_MANAGEMENT_POLICIES.md` - يحدد سياسات إدارة البيانات.
- `backup-manager-template.ts` - يعتمد على هذه الخطة لإنشاء قالب الكود.

### 🏗 البنية التفصيلية
#### 1. أدوات النسخ الاحتياطي
*   **النسخ المحلي:** استخدام واجهة برمجة تطبيقات IndexedDB/SQLite المدمجة لتصدير البيانات إلى ملف JSON مشفر.
*   **النسخ السحابي:** استخدام خدمة تخزين سحابي آمن (مثل AWS S3 أو ما يعادله) مع تشفير من جانب العميل (Client-Side Encryption).

#### 2. خطوات النسخ الاحتياطي التزايدي (Incremental Backup)
1.  **تحديد التغييرات:** يتم استخدام طابع زمني (`last_updated`) لتحديد الإدخالات التي تم تغييرها منذ آخر نسخة احتياطية.
2.  **تشفير البيانات:** يتم تشفير البيانات المحددة باستخدام مفتاح تشفير خاص بالمستخدم (المرحلة 9).
3.  **التحميل:** يتم تحميل البيانات المشفرة إلى الموقع السحابي الآمن.
4.  **التسجيل:** يتم تسجيل نجاح العملية في سجلات النظام.

#### 3. خطوات الاستعادة (Recovery Steps)
1.  **التحقق من المفتاح:** التحقق من مفتاح التشفير الخاص بالمستخدم.
2.  **تنزيل البيانات:** تنزيل أحدث نسخة احتياطية كاملة وجميع النسخ التزايدية اللاحقة.
3.  **فك التشفير:** فك تشفير البيانات.
4.  **الاستيراد:** استيراد البيانات إلى قاعدة البيانات المحلية، مع تطبيق النسخ التزايدية بترتيب زمني.
5.  **إعادة التشغيل:** إعادة تشغيل التطبيق لتحميل الحالة الجديدة.

### ⚙️ آلية العمل
يجب أن تكون عملية النسخ الاحتياطي والاستعادة شفافة للمستخدم قدر الإمكان، مع إمكانية الاستعادة اليدوية في حالة الطوارئ.

---

## 11. DATABASE_SCHEMA_DESIGN

<!-- Metadata -->
**النوع:** مرجع تطوير
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/DATABASE_SCHEMA_DESIGN.md
**التبعيات:** 01-هيكل-قاعدة-البيانات.md

### 🎯 الغرض والهدف
توفير مواصفات تقنية مفصلة لتصميم مخطط قاعدة البيانات (Schema) بناءً على الهيكل المحدد في الوثيقة الإلزامية.

### 📋 المتطلبات الأساسية
ملف `01-هيكل-قاعدة-البيانات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `01-هيكل-قاعدة-البيانات.md` - يوفر الهيكل العام لقاعدة البيانات.

**الملفات اللاحقة:**
- `DATA_MODEL_SPECIFICATIONS.md` - يحدد مواصفات نماذج البيانات.
- `database-model-template.ts` - يعتمد على هذا التصميم لإنشاء قالب الكود.

### 🏗 البنية التفصيلية
#### 1. مخطط جدول `User_Settings`
| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `user_id` | `TEXT` | PRIMARY KEY | معرف المستخدم الفريد. |
| `settings_json` | `TEXT` | NOT NULL | إعدادات المستخدم بتنسيق JSON (مشفرة). |
| `last_updated` | `INTEGER` | NOT NULL | طابع زمني لآخر تحديث. |

#### 2. مخطط جدول `Translation_Cache`
| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `source_hash` | `TEXT` | PRIMARY KEY | مفتاح التخزين المؤقت (Hash). |
| `target_text` | `TEXT` | NOT NULL | النص المترجم. |
| `engine_used` | `TEXT` | NOT NULL | اسم محرك الترجمة. |
| `source_lang` | `TEXT` | NOT NULL | رمز اللغة المصدر. |
| `target_lang` | `TEXT` | NOT NULL | رمز اللغة الهدف. |
| `expiry_date` | `INTEGER` | NOT NULL | طابع زمني لانتهاء الصلاحية. |

#### 3. مخطط جدول `STT_Cache`
| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `audio_hash` | `TEXT` | PRIMARY KEY | مفتاح التخزين المؤقت للصوت (Hash). |
| `recognized_text` | `TEXT` | NOT NULL | النص المعترف به. |
| `engine_used` | `TEXT` | NOT NULL | اسم محرك STT. |
| `language_code` | `TEXT` | NOT NULL | رمز اللغة المعترف بها. |
| `expiry_date` | `INTEGER` | NOT NULL | طابع زمني لانتهاء الصلاحية. |

#### 4. مخطط جدول `Custom_Terms`
| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `term_id` | `INTEGER` | PRIMARY KEY, AUTOINCREMENT | معرف المصطلح الفريد. |
| `user_id` | `TEXT` | NOT NULL | معرف المستخدم. |
| `source_term` | `TEXT` | NOT NULL | المصطلح المصدر. |
| `target_term` | `TEXT` | NOT NULL | المصطلح الهدف. |
| `context` | `TEXT` | NULL | السياق الذي يجب استخدام المصطلح فيه. |

### ⚙️ آلية العمل
يتم استخدام هذا المخطط لإنشاء قاعدة البيانات في بيئات التطوير والاختبار والإنتاج.

---

## 12. DATA_MANAGEMENT_POLICIES

<!-- Metadata -->
**النوع:** مرجع تطوير
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/DATA_MANAGEMENT_POLICIES.md
**التبعيات:** 06-إدارة-البيانات-اللغوية.md

### 🎯 الغرض والهدف
تحديد سياسات إدارة البيانات (Data Governance) التي تحكم كيفية جمع، تخزين، استخدام، وحذف البيانات في النظام، مع التركيز على الامتثال للخصوصية والجودة.

### 📋 المتطلبات الأساسية
ملف `06-إدارة-البيانات-اللغوية.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `06-إدارة-البيانات-اللغوية.md` - يحدد البيانات اللغوية التي يجب إدارتها.

**الملفات اللاحقة:**
- `database-model-template.ts` - يعتمد على هذه السياسات لإنشاء نماذج البيانات.
- `data-cleaner-template.ts` - يعتمد على هذه السياسات لتنظيف البيانات.

### 🏗 البنية التفصيلية
#### 1. سياسة الخصوصية والامتثال
*   **البيانات الشخصية:** لا يتم جمع أي بيانات شخصية (مثل الاسم، البريد الإلكتروني) إلا بموافقة صريحة من المستخدم (المرحلة 9).
*   **بيانات الترجمة:** لا يتم تخزين النصوص المترجمة أو المعترف بها سحابيًا إلا إذا اختار المستخدم ذلك صراحةً (لأغراض التعلم أو التخصيص).
*   **الامتثال:** يجب أن تتوافق جميع سياسات إدارة البيانات مع اللوائح الدولية (مثل GDPR، CCPA).

#### 2. سياسة جودة البيانات
*   **التحقق من الصحة:** يجب التحقق من صحة البيانات عند الإدخال (Input Validation) لضمان عدم تخزين بيانات تالفة.
*   **التوحيد القياسي:** يجب توحيد تنسيق البيانات (مثل رموز اللغة، طوابع الوقت) لضمان الاتساق.
*   **التنظيف الدوري:** يجب تطبيق إجراءات التنظيف الدوري (المرحلة 8/09) للحفاظ على جودة البيانات.

#### 3. سياسة الوصول إلى البيانات
*   **الوصول المقيد:** يجب أن يكون الوصول إلى البيانات الحساسة (مثل مفاتيح API المشفرة) مقيدًا بوحدات النظام المصرح لها فقط.
*   **التسجيل:** يجب تسجيل جميع محاولات الوصول إلى البيانات الحساسة لأغراض المراجعة الأمنية (المرحلة 9).

### ⚙️ آلية العمل
تضمن هذه السياسات أن النظام يعمل بمسؤولية وأخلاقية فيما يتعلق ببيانات المستخدم.

---

## 13. DATA_MODEL_SPECIFICATIONS

<!-- Metadata -->
**النوع:** مرجع تطوير
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/DATA_MODEL_SPECIFICATIONS.md
**التبعيات:** 02-نماذج-البيانات-والكائنات.md

### 🎯 الغرض والهدف
توفير مواصفات تقنية مفصلة لنماذج البيانات والكائنات (Data Models and Objects) المستخدمة في النظام، مع تحديد الأنواع الدقيقة والقيود.

### 📋 المتطلبات الأساسية
ملف `02-نماذج-البيانات-والكائنات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `02-نماذج-البيانات-والكائنات.md` - يوفر نظرة عامة على نماذج البيانات.

**الملفات اللاحقة:**
- `STATE_MANAGEMENT_STRATEGY.md` - يعتمد على هذه النماذج لإدارة الحالة.
- `database-model-template.ts` - يعتمد على هذه المواصفات لإنشاء قالب الكود.

### 🏗 البنية التفصيلية
#### 1. مواصفات `STTResult`
| الحقل | النوع (TypeScript) | الوصف | القيود |
| :--- | :--- | :--- | :--- |
| `text` | `string` | النص المعترف به. | لا يمكن أن يكون فارغًا. |
| `confidence` | `number` | مستوى الثقة (0.0 - 1.0). | يجب أن يكون بين 0 و 1. |
| `engine_used` | `EngineType` | اسم محرك STT المستخدم. | يجب أن يكون من قائمة المحركات المدعومة. |
| `language_code` | `LanguageCode` | رمز اللغة المعترف بها. | يجب أن يكون رمز لغة صالحًا. |
| `speaker_id` | `string | null` | معرف المتحدث. | اختياري. |
| `timestamp` | `number` | وقت بدء مقطع الصوت (بالمللي ثانية). | يجب أن يكون رقمًا موجبًا. |

#### 2. مواصفات `TranslationResult`
| الحقل | النوع (TypeScript) | الوصف | القيود |
| :--- | :--- | :--- | :--- |
| `source_text` | `string` | النص الأصلي. | لا يمكن أن يكون فارغًا. |
| `target_text` | `string` | النص المترجم. | لا يمكن أن يكون فارغًا. |
| `engine_used` | `EngineType` | اسم محرك الترجمة المستخدم. | يجب أن يكون من قائمة المحركات المدعومة. |
| `source_lang` | `LanguageCode` | رمز اللغة المصدر. | يجب أن يكون رمز لغة صالحًا. |
| `target_lang` | `LanguageCode` | رمز اللغة الهدف. | يجب أن يكون رمز لغة صالحًا. |
| `quality_score` | `number | null` | درجة جودة الترجمة. | اختياري، بين 0 و 100. |

#### 3. مواصفات `SessionState`
| الحقل | النوع (TypeScript) | الوصف | القيود |
| :--- | :--- | :--- | :--- |
| `session_id` | `string` | معرف الجلسة الفريد. | لا يمكن أن يكون فارغًا. |
| `is_active` | `boolean` | حالة الجلسة. | مطلوب. |
| `current_engine` | `EngineType` | المحرك الحالي للترجمة. | مطلوب. |
| `user_settings` | `UserSettings` | نسخة من إعدادات المستخدم. | مطلوب. |

### ⚙️ آلية العمل
تستخدم هذه المواصفات لإنشاء الأنواع الموحدة (المرحلة 3) وضمان سلامة البيانات في جميع أنحاء التطبيق.

---

## 14. STATE_MANAGEMENT_STRATEGY

<!-- Metadata -->
**النوع:** مرجع تطوير
**المرحلة:** 8
**المسار:** المرحلة-8-التخزين-وإدارة-البيانات/STATE_MANAGEMENT_STRATEGY.md
**التبعيات:** 03-إدارة-الحالة-والجلسات.md

### 🎯 الغرض والهدف
تحديد الإستراتيجية التفصيلية لإدارة الحالة (State Management) في التطبيق، مع اختيار مكتبة أو نمط تصميم محدد (مثل Redux, MobX, Signals) وتحديد كيفية تفاعله مع التخزين المحلي.

### 📋 المتطلبات الأساسية
ملف `03-إدارة-الحالة-والجلسات.md` (للتسلسل).

### 🔗 العلاقات والترابط
**الملفات السابقة:**
- `03-إدارة-الحالة-والجلسات.md` - يوفر نظرة عامة على إدارة الحالة.

**الملفات اللاحقة:**
- `BACKUP_RECOVERY_PLAN.md` - يعتمد على الحالة لتحديد ما يجب نسخه احتياطيًا.
- `state-manager-template.ts` - يعتمد على هذه الإستراتيجية لإنشاء قالب الكود.

### 🏗 البنية التفصيلية
#### 1. اختيار نمط إدارة الحالة
*   **النمط المقترح:** **Signals** (مثل Preact Signals أو SolidJS Signals) أو نمط **Flux/Redux** مبسط.
*   **السبب:** يوفر أداءً عاليًا، تحديثات دقيقة، وتدفق بيانات أحادي الاتجاه يسهل تتبعه.

#### 2. تدفق البيانات (Data Flow)
1.  **الحدث (Action):** يحدث تفاعل من المستخدم أو النظام (مثل تغيير اللغة، وصول نتيجة STT).
2.  **المغير (Mutator/Reducer):** يتم استدعاء وظيفة نقية لتحديث الحالة في المخزن المركزي.
3.  **المخزن (Store):** يتم تحديث الحالة.
4.  **المشترك (Subscriber):** يتم إخطار المكونات التي تستخدم جزءًا من الحالة المحدثة.
5.  **التخزين (Persistence):** يتم استدعاء خدمة التخزين المؤقت لحفظ الحالة المحدثة محليًا.

#### 3. التخزين الدائم (Persistence)
*   **الخدمة:** يتم استخدام خدمة `StorageService` (المرحلة 3) للتفاعل مع IndexedDB.
*   **الآلية:** يتم "ترطيب" (Hydrate) المخزن المركزي من التخزين المحلي عند بدء التشغيل، ويتم "إماهة" (Persist) التغييرات الهامة إلى التخزين المحلي بشكل دوري (Debounced).

### ⚙️ آلية العمل
تضمن هذه الإستراتيجية أن الحالة موثوقة، قابلة للتتبع، وسريعة الاستجابة لتفاعلات المستخدم.

---

**نهاية الوثيقة المدمجة**
