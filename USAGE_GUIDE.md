# دليل استخدام إضافة Video Translate AI

## 📋 نظرة عامة

هذه الإضافة توفر ترجمة فورية للفيديوهات داخل المتصفح باستخدام الذكاء الاصطناعي. تعمل الإضافة عن طريق التقاط الصوت من الفيديوهات في صفحات الويب، إرسالها إلى خادم وسيط لمعالجة STT والترجمة، ثم عرض الترجمة كترجمة فورية على الفيديو.

## 🚀 التثبيت

### المتطلبات الأساسية
- متصفح Chrome أو Chromium (الإصدار 90 أو أحدث)
- خادم وسيط (Proxy Server) يعمل على `wss://api.yourproxy.com/v1/stt-streaming`
- مفاتيح API مخزنة في الخادم الوسيط (Google STT, DeepL, OpenAI Whisper)

### خطوات التثبيت

1. **استنساخ المستودع:**
   ```bash
   git clone https://github.com/amjadakram738-arch/AK-HD-translation.git
   cd AK-HD-translation
   ```

2. **تحميل الإضافة في Chrome:**
   - افتح Chrome واذهب إلى `chrome://extensions/`
   - فعّل "وضع المطور" (Developer mode) في الزاوية العلوية اليمنى
   - اضغط على "تحميل إضافة غير محزومة" (Load unpacked)
   - اختر مجلد المشروع الذي يحتوي على `manifest.json`

3. **تكوين الخادم الوسيط:**
   - تأكد من أن خادمك الوسيط يعمل على العنوان الصحيح
   - قم بتكوين مفاتيح API في الخادم الوسيط
   - تأكد من أن الخادم يدعم WebSocket مع تنسيق الرسائل الموضح أدناه

## ⚙️ التكوين

### إعدادات الإضافة

1. **لغة الفيديو (المصدر):**
   - تلقائي (auto) لاكتشاف اللغة
   - أو اختيار لغة محددة (مثال: en, ar, fr, ...)

2. **لغة الترجمة (الهدف):**
   - قائمة واسعة من اللغات (ar, en, fr, es, de, ...)

3. **محرك الترجمة:**
   - Google Translate
   - DeepL
   - OpenAI Whisper

4. **عرض الترجمة:**
   - translated (المترجم فقط)
   - both (المترجم + الأصلي)
   - original (الأصلي فقط)

5. **تكوين الخادم الوسيط:**
   - عنوان URL: `wss://api.yourproxy.com/v1/stt-streaming`
   - بروتوكول: WebSocket
   - تنسيق الرسائل: JSON

### تنسيق رسائل WebSocket

**رسالة التكوين (من الإضافة إلى الخادم):**
```json
{
  "action": "CONFIGURE",
  "targetLang": "ar",
  "sourceLang": "auto",
  "engine": "google",
  "subtitleMode": "translated",
  "pageLang": "en",
  "hints": {
    "elementLang": "en",
    "audioTrackLang": "en",
    "captionTrackLangs": ["en"]
  }
}
```

**رسالة البيانات الصوتية (من الإضافة إلى الخادم):**
- تنسيق: Int16Array (بيانات صوتية خام)
- حجم القطعة: 4096 عينة (يمكن التكوين)
- تردد العينة: 44100 هرتز (افتراضي)

**رسالة النتيجة (من الخادم إلى الإضافة):**
```json
{
  "translatedText": "النص المترجم",
  "isFinal": true,
  "originalText": "النص الأصلي"
}
```

## 🎬 الاستخدام

### بدء الترجمة

1. افتح أي صفحة ويب تحتوي على فيديو (YouTube, Vimeo, إلخ)
2. ستظهر أيقونة عائمة (文) فوق الفيديو
3. اضغط على الأيقونة لبدء الترجمة الفورية
4. ستظهر الترجمة كترجمة فورية في أسفل الفيديو

### إيقاف الترجمة

1. اضغط على الأيقونة العائمة مرة أخرى
2. أو افتح popup الإضافة واضغط على "إيقاف الترجمة"

### تغيير الإعدادات

1. اضغط على أيقونة الإضافة في شريط الأدوات
2. اختر لغة الترجمة من القائمة المنسدلة
3. اختر محرك الترجمة من القائمة المنسدلة
4. اضغط على "حفظ الإعدادات"

## 🔧 الاختبار

### اختبار الوظائف الأساسية

1. **اكتشاف الفيديو:**
   - افتح صفحة تحتوي على فيديو
   - تحقق من ظهور الأيقونة العائمة فوق الفيديو

2. **التقاط الصوت:**
   - اضغط على الأيقونة العائمة
   - تحقق من ظهور رسالة "جاري بدء الترجمة..."
   - تحقق من ظهور رسالة في وحدة التحكم: "تم التقاط تيار الصوت بنجاح"

3. **معالجة الصوت:**
   - تحقق من ظهور رسالة: "تم إعداد معالجة الصوت بنجاح"
   - تحقق من عدم وجود أخطاء في وحدة التحكم

4. **اتصال WebSocket:**
   - تحقق من ظهور رسالة: "تم الاتصال بخادم التدفق بنجاح"
   - تحقق من إرسال رسالة التكوين

5. **عرض الترجمة:**
   - تحقق من ظهور الترجمة في أسفل الفيديو
   - تحقق من تحديث الترجمة في الوقت الفعلي

### اختبار إدارة الأخطاء

1. **فشل اتصال WebSocket:**
   - أغلق خادمك الوسيط
   - حاول بدء الترجمة
   - تحقق من ظهور رسالة خطأ: "فشل في الاتصال بخادم الترجمة"
   - تحقق من إعادة الاتصال التلقائي بعد 5 ثوان

2. **فشل AudioContext:**
   - استخدم متصفح لا يدعم AudioContext
   - تحقق من المحاولة التلقائية باستخدام AudioWorklet

3. **فشل tabCapture:**
   - رفض إذن التقاط الصوت
   - تحقق من ظهور رسالة خطأ مناسبة

### اختبار الأداء

1. **زمن الاستجابة:**
   - قياس الوقت من بدء الترجمة حتى ظهور أول ترجمة
   - يجب أن يكون أقل من 2 ثانية في الظروف المثالية

2. **استخدام الذاكرة:**
   - تحقق من عدم وجود تسرب في الذاكرة
   - تحقق من تنظيف الموارد عند إيقاف الترجمة

3. **استقرار الاتصال:**
   - تحقق من عدم انقطاع اتصال WebSocket
   - تحقق من إعادة الاتصال التلقائي في حالة الانقطاع

## 🛠 استكشاف الأخطاء وإصلاحها

### مشاكل شائعة وحلولها

**المشكلة: لا تظهر الأيقونة العائمة**
- الحل: تأكد من أن الصفحة تحتوي على عناصر `<video>`
- الحل: تحقق من عدم وجود أخطاء في وحدة التحكم
- الحل: حاول تحديث الصفحة

**المشكلة: لا يتم التقاط الصوت**
- الحل: تأكد من منح الإضافة إذن `tabCapture`
- الحل: تحقق من أن الفيديو يحتوي على صوت
- الحل: حاول استخدام متصفح مختلف

**المشكلة: لا يتم الاتصال بالخادم الوسيط**
- الحل: تأكد من أن الخادم يعمل
- الحل: تحقق من عنوان URL الصحيح في `background.js`
- الحل: تحقق من عدم وجود جدران نارية تمنع الاتصال

**المشكلة: الترجمة غير دقيقة**
- الحل: حاول تغيير محرك الترجمة
- الحل: تحقق من إعدادات اللغة
- الحل: تأكد من جودة الصوت المدخل

## 📊 تسجيل الأخطاء

### تسجيلات وحدة التحكم

- **معلومات:** `console.log` - معلومات عامة عن عملية الترجمة
- **تحذيرات:** `console.warn` - تحذيرات حول مشاكل غير حرجة
- **أخطاء:** `console.error` - أخطاء حرجة تتطلب التدخل

### تسجيلات الخادم الوسيط

- يجب أن يسجل الخادم جميع الطلبات الواردة
- يجب أن يسجل الخادم زمن استجابة معالجة STT والترجمة
- يجب أن يسجل الخادم أي أخطاء في معالجة البيانات

## 🔐 الأمان

### حدود الأمان

1. **مفاتيح API:**
   - لا يتم تخزين أي مفاتيح API في الإضافة
   - جميع المفاتيح مخزنة في الخادم الوسيط
   - الإضافة تستخدم فقط عنوان URL للخادم الوسيط

2. **الاتصال:**
   - يتم استخدام WebSocket مشفر (WSS)
   - يتم التحقق من صحة جميع البيانات الواردة
   - يتم تنظيف جميع الموارد عند إغلاق الاتصال

3. **الأذونات:**
   - `tabs` - للوصول إلى تبويبات المتصفح
   - `activeTab` - للوصول إلى التبويب النشط فقط
   - `storage` - لتخزين إعدادات المستخدم
   - `tabCapture` - لالتقاط الصوت من التبويبات
   - `scripting` - لحقن scripts في الصفحات

## 📋 المتطلبات غير الوظيفية

1. **الأداء:**
   - زمن استجابة أقل من 2 ثانية
   - استخدام الذاكرة أقل من 100 ميجابايت
   - دعم ما يصل إلى 5 تبويبات في نفس الوقت

2. **التوافق:**
   - Chrome 90+
   - Chromium 90+
   - Edge 90+
   - Opera 76+

3. **الاستقرار:**
   - إعادة الاتصال التلقائي في حالة الفشل
   - بدائل ل APIs غير المدعومة
   - إدارة الأخطاء الشاملة

## 🎯 أفضل الممارسات

1. **الأداء:**
   - استخدام AudioWorklet بدلاً من ScriptProcessorNode
   - تقليل حجم قطع الصوت إلى 2048 عينة
   - استخدام ضغط البيانات إذا لزم الأمر

2. **التوافق:**
   - التحقق من دعم WebAssembly لمحركات STT المحلية
   - استخدام بدائل ل APIs غير المدعومة
   - اختبار على متصفحات مختلفة

3. **الأمان:**
   - عدم تخزين أي بيانات حساسة في الإضافة
   - استخدام HTTPS/WSS فقط
   - التحقق من صحة جميع المدخلات

## 📚 التوثيق الإضافي

- `README.md` - نظرة عامة على المشروع
- `المرحلة-2-التصميم-المعماري.md` - التصميم المعماري الكامل
- `SecurityBoundary.md` - حدود الأمان ومعمارية الوكيل
- `STTEngineContract.md` - عقد محرك تحويل الكلام إلى نص
- `TranslationEngineContract.md` - عقد محرك الترجمة

## 🤝 الدعم

إذا واجهت أي مشاكل أو كان لديك أي أسئلة، يرجى مراجعة:
- ملفات التوثيق في المستودع
- ملف `جدول التصحيحات النصية والمخاطر المتبقية.md`
- ملف `تقرير التنقيح المعماري النهائي.md`

## 📄 الترخيص

تحقق من ملف LICENSE في المستودع للحصول على معلومات الترخيص.