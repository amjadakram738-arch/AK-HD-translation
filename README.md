# Video Translate AI - Chrome Extension

أداة ترجمة فورية للفيديوهات داخل المتصفح باستخدام الذكاء الاصطناعي.

## 📦 المكونات الأساسية

هذا المستودع يحتوي على:

### 1. إضافة Chrome (Manifest V3)
- **manifest.json** - ملف تكوين الإضافة
- **background.js** - Service Worker لإدارة التقاط الصوت والاتصال بالوكيل
- **contentScript.js** - سكريبت لاكتشاف الفيديوهات وعرض الترجمات
- **popup.js** - إدارة واجهة الإعدادات
- **popup.html** - واجهة الإعدادات
- **styles.css** - تصميم الواجهة
- **icons/** - أيقونات الإضافة

### 2. ملفات التوثيق والمواصفات
- مستندات معمارية شاملة (المرحلة 1-14)
- قوالب TypeScript/TSX للمكونات المختلفة
- تكوينات Docker Compose
- ملفات اختبار

## 🚀 تحميل الإضافة في Chrome

### الخطوة 1: تحضير الملفات
```bash
# استنساخ المستودع (إذا لم يكن موجوداً)
git clone https://github.com/amjadakram738-arch/AK-HD-translation.git
cd AK-HD-translation
```

### الخطوة 2: تحميل الإضافة
1. افتح Chrome واذهب إلى `chrome://extensions/`
2. فعّل "وضع المطور" (Developer mode) في الزاوية العلوية اليمنى
3. اضغط على "تحميل إضافة غير محزومة" (Load unpacked)
4. اختر مجلد المشروع الذي يحتوي على `manifest.json`

### الخطوة 3: الاستخدام
1. افتح أي صفحة ويب تحتوي على فيديو (بما في ذلك الصفحات الديناميكية/اللا نهائية والمشغلات داخل iframes)
2. ستظهر أيقونة عائمة (文) فوق كل فيديو يتم اكتشافه
3. اضغط على الأيقونة لبدء/إيقاف الترجمة الفورية
4. استخدم popup الإضافة لتغيير إعدادات اللغة/المحرك وشكل عرض الترجمة

## ⚙️ الإعدادات

يمكنك تخصيص:
- **لغة الفيديو (المصدر)**: تلقائي (Auto-detect) أو اختيار لغة محددة
- **لغة الترجمة (الهدف)**: قائمة واسعة من اللغات
- **محرك الترجمة**: Google Translate, DeepL, OpenAI Whisper
- **عرض الترجمة**: مترجم فقط / مترجم + أصلي / أصلي فقط
- **حجم ومكان الترجمة**: صغير/متوسط/كبير + أعلى/وسط/أسفل

> ملاحظة تقنية: تم اعتماد Offscreen Document لمعالجة الصوت لأن Service Worker في Manifest V3 لا يدعم Web Audio بشكل مباشر.

## 🔧 التطوير

### متطلبات النظام
- Node.js v20+ (مثبت مسبقاً)
- Python 3 (مثبت مسبقاً)
- Chrome/Chromium Browser

### اختبار الكود
```bash
# فحص بناء الجملة (Syntax Check)
node --check background.js
node --check contentScript.js
node --check popup.js

# التحقق من صحة manifest.json
jq empty manifest.json

# التحقق من HTML
python3 -c "from html.parser import HTMLParser; HTMLParser().feed(open('popup.html').read())"
```

### تشغيل خادم محلي (للتطوير)
```bash
python3 -m http.server 3000
```

## 📋 البنية المعمارية

الإضافة تستخدم معمارية موزعة:
- **Extension (Client)**: التقاط الصوت والتفاعل مع الواجهة
- **Proxy Server**: معالجة STT والترجمة (يجب تكوينه بشكل منفصل)
- **Cloud APIs**: محركات STT والترجمة السحابية

## 🔐 الأمان

- جميع مفاتيح API يجب أن تُخزّن في الوكيل (Proxy)
- الإضافة لا تحتوي على أي مفاتيح سرية
- الاتصال يتم عبر WebSocket مشفر (WSS)

## 📚 التوثيق الإضافي

راجع ملفات المراحل (المرحلة-1 إلى المرحلة-14) للحصول على:
- التصميم المعماري الكامل
- دليل التكامل
- معايير الجودة والاختبار
- إرشادات النشر

## 🤝 المساهمة

هذا المشروع يتبع:
- Gitflow branching strategy
- Semantic versioning (SemVer)
- Code review guidelines

## 📄 الترخيص

تحقق من ملف LICENSE في المستودع.

---

**ملاحظة**: هذه الإضافة تتطلب خادم وكيل (proxy) لمعالجة STT والترجمة. راجع التوثيق الكامل لإعداد البنية التحتية الكاملة.
