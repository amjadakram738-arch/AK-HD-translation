# 1. API DESIGN PRINCIPLES

# مبادئ تصميم الواجهات البرمجية (API Design Principles)

## 🎯 الغرض والهدف
تحديد المبادئ التوجيهية لتصميم واجهات برمجة التطبيقات (APIs) الداخلية والخارجية لضمان سهولة الاستخدام، الاتساق، والأداء.

## 🔗 مبادئ تصميم RESTful API
1.  **الموارد (Resources):** يجب أن تكون الواجهات البرمجية موجهة نحو الموارد (مثل `/users`, `/videos`, `/jobs`).
2.  **أفعال HTTP:** استخدام أفعال HTTP القياسية (GET, POST, PUT, DELETE) للتعبير عن العمليات على الموارد.
3.  **الأسماء (Nouns):** استخدام الأسماء (الجمع) في مسارات الموارد (مثل `/videos` وليس `/getVideos`).
4.  **رموز الحالة (Status Codes):** استخدام رموز حالة HTTP القياسية (مثل 200 OK, 201 Created, 400 Bad Request, 500 Internal Server Error).

## ⚙️ معايير الاتصال
| المعيار | الوصف | التطبيق |
| :--- | :--- | :--- |
| **التوثيق** | يجب توثيق جميع الواجهات البرمجية باستخدام OpenAPI (Swagger). | إلزامية لجميع الخدمات. |
| **الترقيم** | استخدام ترقيم الواجهات البرمجية (مثل `/v1/videos`). | ضروري لضمان التوافق مع الإصدارات السابقة. |
| **المصادقة** | استخدام OAuth 2.0 أو مفاتيح API (للاتصال الخارجي) و JWT (للاتصال الداخلي). | إلزامية لجميع الواجهات. |
| **التصفية والفرز** | دعم آليات موحدة للتصفية والفرز والتقسيم (Pagination). | استخدام معلمات الاستعلام (Query Parameters) موحدة. |

## 🛡 الأمان
*   **HTTPS/TLS:** يجب أن يتم الاتصال بجميع الواجهات البرمجية عبر HTTPS/TLS.
*   **التحقق من الإدخال:** يجب التحقق من صحة جميع بيانات الإدخال (Input Validation) لتجنب الثغرات الأمنية.
*   **تحديد المعدل (Rate Limiting):** تطبيق تحديد المعدل على الواجهات الخارجية لحماية النظام من هجمات الحرمان من الخدمة (DoS).

## 🚀 تصميم gRPC API (للاتصال الداخلي)
*   **ملفات Protobuf:** يجب تعريف جميع الرسائل والخدمات في ملفات `.proto` موحدة.
*   **الخدمات:** استخدام أنواع الاتصال الأربعة (Unary, Server Streaming, Client Streaming, Bidirectional Streaming) حسب الحاجة.
*   **الأداء:** تفضيل gRPC على REST للاتصال الداخلي بسبب كفاءته العالية.


---

# 2. ARCHITECTURE STANDARDS

# معايير هندسة النظام (Architecture Standards)

## 🎯 الغرض والهدف
تحديد المعايير والمبادئ الأساسية لتصميم وهندسة نظام الترجمة الفورية المتقدم (STT) لضمان القابلية للتوسع، المرونة، وسهولة الصيانة.

## 🏗 المبادئ الأساسية
1.  **هندسة الخدمات المصغرة (Microservices Architecture):** يجب تقسيم النظام إلى خدمات صغيرة ومستقلة، تتواصل عبر واجهات برمجية خفيفة الوزن (مثل gRPC أو REST).
2.  **التصميم الموجه بالمجال (Domain-Driven Design - DDD):** يجب أن تعكس كل خدمة مجالًا محددًا من أعمال النظام (مثل خدمة STT، خدمة الترجمة، خدمة تراكب الفيديو).
3.  **عدم الحالة (Statelessness):** يجب أن تكون الخدمات عديمة الحالة قدر الإمكان، مع تخزين الحالة في قواعد بيانات خارجية أو مخازن بيانات مؤقتة (مثل Redis).
4.  **التصميم المرن (Resilience):** يجب تطبيق أنماط المرونة مثل Circuit Breaker، Retry، و Bulkhead لضمان استمرار الخدمة في حالة فشل جزء منها.

## 🛠 المكونات التقنية القياسية
| المكون | التقنية القياسية | الغرض |
| :--- | :--- | :--- |
| **التنسيق** | Kubernetes / Docker | إدارة الحاويات والتوزيع. |
| **الاتصال بين الخدمات** | gRPC (للاتصال الداخلي) / REST (للاتصال الخارجي) | بروتوكولات الاتصال الموحدة. |
| **قاعدة البيانات** | PostgreSQL (للقاعدة العلائقية) / MongoDB (للمستندات) | تخزين البيانات الدائم. |
| **قائمة الانتظار** | Kafka / RabbitMQ | المعالجة غير المتزامنة وإدارة المهام. |
| **التخزين السحابي** | S3-Compatible Storage | تخزين ملفات الفيديو والصوت الكبيرة. |

## 📐 معايير التصميم
*   **الاستقلالية:** يجب أن تكون كل خدمة قابلة للنشر والتطوير بشكل مستقل.
*   **التوثيق:** يجب توثيق واجهات API لكل خدمة باستخدام OpenAPI/Swagger.
*   **المراقبة:** يجب أن تتضمن كل خدمة مقاييس (Metrics)، سجلات (Logs)، وتتبع (Tracing) موحد.


---

# 3. BRANCHING STRATEGY

# استراتيجية التفريع (Branching Strategy)

## 🎯 الغرض والهدف
تحديد نموذج موحد لإدارة فروع الكود (Branches) في نظام التحكم في الإصدار (Git) لضمان بيئة تطوير مستقرة ومنظمة.

## 🌿 نموذج Gitflow المعدل
سيتم استخدام نموذج Gitflow المعدل، والذي يتضمن الفروع الرئيسية التالية:

| الفرع | الغرض | الحماية |
| :--- | :--- | :--- |
| **`main`** | يمثل الكود الجاهز للنشر في **الإنتاج**. | محمي (Protected)، لا يمكن الدفع إليه مباشرة. |
| **`develop`** | يمثل الكود المدمج من جميع الميزات، وجاهز للنشر في بيئة **الاختبار (Staging)**. | محمي (Protected)، لا يمكن الدفع إليه مباشرة. |
| **`feature/*`** | لتطوير ميزة جديدة أو تحسين. | غير محمي، يتم حذفه بعد الدمج. |
| **`hotfix/*`** | لإصلاح عاجل في الإنتاج. | غير محمي، يتم حذفه بعد الدمج. |

## 🔄 سير العمل (Workflow)
1.  **بدء الميزة:** يتم إنشاء فرع جديد من `develop` بالتنسيق `feature/اسم-الميزة-المختصر`.
2.  **التطوير:** يتم العمل على الفرع، مع الالتزام بالدفعات الصغيرة والمتكررة.
3.  **طلب الدمج (Merge Request):** عند الانتهاء، يتم إنشاء طلب دمج إلى `develop`.
    *   **المتطلبات:** يجب أن يمر بجميع اختبارات CI، ويتم مراجعته والموافقة عليه من قبل مطور آخر.
4.  **النشر إلى الإنتاج:** يتم إنشاء فرع `release/X.Y.Z` من `develop`، ويتم دمجه في `main` بعد اختبارات Staging النهائية.
5.  **الإصلاح العاجل (Hotfix):** يتم إنشاء فرع `hotfix/اسم-الإصلاح` من `main`، ويتم دمجه في `main` و `develop` في نفس الوقت.

## 🏷 تسمية الإصدارات (Tagging)
*   يجب وضع علامة (Tag) على فرع `main` عند كل عملية نشر ناجحة إلى الإنتاج.
*   **التنسيق:** SemVer (Semantic Versioning) - `vMAJOR.MINOR.PATCH` (مثال: `v1.2.3`).


---

# 4. CODE REVIEW GUIDELINES

# إرشادات مراجعة الكود (Code Review Guidelines)

## 🎯 الغرض والهدف
تحديد إرشادات واضحة لعملية مراجعة الكود لضمان جودة الكود، مشاركة المعرفة، والالتزام بالمعايير.

## 🧐 مسؤوليات المراجع (Reviewer)
1.  **التحقق من الوظائف:** التأكد من أن الكود يحقق الغرض المطلوب منه بشكل صحيح.
2.  **التحقق من الجودة:** التأكد من الالتزام باتفاقيات الكود، ومعايير الجودة، ومبادئ التصميم.
3.  **التحقق من الأمان:** البحث عن أي ثغرات أمنية محتملة أو ممارسات غير آمنة.
4.  **التحقق من الاختبارات:** التأكد من وجود اختبارات وحدة وتكامل كافية للتغييرات.
5.  **التحقق من الوثائق:** التأكد من تحديث الوثائق الداخلية والخارجية ذات الصلة.

## 📝 عملية المراجعة
*   **الحجم:** يجب أن تكون طلبات الدمج صغيرة قدر الإمكان (يفضل أقل من 400 سطر من التغيير) لتسهيل المراجعة.
*   **التعليقات:** يجب أن تكون التعليقات بناءة، مهذبة، وموجهة نحو الكود وليس المطور.
*   **الأنواع:** استخدام أنواع التعليقات الموحدة (مثل "Suggestion"، "Nitpick"، "Blocker").
*   **الموافقة:** تتطلب جميع طلبات الدمج موافقة **مطوّر واحد على الأقل** قبل الدمج.

## 🛡 التركيز على الأمان
*   **التحقق من الإدخال:** هل يتم التحقق من صحة جميع المدخلات (Input Validation)؟
*   **التعامل مع الأسرار:** هل يتم التعامل مع الأسرار (Secrets) بشكل آمن (عدم تسجيلها أو تخزينها في الكود)؟
*   **الترخيص:** هل يتم التحقق من صلاحيات المستخدم (Authorization) قبل تنفيذ العمليات الحساسة؟

## 🚀 تحسين الأداء
*   هل هناك أي استعلامات غير فعالة لقاعدة البيانات (N+1 queries)؟
*   هل يمكن تحسين الكود لتقليل زمن الاستجابة أو استهلاك الموارد؟

## ⏳ زمن المراجعة
يجب أن تتم المراجعة الأولية لطلب الدمج في غضون **24 ساعة** من تقديمه.


---

# 5. CODING CONVENTIONS

# اتفاقيات كتابة الكود (Coding Conventions)

## 🎯 الغرض والهدف
توفير مجموعة موحدة من القواعد والمبادئ لكتابة الكود عبر جميع الخدمات واللغات المستخدمة في المشروع لضمان القراءة، الاتساق، وسهولة الصيانة.

## 📝 مبادئ عامة
1.  **القراءة أولاً:** يجب أن يكون الكود سهل القراءة والفهم من قبل أي مطور آخر.
2.  **الاتساق:** يجب تطبيق نفس الاتفاقيات في جميع أنحاء قاعدة الكود.
3.  **التعليقات التوضيحية:** يجب استخدام التعليقات (Docstrings) لتوضيح الغرض من الفئات، الدوال، والواجهات العامة.

## 🏷 اتفاقيات التسمية (Naming Conventions)
| العنصر | الاتفاقية | مثال |
| :--- | :--- | :--- |
| **المتغيرات** | Camel Case (بدءًا بحرف صغير) | `userName`, `totalCount` |
| **الثوابت** | SCREAMING_SNAKE_CASE | `MAX_RETRIES`, `API_KEY` |
| **الدوال/الطرق** | Camel Case (بدءًا بحرف صغير) | `calculateTotal()`, `processRequest()` |
| **الفئات/الواجهات** | Pascal Case | `UserService`, `VideoProcessor` |
| **الملفات** | Kebab Case (للملفات غير البرمجية) / Snake Case (للملفات البرمجية) | `user-guide.md`, `video_processor.py` |

## 📐 التنسيق (Formatting)
*   **المسافة البادئة (Indentation):** استخدام 4 مسافات (Spaces) بدلاً من علامات الجدولة (Tabs).
*   **طول السطر:** يجب ألا يتجاوز طول السطر 120 حرفًا.
*   **الأقواس المعقوفة (Braces):** استخدام نمط K&R (القوس الافتتاحي في نفس السطر).

## 🛡 أدوات فرض الاتفاقيات
*   **Linters:** استخدام ESLint (لـ TypeScript/JavaScript) و Flake8 (لـ Python).
*   **Formatters:** استخدام Prettier (لـ TypeScript/JavaScript) و Black (لـ Python).
*   **التكامل المستمر (CI):** يجب أن يفشل بناء المشروع إذا لم يتم الالتزام باتفاقيات الكود.


---

# 6. DATA MODEL SPECIFICATIONS

# مواصفات نموذج البيانات (Data Model Specifications)

## 🎯 الغرض والهدف
تحديد المعايير والمواصفات لنمذجة البيانات عبر جميع قواعد البيانات والخدمات لضمان الاتساق، التكامل، وسهولة الاستعلام.

## 🔑 مبادئ نمذجة البيانات
1.  **التوحيد:** استخدام أنواع بيانات موحدة قدر الإمكان (مثل استخدام UUIDs للمعرفات الرئيسية).
2.  **التعريف الواضح:** يجب أن يكون لكل حقل (Field) اسم واضح، نوع بيانات، ووصف دقيق.
3.  **المرونة:** يجب أن تكون النماذج مرنة بما يكفي لدعم التغييرات المستقبلية دون الحاجة إلى ترحيل بيانات معقد.

## 💾 أنواع البيانات القياسية
| النوع | الوصف | الاستخدام المفضل |
| :--- | :--- | :--- |
| **المعرفات (IDs)** | معرف فريد عالمي (UUID v4) | المفاتيح الأساسية (Primary Keys) والمفاتيح الخارجية (Foreign Keys). |
| **التاريخ والوقت** | Timestamp with Timezone (ISO 8601) | تسجيل أوقات الإنشاء والتحديث. |
| **النص** | VARCHAR(255) أو TEXT | النصوص القصيرة والطويلة. |
| **العملة** | DECIMAL(10, 2) | تجنب استخدام Floating Point للعملات. |
| **الحالة** | ENUM أو Small Integer | لتمثيل حالة الكيان (مثل PENDING, PROCESSING, COMPLETED). |

## 🔗 العلاقات
*   **العلاقات العلائقية (Relational):** استخدام المفاتيح الخارجية لفرض التكامل المرجعي في قواعد البيانات العلائقية (PostgreSQL).
*   **العلاقات غير العلائقية (Non-Relational):** استخدام التضمين (Embedding) أو المراجع (References) في قواعد بيانات المستندات (MongoDB).

## 📝 أمثلة على نماذج البيانات الأساسية

### نموذج مهمة المعالجة (Processing Job)
| الحقل | النوع | الوصف |
| :--- | :--- | :--- |
| `job_id` | UUID | المعرف الفريد للمهمة. |
| `user_id` | UUID | معرف المستخدم الذي أنشأ المهمة. |
| `status` | ENUM | حالة المهمة (PENDING, PROCESSING, COMPLETED, FAILED). |
| `input_url` | VARCHAR | رابط S3 لملف الإدخال. |
| `output_url` | VARCHAR | رابط S3 لملف الإخراج. |
| `created_at` | Timestamp | تاريخ إنشاء المهمة. |


---

# 7. DEPLOYMENT PROTOCOLS

# بروتوكولات النشر (Deployment Protocols)

## 🎯 الغرض والهدف
تحديد الإجراءات والبروتوكولات الموحدة لنشر الخدمات عبر البيئات المختلفة (التطوير، الاختبار، الإنتاج) لضمان عملية نشر سريعة، موثوقة، وآمنة.

## ⚙️ بيئات النشر
| البيئة | الغرض | آلية النشر |
| :--- | :--- | :--- |
| **Development (Dev)** | اختبار الميزات الجديدة بشكل سريع. | نشر تلقائي عند كل دفع (Push) إلى فرع الميزة. |
| **Staging (Test)** | محاكاة بيئة الإنتاج، اختبارات E2E، واختبارات القبول للمستخدم. | نشر تلقائي عند الدمج في فرع `develop`. |
| **Production (Prod)** | البيئة الحية التي يخدم منها المستخدمون. | نشر يدوي/مجدول بعد الموافقة النهائية على فرع `main`. |

## 🛡 بروتوكولات الأمان للنشر
1.  **المصادقة:** يجب أن تتم جميع عمليات النشر باستخدام حسابات خدمة (Service Accounts) ذات صلاحيات محدودة.
2.  **إدارة الأسرار:** يجب سحب الأسرار (Secrets) من نظام إدارة الأسرار (Vault) في وقت النشر، وعدم تخزينها في ملفات التكوين.
3.  **التحقق من الصورة:** يجب التحقق من توقيع صور Docker قبل نشرها لضمان عدم التلاعب بها.

## 🔄 استراتيجيات النشر
*   **النشر المتدحرج (Rolling Deployment):** هي الاستراتيجية الافتراضية، حيث يتم استبدال النسخ القديمة تدريجيًا بالجديدة.
*   **النشر الأزرق/الأخضر (Blue/Green Deployment):** تستخدم للنشرات الحرجة، حيث يتم نشر الإصدار الجديد (الأخضر) بجانب القديم (الأزرق)، ثم يتم تحويل حركة المرور دفعة واحدة.
*   **النشر الكناري (Canary Deployment):** تستخدم للنشرات عالية المخاطر، حيث يتم نشر الإصدار الجديد لمجموعة صغيرة من المستخدمين أولاً.

## 🚨 بروتوكولات التراجع (Rollback)
*   **المراقبة الفورية:** يجب مراقبة المقاييس الرئيسية (Latency, Errors, CPU) لمدة 5 دقائق بعد النشر.
*   **التراجع التلقائي:** إذا تجاوزت المقاييس الحدود المحددة، يجب أن يتم التراجع تلقائيًا إلى الإصدار السابق المستقر.
*   **التراجع اليدوي:** يجب أن تكون هناك آلية بسيطة وموثقة للتراجع اليدوي في حالة اكتشاف مشكلة غير مكتشفة آليًا.


---

# 8. DEVELOPMENT WORKFLOW

# سير عمل التطوير (Development Workflow)

## 🎯 الغرض والهدف
تحديد الخطوات الموحدة التي يتبعها المطورون من بداية المهمة حتى نشرها في الإنتاج.

## 🔄 خطوات سير العمل
| الخطوة | الوصف | الأدوات/الموارد |
| :--- | :--- | :--- |
| **1. التخطيط** | تحديد المهمة (Task) من نظام إدارة المشاريع (مثل Jira/Trello) وتعيينها للمطور. | نظام إدارة المشاريع |
| **2. التهيئة** | سحب أحدث نسخة من فرع `develop` وإنشاء فرع ميزة جديد (`feature/task-id`). | Git, IDE |
| **3. التطوير** | كتابة الكود، مع الالتزام باتفاقيات التسمية والجودة، وكتابة اختبارات الوحدة. | IDE, Linters, Formatters |
| **4. الاختبار المحلي** | تشغيل اختبارات الوحدة والتكامل محليًا للتأكد من عمل الميزة. | Docker Compose, Testing Frameworks |
| **5. طلب الدمج (MR)** | إنشاء طلب دمج إلى فرع `develop`، مع وصف واضح للتغييرات. | Git, منصة Git (GitHub/GitLab) |
| **6. مراجعة الكود** | مراجعة الكود من قبل مطور آخر، مع معالجة أي ملاحظات. | منصة Git |
| **7. التكامل المستمر (CI)** | تشغيل خط أنابيب CI (اختبارات، فحص أمني، بناء). | CI/CD Pipeline |
| **8. النشر إلى Staging** | النشر التلقائي إلى بيئة الاختبار (Staging) لإجراء اختبارات E2E. | CD Pipeline |
| **9. النشر إلى الإنتاج** | بعد الموافقة النهائية، يتم دمج `develop` في `main` والنشر إلى الإنتاج. | CD Pipeline |

## 🛠 أدوات التطوير الأساسية
*   **IDE:** Visual Studio Code (مع الإضافات الموصى بها).
*   **التحكم في الإصدار:** Git.
*   **الحاويات:** Docker.
*   **إدارة الحزم:** PNPM (لـ Node.js) أو Poetry (لـ Python).

## 📝 التزام رسائل الالتزام (Commit Messages)
يجب أن تتبع رسائل الالتزام نمط Conventional Commits لضمان سجل تغييرات واضح.
*   **التنسيق:** `<type>(<scope>): <subject>`
*   **الأنواع الشائعة:** `feat` (ميزة جديدة)، `fix` (إصلاح خطأ)، `docs` (تغييرات في الوثائق)، `style` (تنسيق)، `refactor` (إعادة هيكلة)، `test` (إضافة اختبارات).


---

# 9. FOLDER ORGANIZATION

# تنظيم المجلدات (Folder Organization)

## 🎯 الغرض والهدف
تحديد المبادئ التفصيلية لتنظيم المحتوى داخل المجلدات لضمان سهولة العثور على الملفات ذات الصلة.

## 📂 مبادئ التنظيم
1.  **التجميع حسب الميزة (Feature Grouping):** يجب تجميع الملفات ذات الصلة بميزة معينة في مجلد واحد.
2.  **التنظيم حسب النوع (Type Grouping):** داخل مجلد الميزة، يمكن تجميع الملفات حسب نوعها (مثل `models`, `services`, `controllers`).
3.  **العمق المحدود:** يجب ألا يتجاوز عمق المجلدات 4 مستويات لتجنب التعقيد.

## 📁 أمثلة على تنظيم المجلدات

### 1. تنظيم الميزات (Feature-based Organization)
```
/src
├── /jobs
│   ├── job.model.ts
│   ├── job.service.ts
│   └── job.controller.ts
├── /users
│   ├── user.model.ts
│   ├── user.service.ts
│   └── user.controller.ts
└── /shared
    ├── constants.ts
    └── utils.ts
```

### 2. تنظيم البنية التحتية (Infra Organization)
```
/infra
├── /environments
│   ├── dev
│   └── prod
├── /modules
│   ├── s3-bucket
│   └── kubernetes-cluster
└── main.tf
```

### 3. تنظيم الوثائق (Docs Organization)
```
/docs
├── /architecture
├── /guides
├── /api
└── README.md
```

## 📝 ملاحظات إضافية
*   **ملفات الاختبار:** يجب وضع ملفات الاختبار بجوار الكود الذي تختبره، باستخدام لاحقة موحدة (مثل `.test.ts` أو `_test.py`).
*   **ملفات التكوين:** يجب وضع ملفات التكوين الخاصة بالخدمة في جذر مجلد الخدمة، وملفات التكوين العامة في جذر المشروع.


---

# 10. MERGE REQUEST PROCESS

# عملية طلب الدمج (Merge Request Process)

## 🎯 الغرض والهدف
تحديد الخطوات الإجرائية لإنشاء، مراجعة، ودمج طلبات الدمج (Merge Requests - MRs) لضمان جودة واستقرار فرع `develop`.

## 📝 إنشاء طلب الدمج (MR Creation)
1.  **العنوان:** يجب أن يتبع العنوان نمط Conventional Commits (مثال: `feat(stt): Add support for new language model`).
2.  **الوصف:** يجب أن يتضمن الوصف ملخصًا للتغييرات، سبب التغيير، وكيفية اختباره.
3.  **الربط بالمهمة:** يجب ربط طلب الدمج بالمهمة ذات الصلة في نظام إدارة المشاريع.
4.  **المستهدف:** يجب أن يكون الفرع المستهدف هو `develop` (باستثناء Hotfixes التي تستهدف `main`).

## 🔄 مراحل طلب الدمج
| المرحلة | المسؤول | الإجراءات الرئيسية |
| :--- | :--- | :--- |
| **1. قيد المراجعة** | المطور | تعيين مراجع واحد على الأقل. |
| **2. قيد التعديل** | المطور | معالجة تعليقات المراجعين ودفع التغييرات. |
| **3. جاهز للدمج** | المراجعون | الموافقة على طلب الدمج بعد التأكد من معالجة جميع التعليقات ونجاح اختبارات CI. |
| **4. الدمج** | المطور / النظام الآلي | دمج الفرع في `develop` (يفضل استخدام Squash and Merge). |

## 🛡 متطلبات الدمج الإلزامية
*   **نجاح CI:** يجب أن تكون جميع اختبارات خط أنابيب التكامل المستمر (CI) ناجحة.
*   **موافقة المراجع:** يجب الحصول على موافقة واحدة على الأقل من مراجع معتمد.
*   **حل التعارضات:** يجب حل جميع تعارضات الدمج قبل الموافقة.
*   **تغطية الاختبار:** يجب أن لا تقل تغطية الكود عن الحد الأدنى المحدد (80%).

## 🗑 تنظيف الفروع
يجب حذف فرع الميزة تلقائيًا بعد دمجه بنجاح في `develop`.


---

# 11. NAMING CONVENTIONS

# اتفاقيات التسمية (Naming Conventions)

## 🎯 الغرض والهدف
تحديد قواعد تسمية موحدة للملفات، المجلدات، الموارد السحابية، وقواعد البيانات لضمان الاتساق والوضوح عبر المشروع.

## 📁 تسمية الملفات والمجلدات
| العنصر | الاتفاقية | مثال |
| :--- | :--- | :--- |
| **المجلدات** | Kebab Case (أحرف صغيرة، فواصل شرطية) | `video-processor`, `api-gateway` |
| **ملفات الكود** | Snake Case (لـ Python) / Kebab Case (لـ JS/TS) | `video_utils.py`, `user-service.ts` |
| **ملفات التكوين** | Kebab Case | `config-prod.yml`, `docker-compose.yml` |
| **ملفات الوثائق** | Kebab Case | `architecture-standards.md`, `user-guide.md` |

## 💻 تسمية الكود (مكرر من CODING_CONVENTIONS.md للتأكيد)
| العنصر | الاتفاقية | مثال |
| :--- | :--- | :--- |
| **الفئات/الواجهات** | Pascal Case | `UserService`, `JobStatus` |
| **الدوال/الطرق** | Camel Case | `processVideo`, `getUserById` |
| **المتغيرات** | Camel Case | `videoUrl`, `jobId` |
| **الثوابت** | SCREAMING_SNAKE_CASE | `MAX_FILE_SIZE` |

## ☁️ تسمية موارد البنية التحتية (Cloud Resources)
*   **البادئة:** يجب أن تبدأ جميع الموارد ببادئة المشروع (مثل `stt-`).
*   **النمط:** `[project]-[service]-[environment]-[resource-type]`
*   **أمثلة:**
    *   `stt-db-prod-postgres` (قاعدة بيانات الإنتاج)
    *   `stt-api-dev-gateway` (بوابة API للتطوير)
    *   `stt-s3-raw-videos` (مخزن S3 للفيديوهات الخام)

## 💾 تسمية قواعد البيانات والجداول
*   **قواعد البيانات:** Snake Case (أحرف صغيرة) | `stt_main_db`
*   **الجداول:** Snake Case (الجمع) | `users`, `processing_jobs`
*   **الأعمدة:** Snake Case (المفرد) | `user_id`, `file_name`


---

# 12. PERFORMANCE REQUIREMENTS

# متطلبات الأداء (Performance Requirements)

## 🎯 الغرض والهدف
تحديد متطلبات الأداء الكمية التي يجب أن يحققها النظام لضمان تجربة مستخدم مرضية وكفاءة تشغيلية.

## ⏱ متطلبات زمن الاستجابة (Latency)
| الخدمة | المقياس | الهدف (P95) |
| :--- | :--- | :--- |
| **STT (ملف)** | زمن معالجة ملف صوتي مدته دقيقة واحدة. | < 5 ثوانٍ |
| **الترجمة (ملف)** | زمن ترجمة نص ناتج عن دقيقة صوتية. | < 1 ثانية |
| **تراكب الفيديو** | زمن تراكب الترجمة على فيديو مدته 10 دقائق. | < 5 دقائق |
| **API الأساسية** | زمن استجابة طلبات GET/POST. | < 200 مللي ثانية |

## 🚀 متطلبات الإنتاجية (Throughput)
*   **الحد الأدنى:** يجب أن يدعم النظام ما لا يقل عن **100 طلب STT متزامن** في الثانية.
*   **الحد الأقصى:** يجب أن يكون النظام قادرًا على التوسع لدعم **500 طلب STT متزامن** في الثانية.

## ⚖ متطلبات التحجيم (Scalability)
1.  **التحجيم الأفقي:** يجب أن تكون جميع الخدمات قابلة للتحجيم أفقيًا (إضافة المزيد من النسخ/Pods) دون الحاجة إلى تغيير في الكود.
2.  **التحجيم التلقائي:** يجب إعداد التحجيم التلقائي (Autoscaling) بناءً على مقاييس الاستخدام (CPU/Queue Length).

## 💾 متطلبات استهلاك الموارد
*   **الذاكرة (Memory):** يجب أن لا يتجاوز استهلاك الذاكرة لكل نسخة خدمة (Pod) **2 جيجابايت** في الظروف العادية.
*   **وحدة المعالجة المركزية (CPU):** يجب أن لا يتجاوز متوسط استخدام وحدة المعالجة المركزية لكل نسخة خدمة **70%** من الموارد المخصصة.

## 🛠 اختبارات الأداء
*   **اختبار الحمل (Load Testing):** محاكاة الاستخدام المتوقع (100 طلب/ثانية) لمدة ساعة واحدة.
*   **اختبار الإجهاد (Stress Testing):** زيادة الحمل تدريجيًا حتى نقطة الفشل لتحديد الحد الأقصى للإنتاجية.
*   **اختبار التحمل (Soak Testing):** تشغيل النظام تحت حمل متوسط لمدة 24 ساعة للتحقق من تسرب الذاكرة.


---

# 13. PROJECT STRUCTURE

# هيكل المشروع (Project Structure)

## 🎯 الغرض والهدف
تحديد هيكل المجلدات والملفات الموحد لجميع الخدمات والمستودعات (Repositories) لضمان سهولة التنقل، الاتساق، وسرعة انضمام المطورين الجدد.

## 📁 الهيكل العام للمستودع (Monorepo)
يفضل استخدام هيكل المستودع الأحادي (Monorepo) لتجميع جميع الخدمات والمكتبات المشتركة في مكان واحد.

```
/project-root
├── /apps                 # الخدمات القابلة للنشر (Microservices)
│   ├── /stt-service      # خدمة تحويل الكلام إلى نص
│   ├── /translation-service # خدمة الترجمة
│   ├── /video-overlay-service # خدمة تراكب الفيديو
│   └── /api-gateway      # بوابة API
├── /libs                 # المكتبات المشتركة (Shared Libraries)
│   ├── /common-types     # أنواع البيانات والواجهات المشتركة
│   ├── /db-access        # طبقة الوصول إلى قاعدة البيانات المشتركة
│   └── /logging          # مكتبة التسجيل الموحدة
├── /docs                 # الوثائق العامة للمشروع
├── /infra                # ملفات البنية التحتية ككود (Terraform, Kubernetes)
├── /scripts              # سكربتات التشغيل والصيانة
├── .gitignore
└── package.json
```

## 📂 هيكل الخدمة المصغرة (Microservice Structure)
يجب أن تتبع كل خدمة مصغرة (تحت `/apps`) الهيكل التالي:

```
/service-name
├── /src                  # الكود المصدري
│   ├── /api              # معالجات API (Controllers)
│   ├── /core             # منطق العمل الأساسي (Business Logic)
│   ├── /data             # طبقة الوصول إلى البيانات (Repositories)
│   ├── /models           # نماذج البيانات (Types/Interfaces)
│   └── index.ts          # نقطة الدخول للخدمة
├── /tests                # ملفات الاختبار
├── Dockerfile            # ملف بناء الحاوية
├── package.json          # تبعيات الخدمة
└── README.md             # وصف الخدمة
```


---

# 14. QUALITY STANDARDS

# معايير الجودة (Quality Standards)

## 🎯 الغرض والهدف
تحديد المعايير الكمية والنوعية التي يجب أن تلبيها جميع مخرجات المشروع (الكود، الوثائق، المنتج النهائي) لضمان جودة عالية.

## 💻 جودة الكود
1.  **التغطية بالاختبار (Test Coverage):** يجب أن لا تقل تغطية الكود بالاختبارات الآلية عن **80%** للخدمات الأساسية.
2.  **تعقيد الكود (Code Complexity):** يجب أن تكون قيمة التعقيد السيكلوماتي (Cyclomatic Complexity) لكل دالة أقل من **10**.
3.  **التحليل الثابت (Static Analysis):** يجب أن يمر الكود بجميع فحوصات Linter و Static Analyzer دون أخطاء أو تحذيرات حرجة.
4.  **التوثيق الداخلي:** يجب أن تكون جميع الدوال والفئات العامة موثقة بشكل جيد باستخدام Docstrings.

## 🚀 جودة الأداء
*   **زمن الاستجابة (Latency):** يجب أن يكون زمن استجابة واجهات API الأساسية (P95) أقل من **200 مللي ثانية**.
*   **الإنتاجية (Throughput):** يجب أن يكون النظام قادرًا على معالجة ما لا يقل عن **100 مهمة STT/ترجمة** في الدقيقة.
*   **استخدام الموارد:** يجب أن يظل استخدام وحدة المعالجة المركزية (CPU) والذاكرة (Memory) ضمن الحدود المحددة (مثل أقل من 70% من الموارد المخصصة).

## 📄 جودة الوثائق
1.  **الوضوح والدقة:** يجب أن تكون جميع الوثائق واضحة، دقيقة، ومحدثة باستمرار.
2.  **الاتساق:** يجب أن تتبع جميع الوثائق تنسيق Markdown الموحد واتفاقيات التسمية المحددة.
3.  **التكامل:** يجب أن يتم تخزين الوثائق بجوار الكود (Docs as Code) وتحديثها مع تغييرات الكود.

## 🛠 معايير الصيانة
*   **الاعتمادية (Reliability):** يجب أن يكون معدل وقت التشغيل (Uptime) للنظام **99.9%** (أقل من 8 ساعات تعطل سنويًا).
*   **وقت الاستعادة (MTTR):** يجب أن يكون متوسط الوقت اللازم للاستعادة بعد الفشل أقل من **30 دقيقة**.


---

# 15. SECURITY POLICIES

# سياسات الأمان (Security Policies)

## 🎯 الغرض والهدف
تحديد السياسات والإجراءات الإلزامية لحماية النظام، البيانات، والبنية التحتية من التهديدات الداخلية والخارجية.

## 🛡 سياسات حماية البيانات
1.  **التشفير أثناء النقل (Encryption in Transit):** يجب أن يتم تشفير جميع الاتصالات بين المستخدمين والنظام، وبين الخدمات الداخلية، باستخدام TLS 1.2 أو أعلى.
2.  **التشفير أثناء التخزين (Encryption at Rest):** يجب تشفير جميع البيانات الحساسة المخزنة في قواعد البيانات، ومخازن الملفات (S3)، والتخزين المؤقت.
3.  **الحد الأدنى من الامتيازات (Least Privilege):** يجب منح المستخدمين والخدمات الحد الأدنى من الصلاحيات اللازمة لأداء وظائفهم.

## 🔑 سياسات إدارة الوصول والمصادقة
*   **المصادقة القوية:** يجب استخدام المصادقة متعددة العوامل (MFA) للوصول الإداري إلى البنية التحتية.
*   **كلمات المرور:** يجب أن تكون كلمات المرور قوية، ويتم تخزينها باستخدام خوارزميات تجزئة أحادية الاتجاه (مثل Argon2 أو bcrypt).
*   **إدارة الأسرار:** يجب تخزين جميع الأسرار (مفاتيح API، كلمات المرور) في نظام إدارة أسرار مركزي (مثل HashiCorp Vault أو AWS Secrets Manager).

## 🚨 سياسات الاستجابة للحوادث
1.  **التنبيه الفوري:** يجب إعداد تنبيهات فورية لأي نشاط مشبوه أو خرق أمني محتمل.
2.  **خطة الاستجابة:** يجب أن تكون هناك خطة واضحة وموثقة للاستجابة للحوادث الأمنية، تتضمن خطوات الاحتواء، التحقيق، والاستعادة.

## 🔄 سياسات دورة حياة التطوير الآمن (SDLC)
*   **مراجعة الكود:** يجب أن تتضمن مراجعة الكود فحصًا أمنيًا إلزاميًا.
*   **فحص الثغرات:** يجب إجراء فحص تلقائي للثغرات الأمنية (SAST/DAST) كجزء من خط أنابيب CI/CD.
*   **تحديث المكتبات:** يجب تحديث جميع المكتبات والتبعيات بانتظام لمعالجة الثغرات المعروفة.


---

# 16. TESTING STRATEGY

# استراتيجية الاختبار (Testing Strategy)

## 🎯 الغرض والهدف
تحديد النهج الشامل للاختبار عبر دورة حياة تطوير النظام لضمان جودة المنتج النهائي.

## 🧪 هرم الاختبار (Testing Pyramid)
يجب أن يتبع الاختبار نموذج هرم الاختبار، حيث تكون اختبارات الوحدة هي الأغلبية، تليها اختبارات التكامل، وأخيرًا اختبارات النهاية إلى النهاية (E2E).

| نوع الاختبار | الوصف | الأدوات المقترحة |
| :--- | :--- | :--- |
| **اختبارات الوحدة (Unit Tests)** | اختبار أصغر جزء من الكود (دالة أو فئة) بشكل معزول. | Jest, Mocha (لـ JS/TS) / Pytest (لـ Python) |
| **اختبارات التكامل (Integration Tests)** | اختبار تفاعل المكونات مع بعضها البعض (مثل الخدمة مع قاعدة البيانات). | Supertest, Testcontainers |
| **اختبارات النهاية إلى النهاية (E2E Tests)** | محاكاة سلوك المستخدم النهائي عبر واجهة المستخدم أو API. | Cypress, Playwright |
| **اختبارات الأداء (Performance Tests)** | قياس سرعة واستقرار النظام تحت الحمل. | JMeter, Locust |

## 🔄 دورة حياة الاختبار
1.  **التطوير:** كتابة اختبارات الوحدة قبل كتابة الكود (TDD).
2.  **الدمج المستمر (CI):** تشغيل جميع اختبارات الوحدة والتكامل تلقائيًا عند كل طلب دمج (Merge Request).
3.  **التسليم المستمر (CD):** تشغيل اختبارات E2E واختبارات الأداء في بيئة الاختبار (Staging) قبل النشر إلى الإنتاج.

## ⚙️ اختبارات الجودة الخاصة بالمنتج
*   **اختبار دقة STT:** استخدام مقاييس WER (Word Error Rate) و CER (Character Error Rate).
*   **اختبار جودة الترجمة:** استخدام مقاييس BLEU و METEOR.
*   **اختبار التزامن:** التحقق من دقة التوقيت بين الترجمة والفيديو (أقل من 100 مللي ثانية).


---

