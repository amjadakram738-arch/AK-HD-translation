
--- START OF FILE: 01-تنفيذ-واجهة-محرك-STT.md ---

# 01-تنفيذ-واجهة-محرك-STT

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/01-تنفيذ-واجهة-محرك-STT.md
**التبعيات:** المرحلة-4-نظام-الترجمة-متعدد-المحركات/SUCCESS_METRICS_PHASE4.md

## 🎯 الغرض والهدف
تنفيذ الواجهة الأساسية لجميع محركات التعرف على الكلام (STT) في النظام، لضمان التوافق والتبديل السلس بين المحركات المختلفة (سحابية ومحلية).

## 📋 المتطلبات الأساسية
اكتمال المرحلة 4.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/09-تنفيذ-وحدة-التحكم-في-STT.md` - يستخدم هذه الواجهة.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/05-تنفيذ-محول-محلي-للتخزين-المؤقت-لـ-STT.md` - يطبق هذه الواجهة.

### الملفات اللاحقة  
- `02-تنفيذ-محول-Google-STT.md` - يطبق هذه الواجهة.
- `03-تنفيذ-محول-Azure-STT.md` - يطبق هذه الواجهة.

## 🏗 البنية التفصيلية
### 1. تعريف الواجهة `ISTTEngine`
**الوصف:** تعريف الواجهة `ISTTEngine` في `src/types/common.ts` التي يجب أن يطبقها كل محرك STT.

```typescript
// src/types/common.ts (تحديث)

/**
 * يمثل مقطعًا صوتيًا يتم إرساله إلى محرك STT.
 */
export interface AudioSegment {
  data: ArrayBuffer; // البيانات الثنائية للمقطع الصوتي
  language: LanguageCode; // لغة الكلام المتوقع
  requestId: string; // معرف الطلب لتتبع العملية
  timestamp: number; // وقت بداية المقطع
}

/**
 * يمثل نتيجة التعرف على الكلام.
 */
export interface STTResult {
  text: string; // النص المعترف به
  engineId: string; // معرف المحرك الذي قام بالتعرف
  latency: number; // زمن الاستجابة بالمللي ثانية
  requestId: string; // معرف الطلب
  isFinal: boolean; // هل هذه هي النتيجة النهائية للمقطع؟
}

/**
 * واجهة محرك التعرف على الكلام (Speech-to-Text).
 */
export interface ISTTEngine {
  id: string;
  name: string;
  
  /**
   * معالجة مقطع صوتي وإرجاع النص المعترف به.
   * @param audioSegment مقطع البيانات الصوتية.
   * @returns وعد بنتيجة التعرف على الكلام.
   */
  process(audioSegment: AudioSegment): Promise<STTResult>;
}
```

### 2. تنفيذ الفئة الأساسية `BaseSTTAdapter`
**الوصف:** إنشاء فئة أساسية مجردة `BaseSTTAdapter` في `src/core/adapters/BaseSTTAdapter.ts` لتوفير وظائف مشتركة.

```typescript
// src/core/adapters/BaseSTTAdapter.ts
import { ISTTEngine, AudioSegment, STTResult } from '../../types/common';

export abstract class BaseSTTAdapter implements ISTTEngine {
  abstract id: string;
  abstract name: string;

  /**
   * تنفيذ مجرد لوظيفة المعالجة.
   */
  abstract process(audioSegment: AudioSegment): Promise<STTResult>;

  /**
   * دالة مساعدة مشتركة لمعالجة استجابات API.
   */
  protected async fetchSTT(url: string, options: RequestInit, audioSegment: AudioSegment): Promise<STTResult> {
    const startTime = Date.now();
    try {
      const response = await fetch(url, options);
      if (!response.ok) {
        throw new Error(`STT API failed with status: ${response.status}`);
      }
      const data = await response.json();
      const recognizedText = this.extractRecognizedText(data);
      const endTime = Date.now();

      return {
        text: recognizedText,
        engineId: this.id,
        latency: endTime - startTime,
        requestId: audioSegment.requestId,
        isFinal: true, // افتراض أن الاستجابة النهائية
      };
    } catch (error) {
      // يجب أن يتم تسجيل الخطأ هنا
      throw error;
    }
  }

  /**
   * دالة مجردة لاستخراج النص المعترف به من استجابة API.
   */
  protected abstract extractRecognizedText(data: any): string;
}
```

## ⚙️ آلية العمل
تضمن الواجهة `ISTTEngine` أن جميع محركات STT تلتزم بعقد موحد، مما يسهل على `STTEngineController` إدارتها والتبديل بينها.

## 🔍 حالات الاستخدام
*   **حالة 1:** `GoogleSTTAdapter` يرث من `BaseSTTAdapter` ويطبق دالة `process` و `extractRecognizedText`.
*   **حالة 2:** `STTEngineController` يستدعي دالة `process` على أي محرك STT دون الحاجة لمعرفة تفاصيله الداخلية.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 01-تنفيذ-واجهة-محرك-STT.md ---


--- START OF FILE: 02-تنفيذ-محول-Google-STT.md ---

# 02-تنفيذ-محول-Google-STT

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/02-تنفيذ-محول-Google-STT.md
**التبعيات:** 01-تنفيذ-واجهة-محرك-STT.md

## 🎯 الغرض والهدف
تنفيذ محول Google Cloud Speech-to-Text (`GoogleSTTAdapter`) الذي يطبق واجهة `ISTTEngine`، ليكون المحرك السحابي الأساسي للتعرف على الكلام.

## 📋 المتطلبات الأساسية
ملف `01-تنفيذ-واجهة-محرك-STT.md` لتوفير الواجهة والفئة الأساسية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-تنفيذ-واجهة-محرك-STT.md` - يوفر `BaseSTTAdapter`.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/13-تنفيذ-وحدة-التحكم-في-الأمان.md` - لتوفير فك تشفير مفتاح API.

### الملفات اللاحقة  
- `03-تنفيذ-محول-Azure-STT.md` - لتنفيذ محول Azure.
- `04-تنفيذ-محول-OpenAI-Whisper.md` - لتنفيذ محول Whisper.

## 🏗 البنية التفصيلية
### 1. تنفيذ `GoogleSTTAdapter`
**الوصف:** إنشاء فئة `GoogleSTTAdapter` في `src/core/adapters/GoogleSTTAdapter.ts` ترث من `BaseSTTAdapter`.

```typescript
// src/core/adapters/GoogleSTTAdapter.ts
import { BaseSTTAdapter } from './BaseSTTAdapter';
import { AudioSegment, STTResult } from '../../types/common';
import { SecurityManager } from '../managers/SecurityManager'; // افتراض وجوده

export class GoogleSTTAdapter extends BaseSTTAdapter {
  id = 'google-stt';
  name = 'Google Cloud STT';

  async process(audioSegment: AudioSegment): Promise<STTResult> {
    // 1. جلب مفتاح API المشفر من SettingsManager
    const encryptedApiKey = '...'; // يتم جلبه من SettingsManager
    // 2. فك تشفير مفتاح API باستخدام SecurityManager
    const apiKey = await SecurityManager.decrypt(encryptedApiKey);

    const url = `https://speech.googleapis.com/v1/speech:recognize?key=${apiKey}`;
    
    // تحويل البيانات الثنائية إلى Base64
    const audioContentBase64 = Buffer.from(audioSegment.data).toString('base64');

    const body = {
      config: {
        encoding: 'LINEAR16', // يجب أن يكون متوافقًا مع تنسيق الصوت الملتقط
        sampleRateHertz: 16000, // يجب أن يكون متوافقًا مع تنسيق الصوت الملتقط
        languageCode: audioSegment.language,
      },
      audio: {
        content: audioContentBase64,
      },
    };

    const options: RequestInit = {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(body),
    };

    return this.fetchSTT(url, options, audioSegment);
  }

  protected extractRecognizedText(data: any): string {
    // منطق استخراج النص المعترف به من استجابة Google STT API
    try {
      return data.results[0].alternatives[0].transcript;
    } catch (e) {
      throw new Error('Failed to parse Google STT response.');
    }
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `GoogleSTTAdapter` لتنفيذ التعرف على الكلام عبر Google Cloud STT API، مما يوفر خيارًا قويًا وفعالًا للتعرف على الكلام.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTEngineController` يختار Google STT بناءً على تفضيلات المستخدم أو جودة التعرف للغة معينة.
*   **حالة 2:** يتم استخدام Google STT كخيار أساسي للتعرف على الكلام في الوقت الفعلي.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 02-تنفيذ-محول-Google-STT.md ---


--- START OF FILE: 03-تنفيذ-محول-Azure-STT.md ---

# 03-تنفيذ-محول-Azure-STT

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/03-تنفيذ-محول-Azure-STT.md
**التبعيات:** 02-تنفيذ-محول-Google-STT.md

## 🎯 الغرض والهدف
تنفيذ محول Azure Cognitive Services Speech-to-Text (`AzureSTTAdapter`) الذي يطبق واجهة `ISTTEngine`، لتوفير خيار سحابي بديل لـ Google STT.

## 📋 المتطلبات الأساسية
ملف `02-تنفيذ-محول-Google-STT.md` (للتسلسل)، و `01-تنفيذ-واجهة-محرك-STT.md` لتوفير الواجهة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-تنفيذ-واجهة-محرك-STT.md` - يوفر `BaseSTTAdapter`.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/13-تنفيذ-وحدة-التحكم-في-الأمان.md` - لتوفير فك تشفير مفتاح API.

### الملفات اللاحقة  
- `04-تنفيذ-محول-OpenAI-Whisper.md` - لتنفيذ محول Whisper.
- `05-تنفيذ-محول-محلي-لـ-STT-متقدم.md` - لتنفيذ محول STT محلي متقدم.

## 🏗 البنية التفصيلية
### 1. تنفيذ `AzureSTTAdapter`
**الوصف:** إنشاء فئة `AzureSTTAdapter` في `src/core/adapters/AzureSTTAdapter.ts` ترث من `BaseSTTAdapter`.

```typescript
// src/core/adapters/AzureSTTAdapter.ts
import { BaseSTTAdapter } from './BaseSTTAdapter';
import { AudioSegment, STTResult } from '../../types/common';
import { SecurityManager } from '../managers/SecurityManager'; // افتراض وجوده

export class AzureSTTAdapter extends BaseSTTAdapter {
  id = 'azure-stt';
  name = 'Azure Cognitive Services STT';

  async process(audioSegment: AudioSegment): Promise<STTResult> {
    // 1. جلب مفتاح API المشفر ونقطة النهاية من SettingsManager
    const encryptedApiKey = '...'; 
    const endpoint = '...'; 
    // 2. فك تشفير مفتاح API باستخدام SecurityManager
    const apiKey = await SecurityManager.decrypt(encryptedApiKey);

    const url = `${endpoint}/speech/recognition/conversation/cognitiveservices/v1?language=${audioSegment.language}`;

    const options: RequestInit = {
      method: 'POST',
      headers: {
        'Ocp-Apim-Subscription-Key': apiKey,
        'Content-Type': 'audio/wav', // يجب أن يكون متوافقًا مع تنسيق الصوت الملتقط
      },
      body: audioSegment.data, // إرسال البيانات الثنائية مباشرة
    };

    return this.fetchSTT(url, options, audioSegment);
  }

  protected extractRecognizedText(data: any): string {
    // منطق استخراج النص المعترف به من استجابة Azure STT API
    try {
      // Azure API قد يتطلب اتصال WebSocket أو معالجة مختلفة، هذا مثال مبسط لـ REST API
      return data.DisplayText;
    } catch (e) {
      throw new Error('Failed to parse Azure STT response.');
    }
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `AzureSTTAdapter` لتنفيذ التعرف على الكلام عبر Azure STT API، مما يوفر خيارًا قويًا آخر، خاصة للغات التي قد يتفوق فيها Azure.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTEngineController` يختار Azure STT بناءً على تفضيلات المستخدم أو جودة التعرف للغة معينة.
*   **حالة 2:** يتم استخدام Azure STT كخيار احتياطي إذا فشل Google STT.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 03-تنفيذ-محول-Azure-STT.md ---


--- START OF FILE: 04-تنفيذ-محول-OpenAI-Whisper.md ---

# 04-تنفيذ-محول-OpenAI-Whisper

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/04-تنفيذ-محول-OpenAI-Whisper.md
**التبعيات:** 03-تنفيذ-محول-Azure-STT.md

## 🎯 الغرض والهدف
تنفيذ محول OpenAI Whisper API (`WhisperSTTAdapter`) الذي يطبق واجهة `ISTTEngine`، للاستفادة من دقة نموذج Whisper في التعرف على الكلام.

## 📋 المتطلبات الأساسية
ملف `03-تنفيذ-محول-Azure-STT.md` (للتسلسل)، و `01-تنفيذ-واجهة-محرك-STT.md` لتوفير الواجهة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-تنفيذ-واجهة-محرك-STT.md` - يوفر `BaseSTTAdapter`.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/13-تنفيذ-وحدة-التحكم-في-الأمان.md` - لتوفير فك تشفير مفتاح API.

### الملفات اللاحقة  
- `05-تنفيذ-محول-محلي-لـ-STT-متقدم.md` - لتنفيذ محول STT محلي متقدم.
- `06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md` - لتنفيذ معالجة الصوت.

## 🏗 البنية التفصيلية
### 1. تنفيذ `WhisperSTTAdapter`
**الوصف:** إنشاء فئة `WhisperSTTAdapter` في `src/core/adapters/WhisperSTTAdapter.ts` ترث من `BaseSTTAdapter`.

```typescript
// src/core/adapters/WhisperSTTAdapter.ts
import { BaseSTTAdapter } from './BaseSTTAdapter';
import { AudioSegment, STTResult } from '../../types/common';
import { SecurityManager } from '../managers/SecurityManager'; // افتراض وجوده

export class WhisperSTTAdapter extends BaseSTTAdapter {
  id = 'whisper-stt';
  name = 'OpenAI Whisper API';

  async process(audioSegment: AudioSegment): Promise<STTResult> {
    // 1. جلب مفتاح API المشفر من SettingsManager
    const encryptedApiKey = '...'; 
    // 2. فك تشفير مفتاح API باستخدام SecurityManager
    const apiKey = await SecurityManager.decrypt(encryptedApiKey);

    const url = `https://api.openai.com/v1/audio/transcriptions`;

    // يجب تحويل AudioSegment إلى ملف FormData لإرساله إلى Whisper API
    const formData = new FormData();
    // يجب أن يكون اسم الملف "file"
    formData.append('file', new Blob([audioSegment.data], { type: 'audio/wav' }), 'audio.wav');
    formData.append('model', 'whisper-1');
    formData = formData.append('language', audioSegment.language); // Whisper يدعم تحديد اللغة

    const options: RequestInit = {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${apiKey}`,
        // لا يتم تعيين Content-Type هنا، يتم تعيينه تلقائيًا بواسطة FormData
      },
      body: formData,
    };

    return this.fetchSTT(url, options, audioSegment);
  }

  protected extractRecognizedText(data: any): string {
    // منطق استخراج النص المعترف به من استجابة Whisper API
    try {
      return data.text;
    } catch (e) {
      throw new Error('Failed to parse Whisper STT response.');
    }
  }
}
```

## ⚙️ آلية العمل
يتم استخدام `WhisperSTTAdapter` للاستفادة من دقة Whisper، خاصة في البيئات الصاخبة أو للغات الأقل شيوعًا.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTEngineController` يختار Whisper STT بناءً على تفضيلات المستخدم أو عندما تكون الدقة هي الأولوية القصوى.
*   **حالة 2:** يتم استخدام Whisper كخيار ثالث للتعرف على الكلام.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 04-تنفيذ-محول-OpenAI-Whisper.md ---


--- START OF FILE: 05-تنفيذ-محول-محلي-لـ-STT-متقدم.md ---

# 05-تنفيذ-محول-محلي-لـ-STT-متقدم

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/05-تنفيذ-محول-محلي-لـ-STT-متقدم.md
**التبعيات:** 04-تنفيذ-محول-OpenAI-Whisper.md

## 🎯 الغرض والهدف
تنفيذ محول STT محلي متقدم (`AdvancedOfflineSTTAdapter`) الذي يطبق واجهة `ISTTEngine`، باستخدام نموذج STT محلي أكثر قوة من النموذج الأساسي (مثل نموذج Whisper أكبر أو نموذج مخصص يعمل عبر WebAssembly).

## 📋 المتطلبات الأساسية
ملف `04-تنفيذ-محول-OpenAI-Whisper.md` (للتسلسل)، و `01-تنفيذ-واجهة-محرك-STT.md` لتوفير الواجهة.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-تنفيذ-واجهة-محرك-STT.md` - يوفر `BaseSTTAdapter`.
- `المرحلة-4-نظام-الترجمة-متعدد-المحركات/07-تنفيذ-محول-محلي-لـ-STT-دون-اتصال.md` - يوفر المحول المحلي الأساسي.

### الملفات اللاحقة  
- `06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md` - لتنفيذ معالجة الصوت.
- `07-تنفيذ-وحدة-التعرف-على-المتحدث.md` - لتنفيذ التعرف على المتحدث.

## 🏗 البنية التفصيلية
### 1. تنفيذ `AdvancedOfflineSTTAdapter`
**الوصف:** إنشاء فئة `AdvancedOfflineSTTAdapter` في `src/core/adapters/AdvancedOfflineSTTAdapter.ts` ترث من `BaseSTTAdapter`.

```typescript
// src/core/adapters/AdvancedOfflineSTTAdapter.ts
import { BaseSTTAdapter } from './BaseSTTAdapter';
import { AudioSegment, STTResult } from '../../types/common';
import { ResourceManager } from '../managers/ResourceManager'; // افتراض وجوده

export class AdvancedOfflineSTTAdapter extends BaseSTTAdapter {
  id = 'advanced-offline-stt';
  name = 'Advanced Offline STT Engine';
  private model: any = null; // نموذج STT محلي متقدم

  constructor(private resourceManager: ResourceManager) {
    super();
  }

  async init(): Promise<void> {
    // 1. تحميل نموذج STT المحلي المتقدم باستخدام ResourceManager
    const modelPath = 'resources/advanced_stt_model.wasm';
    const modelData = await this.resourceManager.loadResource(modelPath);
    // 2. تهيئة النموذج
    this.model = this.initializeModel(modelData);
  }

  async process(audioSegment: AudioSegment): Promise<STTResult> {
    if (!this.model) {
      throw new Error('Advanced Offline STT model not initialized.');
    }

    const startTime = Date.now();
    // 1. تنفيذ التعرف على الكلام باستخدام النموذج المحلي المتقدم
    const recognizedText = await this.model.recognize(audioSegment.data);
    const endTime = Date.now();

    return {
      text: recognizedText,
      engineId: this.id,
      latency: endTime - startTime,
      requestId: audioSegment.requestId,
      isFinal: true,
    };
  }

  protected extractRecognizedText(data: any): string {
    // لا ينطبق على هذا المحول
    return '';
  }

  private initializeModel(data: any): any {
    // منطق تهيئة نموذج WASM المتقدم
    return {
      recognize: (audioData: ArrayBuffer) => `[Advanced Offline STT of ${audioData.byteLength} bytes]`,
    };
  }
}
```

## ⚙️ آلية العمل
يوفر هذا المحول خيارًا محليًا عالي الجودة، مما يقلل من الاعتماد على الخدمات السحابية مع الحفاظ على دقة جيدة.

## 🔍 حالات الاستخدام
*   **حالة 1:** المستخدم يختار وضع "الجودة العالية دون اتصال" في الإعدادات.
*   **حالة 2:** يتم استخدام هذا المحول كخيار احتياطي محلي إذا فشلت جميع المحركات السحابية.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 05-تنفيذ-محول-محلي-لـ-STT-متقدم.md ---


--- START OF FILE: 06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md ---

# 06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md
**التبعيات:** 05-تنفيذ-محول-محلي-لـ-STT-متقدم.md

## 🎯 الغرض والهدف
تنفيذ وحدة معالجة الصوت المتقدمة (`AudioProcessor`) التي تقع في `Content Script`، وهي مسؤولة عن تحسين جودة مقاطع الصوت الملتقطة قبل إرسالها إلى محركات STT.

## 📋 المتطلبات الأساسية
ملف `05-تنفيذ-محول-محلي-لـ-STT-متقدم.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` الذي يوفر البيانات الصوتية الأولية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` - يوفر البيانات الصوتية الأولية.

### الملفات اللاحقة  
- `07-تنفيذ-وحدة-التعرف-على-المتحدث.md` - يستخدم الصوت المعالج.
- `08-تنفيذ-وحدة-التعرف-على-اللغة.md` - يستخدم الصوت المعالج.

## 🏗 البنية التفصيلية
### 1. تنفيذ `AudioProcessor`
**الوصف:** إنشاء فئة `AudioProcessor` في `src/content/services/AudioProcessor.ts` تستخدم Web Audio API.

```typescript
// src/content/services/AudioProcessor.ts
import { AudioSegment } from '../../types/common';

class AudioProcessor {
  private audioContext: AudioContext;

  constructor() {
    this.audioContext = new (window.AudioContext || (window as any).webkitAudioContext)();
  }

  async process(audioSegment: AudioSegment): Promise<AudioSegment> {
    // 1. فك تشفير البيانات الصوتية
    const audioBuffer = await this.audioContext.decodeAudioData(audioSegment.data.slice(0));

    // 2. تطبيق مرشحات متقدمة (مثل إلغاء الضوضاء، التسوية)
    const processedBuffer = this.applyAdvancedFilters(audioBuffer);

    // 3. إعادة ترميز البيانات الصوتية إلى تنسيق مناسب لـ STT (مثل WAV)
    const processedData = this.encodeToWav(processedBuffer);

    return {
      ...audioSegment,
      data: processedData,
    };
  }

  private applyAdvancedFilters(buffer: AudioBuffer): AudioBuffer {
    // منطق متقدم لإلغاء الضوضاء والتسوية
    return buffer;
  }

  private encodeToWav(buffer: AudioBuffer): ArrayBuffer {
    // منطق تحويل AudioBuffer إلى بيانات WAV ثنائية
    return new ArrayBuffer(0);
  }
}
```

## ⚙️ آلية العمل
تعمل `AudioProcessor` على تحسين جودة الصوت، مما يزيد من دقة محركات STT ويقلل من الأخطاء.

## 🔍 حالات الاستخدام
*   **حالة 1:** `AudioCaptureUnit` يرسل مقطع صوتي خام إلى `AudioProcessor` قبل إرساله إلى `STTUnit`.
*   **حالة 2:** يتم استخدام `AudioProcessor` لتطبيق مرشحات إلغاء الضوضاء في بيئة صاخبة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md ---


--- START OF FILE: 07-تنفيذ-وحدة-التعرف-على-المتحدث.md ---

# 07-تنفيذ-وحدة-التعرف-على-المتحدث

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/07-تنفيذ-وحدة-التعرف-على-المتحدث.md
**التبعيات:** 06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md

## 🎯 الغرض والهدف
تنفيذ وحدة التعرف على المتحدث (`SpeakerDiarizationUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحديد المتحدث في كل مقطع صوتي (FR2.0 - تحديد المتحدث).

## 📋 المتطلبات الأساسية
ملف `06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` الذي يوفر البيانات الصوتية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md` - يوفر الصوت المعالج.

### الملفات اللاحقة  
- `08-تنفيذ-وحدة-التعرف-على-اللغة.md` - يستخدم معلومات المتحدث.
- `09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md` - يستخدم معلومات المتحدث.

## 🏗 البنية التفصيلية
### 1. تنفيذ `SpeakerDiarizationUnit`
**الوصف:** إنشاء فئة `SpeakerDiarizationUnit` في `src/core/services/SpeakerDiarizationUnit.ts` تستخدم إما API سحابي أو نموذج محلي خفيف الوزن.

```typescript
// src/core/services/SpeakerDiarizationUnit.ts
import { AudioSegment } from '../../types/common';

export interface SpeakerResult {
  speakerId: string; // معرف المتحدث (مثل "Speaker 1", "Speaker 2")
  confidence: number; // مستوى الثقة
}

class SpeakerDiarizationUnit {
  private knownSpeakers: Map<string, string> = new Map(); // لتخزين المتحدثين المعروفين

  async identifySpeaker(audioSegment: AudioSegment): Promise<SpeakerResult> {
    // 1. استخدام نموذج محلي خفيف الوزن (مثل WebAssembly)
    const speakerId = await this.runLocalDiarizationModel(audioSegment.data);

    // 2. إذا كان المتحدث معروفًا، إرجاع المعرف المخزن
    if (this.knownSpeakers.has(speakerId)) {
      return { speakerId: this.knownSpeakers.get(speakerId)!, confidence: 0.95 };
    }

    // 3. إذا كان المتحدث جديدًا، تعيين معرف جديد
    const newSpeakerId = `Speaker ${this.knownSpeakers.size + 1}`;
    this.knownSpeakers.set(speakerId, newSpeakerId);

    return { speakerId: newSpeakerId, confidence: 0.8 };
  }

  private async runLocalDiarizationModel(audioData: ArrayBuffer): Promise<string> {
    // منطق تشغيل نموذج تحديد المتحدث المحلي
    // مثال: إرجاع تجزئة (Hash) للبيانات الصوتية كمعرف مؤقت
    return `hash_${audioData.byteLength % 3}`;
  }
}
```

## ⚙️ آلية العمل
تضيف `SpeakerDiarizationUnit` طبقة من الذكاء إلى عملية STT، مما يسمح بتمييز النصوص المعترف بها بناءً على المتحدث.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام معرف المتحدث في الترجمة النهائية لعرض "المتحدث 1: النص المترجم".
*   **حالة 2:** يتم استخدام معلومات المتحدث في نظام الخبراء لتطبيق قواعد خاصة بالمتحدث.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 07-تنفيذ-وحدة-التعرف-على-المتحدث.md ---


--- START OF FILE: 08-تنفيذ-وحدة-التعرف-على-اللغة.md ---

# 08-تنفيذ-وحدة-التعرف-على-اللغة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/08-تنفيذ-وحدة-التعرف-على-اللغة.md
**التبعيات:** 07-تنفيذ-وحدة-التعرف-على-المتحدث.md

## 🎯 الغرض والهدف
تنفيذ وحدة التعرف على اللغة (`LanguageDetectionUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحديد اللغة المنطوقة في مقطع الصوت (FR2.0 - تحديد اللغة).

## 📋 المتطلبات الأساسية
ملف `07-تنفيذ-وحدة-التعرف-على-المتحدث.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` الذي يوفر البيانات الصوتية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `07-تنفيذ-وحدة-التعرف-على-المتحدث.md` - يوفر الصوت المعالج.

### الملفات اللاحقة  
- `09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md` - يستخدم اللغة المحددة.
- `10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md` - يستخدم اللغة المحددة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `LanguageDetectionUnit`
**الوصف:** إنشاء فئة `LanguageDetectionUnit` في `src/core/services/LanguageDetectionUnit.ts` تستخدم إما API سحابي أو نموذج محلي خفيف الوزن.

```typescript
// src/core/services/LanguageDetectionUnit.ts
import { AudioSegment, LanguageCode } from '../../types/common';

export interface LanguageResult {
  language: LanguageCode; // رمز اللغة المحددة (مثل 'ar', 'en')
  confidence: number; // مستوى الثقة
}

class LanguageDetectionUnit {
  private defaultLanguage: LanguageCode = 'en';

  async detectLanguage(audioSegment: AudioSegment): Promise<LanguageResult> {
    // 1. استخدام نموذج محلي خفيف الوزن (مثل WebAssembly)
    const detectedCode = await this.runLocalDetectionModel(audioSegment.data);

    // 2. التحقق من الثقة
    if (detectedCode && detectedCode !== 'unknown') {
      return { language: detectedCode as LanguageCode, confidence: 0.9 };
    }

    // 3. العودة إلى اللغة الافتراضية
    return { language: this.defaultLanguage, confidence: 0.5 };
  }

  private async runLocalDetectionModel(audioData: ArrayBuffer): Promise<string> {
    // منطق تشغيل نموذج تحديد اللغة المحلي
    // مثال: إرجاع 'ar' أو 'en' بناءً على تحليل طيفي بسيط
    return 'ar';
  }
}
```

## ⚙️ آلية العمل
تضمن `LanguageDetectionUnit` أن يتم إرسال مقطع الصوت إلى محرك STT المناسب للغة، مما يزيد من دقة التعرف.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام اللغة المحددة لتوجيه مقطع الصوت إلى محرك STT الذي يدعم تلك اللغة بشكل أفضل.
*   **حالة 2:** يتم استخدام اللغة المحددة في نظام الخبراء لتطبيق قواعد خاصة باللغة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 08-تنفيذ-وحدة-التعرف-على-اللغة.md ---


--- START OF FILE: 09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md ---

# 09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md
**التبعيات:** 08-تنفيذ-وحدة-التعرف-على-اللغة.md

## 🎯 الغرض والهدف
تنفيذ وحدة تحديد المصطلحات الخاصة (`TerminologyUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحديد المصطلحات الخاصة بالمجال (مثل الأسماء، الأماكن، المصطلحات التقنية) في النص المعترف به.

## 📋 المتطلبات الأساسية
ملف `08-تنفيذ-وحدة-التعرف-على-اللغة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/18-تنفيذ-وحدة-التعرف-على-الكلام.md` الذي يوفر النص المعترف به.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `08-تنفيذ-وحدة-التعرف-على-اللغة.md` - يوفر اللغة المحددة.

### الملفات اللاحقة  
- `10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md` - لا يوجد ارتباط مباشر.
- `11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `TerminologyUnit`
**الوصف:** إنشاء فئة `TerminologyUnit` في `src/core/services/TerminologyUnit.ts` تستخدم قائمة مصطلحات مخصصة.

```typescript
// src/core/services/TerminologyUnit.ts
import { LanguageCode } from '../../types/common';

export interface TerminologyResult {
  term: string; // المصطلح المحدد
  type: 'PERSON' | 'LOCATION' | 'TECHNICAL' | 'CUSTOM'; // نوع المصطلح
  index: number; // موقع المصطلح في النص
}

class TerminologyUnit {
  private customTerminology: Map<LanguageCode, string[]> = new Map();

  constructor() {
    // تحميل المصطلحات المخصصة من التخزين
    this.customTerminology.set('ar', ['القدس', 'الرياض', 'Manus AI']);
    this.customTerminology.set('en', ['Jerusalem', 'Riyadh', 'Manus AI']);
  }

  async identifyTerms(text: string, language: LanguageCode): Promise<TerminologyResult[]> {
    const results: TerminologyResult[] = [];
    const terms = this.customTerminology.get(language) || [];

    for (const term of terms) {
      let index = text.indexOf(term);
      while (index !== -1) {
        results.push({
          term: term,
          type: 'CUSTOM',
          index: index,
        });
        index = text.indexOf(term, index + 1);
      }
    }

    return results;
  }
}
```

## ⚙️ آلية العمل
تضمن `TerminologyUnit` أن المصطلحات الخاصة يتم تحديدها بشكل صحيح، مما يساعد في تحسين دقة الترجمة (باستخدام ذاكرة الترجمة).

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام المصطلحات المحددة لتطبيق قواعد ترجمة خاصة في `TranslationOrchestrator`.
*   **حالة 2:** يتم استخدام المصطلحات المحددة في نظام الخبراء لتطبيق قواعد خاصة بالمصطلحات.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md ---


--- START OF FILE: 10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md ---

# 10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md
**التبعيات:** 09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md

## 🎯 الغرض والهدف
تنفيذ وحدة تحديد اللهجة واللكنة (`DialectDetectionUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحديد اللهجة أو اللكنة الخاصة بالمتحدث (مثل العربية المصرية، الإنجليزية الأمريكية).

## 📋 المتطلبات الأساسية
ملف `09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` الذي يوفر البيانات الصوتية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md` - يستخدم معلومات اللهجة.
- `12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md` - يستخدم معلومات اللهجة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `DialectDetectionUnit`
**الوصف:** إنشاء فئة `DialectDetectionUnit` في `src/core/services/DialectDetectionUnit.ts` تستخدم نموذجًا مصغرًا لتحديد اللهجة.

```typescript
// src/core/services/DialectDetectionUnit.ts
import { AudioSegment, LanguageCode } from '../../types/common';

export interface DialectResult {
  dialect: string; // رمز اللهجة (مثل 'ar-EG', 'en-US')
  confidence: number; // مستوى الثقة
}

class DialectDetectionUnit {
  async detectDialect(audioSegment: AudioSegment, language: LanguageCode): Promise<DialectResult> {
    // 1. استخدام نموذج محلي خفيف الوزن (مثل WebAssembly)
    const detectedDialect = await this.runLocalDialectModel(audioSegment.data, language);

    // 2. التحقق من الثقة
    if (detectedDialect) {
      return { dialect: detectedDialect, confidence: 0.85 };
    }

    // 3. العودة إلى اللهجة الافتراضية
    return { dialect: `${language}-default`, confidence: 0.6 };
  }

  private async runLocalDialectModel(audioData: ArrayBuffer, language: LanguageCode): Promise<string | null> {
    // منطق تشغيل نموذج تحديد اللهجة المحلي
    if (language === 'ar') {
      return 'ar-EG'; // مثال: افتراض اللهجة المصرية
    }
    return null;
  }
}
```

## ⚙️ آلية العمل
تساعد `DialectDetectionUnit` في اختيار محرك STT الأمثل الذي يدعم اللهجة المحددة، مما يزيد من دقة التعرف.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام اللهجة المحددة لتوجيه مقطع الصوت إلى محرك STT الذي يدعم تلك اللهجة بشكل أفضل.
*   **حالة 2:** يتم استخدام اللهجة المحددة في نظام الخبراء لتطبيق قواعد خاصة باللهجة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md ---


--- START OF FILE: 11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md ---

# 11-تنفيذ-وحدة-تحديد-العواطف-والنبرة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md
**التبعيات:** 10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md

## 🎯 الغرض والهدف
تنفيذ وحدة تحديد العواطف والنبرة (`EmotionDetectionUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحليل مقطع الصوت لتحديد العاطفة والنبرة (مثل الفرح، الغضب، الحزن).

## 📋 المتطلبات الأساسية
ملف `10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/16-تنفيذ-وحدة-التقاط-الصوت.md` الذي يوفر البيانات الصوتية.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md` - يستخدم معلومات العواطف.
- `13-تنفيذ-وحدة-التحقق-من-دقة-STT.md` - يستخدم معلومات العواطف.

## 🏗 البنية التفصيلية
### 1. تنفيذ `EmotionDetectionUnit`
**الوصف:** إنشاء فئة `EmotionDetectionUnit` في `src/core/services/EmotionDetectionUnit.ts` تستخدم نموذجًا مصغرًا لتحديد العواطف.

```typescript
// src/core/services/EmotionDetectionUnit.ts
import { AudioSegment } from '../../types/common';

export interface EmotionResult {
  emotion: 'JOY' | 'ANGER' | 'SADNESS' | 'NEUTRAL' | 'OTHER'; // العاطفة المحددة
  confidence: number; // مستوى الثقة
}

class EmotionDetectionUnit {
  async detectEmotion(audioSegment: AudioSegment): Promise<EmotionResult> {
    // 1. استخدام نموذج محلي خفيف الوزن (مثل WebAssembly)
    const detectedEmotion = await this.runLocalEmotionModel(audioSegment.data);

    // 2. التحقق من الثقة
    if (detectedEmotion) {
      return { emotion: detectedEmotion, confidence: 0.75 };
    }

    // 3. العودة إلى العاطفة الافتراضية
    return { emotion: 'NEUTRAL', confidence: 0.9 };
  }

  private async runLocalEmotionModel(audioData: ArrayBuffer): Promise<EmotionResult['emotion'] | null> {
    // منطق تشغيل نموذج تحديد العواطف المحلي
    // مثال: تحليل بسيط لخصائص الصوت
    return 'NEUTRAL';
  }
}
```

## ⚙️ آلية العمل
تساعد `EmotionDetectionUnit` في توفير سياق إضافي للترجمة، مما يسمح لنظام الخبراء بتطبيق قواعد ترجمة تعكس النبرة العاطفية.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام العاطفة المحددة لتطبيق قواعد ترجمة تعكس النبرة العاطفية.
*   **حالة 2:** يتم استخدام العاطفة المحددة في نظام الخبراء لتطبيق قواعد خاصة بالعواطف.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md ---


--- START OF FILE: 12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md ---

# 12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md
**التبعيات:** 11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md

## 🎯 الغرض والهدف
تنفيذ وحدة معالجة اللغة الطبيعية المتقدمة (`AdvancedNLPUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تحليل النص المعترف به (STT) لاستخراج الكيانات، وتحديد النية، وتصحيح الأخطاء النحوية.

## 📋 المتطلبات الأساسية
ملف `11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/18-تنفيذ-وحدة-التعرف-على-الكلام.md` الذي يوفر النص المعترف به.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md` - يوفر معلومات العواطف.

### الملفات اللاحقة  
- `13-تنفيذ-وحدة-التحقق-من-دقة-STT.md` - يستخدم النص المصحح.
- `14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md` - يستخدم النص المصحح.

## 🏗 البنية التفصيلية
### 1. تنفيذ `AdvancedNLPUnit`
**الوصف:** إنشاء فئة `AdvancedNLPUnit` في `src/core/services/AdvancedNLPUnit.ts` تستخدم نموذجًا مصغرًا لـ NLP.

```typescript
// src/core/services/AdvancedNLPUnit.ts
import { LanguageCode } from '../../types/common';

export interface NLPResult {
  correctedText: string; // النص المصحح
  entities: { type: string, value: string }[]; // الكيانات المستخرجة
  intent: string; // النية المحددة
}

class AdvancedNLPUnit {
  async processText(text: string, language: LanguageCode): Promise<NLPResult> {
    // 1. تصحيح الأخطاء النحوية والإملائية (Post-processing)
    const correctedText = this.correctGrammar(text, language);

    // 2. استخراج الكيانات (Named Entity Recognition)
    const entities = this.extractEntities(correctedText, language);

    // 3. تحديد النية (Intent Detection)
    const intent = this.detectIntent(correctedText);

    return {
      correctedText,
      entities,
      intent,
    };
  }

  private correctGrammar(text: string, language: LanguageCode): string {
    // منطق تصحيح الأخطاء النحوية والإملائية
    return text.replace('مانوس', 'Manus AI');
  }

  private extractEntities(text: string, language: LanguageCode): NLPResult['entities'] {
    // منطق استخراج الكيانات
    return [];
  }

  private detectIntent(text: string): string {
    // منطق تحديد النية
    return 'INFORMATIONAL';
  }
}
```

## ⚙️ آلية العمل
تضمن `AdvancedNLPUnit` أن النص المعترف به يتم تنقيحه وتصحيحه قبل إرساله للترجمة، مما يزيد من دقة الترجمة النهائية.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام النص المصحح كمدخل لـ `TranslationOrchestrator`.
*   **حالة 2:** يتم استخدام الكيانات والنية في نظام الخبراء لتطبيق قواعد ترجمة خاصة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md ---


--- START OF FILE: 13-تنفيذ-وحدة-التحقق-من-دقة-STT.md ---

# 13-تنفيذ-وحدة-التحقق-من-دقة-STT

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/13-تنفيذ-وحدة-التحقق-من-دقة-STT.md
**التبعيات:** 12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md

## 🎯 الغرض والهدف
تنفيذ وحدة التحقق من دقة STT (`STTAccuracyChecker`) التي تقع في `Service Worker`، وهي مسؤولة عن تقييم جودة النص المعترف به وتحديد ما إذا كان يجب إعادة محاولة التعرف باستخدام محرك آخر.

## 📋 المتطلبات الأساسية
ملف `12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/18-تنفيذ-وحدة-التعرف-على-الكلام.md` الذي يوفر النص المعترف به.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md` - يوفر النص المصحح.

### الملفات اللاحقة  
- `14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md` - لا يوجد ارتباط مباشر.
- `15-تنفيذ-وحدة-التحكم-في-جودة-STT.md` - يستخدم نتائج التحقق.

## 🏗 البنية التفصيلية
### 1. تنفيذ `STTAccuracyChecker`
**الوصف:** إنشاء فئة `STTAccuracyChecker` في `src/core/services/STTAccuracyChecker.ts` تستخدم مقاييس لغوية لتقييم الجودة.

```typescript
// src/core/services/STTAccuracyChecker.ts
import { STTResult, LanguageCode } from '../../types/common';

export interface STTQualityResult {
  isAcceptable: boolean; // هل النص مقبول للترجمة؟
  confidenceScore: number; // درجة الثقة الإجمالية
  reason: string; // سبب الرفض أو القبول
}

class STTAccuracyChecker {
  async checkAccuracy(sttResult: STTResult, language: LanguageCode): Promise<STTQualityResult> {
    // 1. مقياس التعقيد اللغوي (Perplexity)
    const perplexity = this.calculatePerplexity(sttResult.text, language);

    // 2. مقياس نسبة الكلمات غير المعروفة (OOV)
    const oovRate = this.calculateOOVRate(sttResult.text, language);

    // 3. مقياس الثقة الإجمالية (مزيج من ثقة المحرك والمقاييس اللغوية)
    const confidenceScore = this.combineScores(sttResult.latency, perplexity, oovRate);

    if (confidenceScore < 0.6) {
      return {
        isAcceptable: false,
        confidenceScore: confidenceScore,
        reason: 'Low linguistic confidence or high OOV rate.',
      };
    }

    return {
      isAcceptable: true,
      confidenceScore: confidenceScore,
      reason: 'Acceptable quality.',
    };
  }

  private calculatePerplexity(text: string, language: LanguageCode): number {
    // منطق حساب التعقيد اللغوي (يتطلب نموذج لغوي محلي)
    return 10; // مثال
  }

  private calculateOOVRate(text: string, language: LanguageCode): number {
    // منطق حساب نسبة الكلمات غير المعروفة
    return 0.05; // مثال
  }

  private combineScores(latency: number, perplexity: number, oovRate: number): number {
    // منطق دمج المقاييس في درجة ثقة واحدة
    return 0.8; // مثال
  }
}
```

## ⚙️ آلية العمل
تضمن `STTAccuracyChecker` أن النص المعترف به ذو جودة عالية قبل إرساله للترجمة، مما يقلل من الأخطاء المتراكمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTUnit` يستخدم نتيجة التحقق لتحديد ما إذا كان يجب إعادة محاولة التعرف باستخدام محرك STT آخر.
*   **حالة 2:** يتم استخدام درجة الثقة في نظام الخبراء لتطبيق قواعد ترجمة خاصة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 13-تنفيذ-وحدة-التحقق-من-دقة-STT.md ---


--- START OF FILE: 14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md ---

# 14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md
**التبعيات:** 13-تنفيذ-وحدة-التحقق-من-دقة-STT.md

## 🎯 الغرض والهدف
تنفيذ وحدة التحقق من دقة الترجمة (`TranslationAccuracyChecker`) التي تقع في `Service Worker`، وهي مسؤولة عن تقييم جودة النص المترجم وتحديد ما إذا كان يجب إعادة محاولة الترجمة باستخدام محرك آخر.

## 📋 المتطلبات الأساسية
ملف `13-تنفيذ-وحدة-التحقق-من-دقة-STT.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/15-تنفيذ-وحدة-الترجمة-المركزية.md` الذي يوفر النص المترجم.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `13-تنفيذ-وحدة-التحقق-من-دقة-STT.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `15-تنفيذ-وحدة-التحكم-في-جودة-STT.md` - لا يوجد ارتباط مباشر.
- `16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md` - يستخدم نتائج التحقق.

## 🏗 البنية التفصيلية
### 1. تنفيذ `TranslationAccuracyChecker`
**الوصف:** إنشاء فئة `TranslationAccuracyChecker` في `src/core/services/TranslationAccuracyChecker.ts` تستخدم مقاييس لغوية لتقييم الجودة.

```typescript
// src/core/services/TranslationAccuracyChecker.ts
import { TranslationResult, LanguageCode } from '../../types/common';

export interface TranslationQualityResult {
  isAcceptable: boolean; // هل الترجمة مقبولة للعرض؟
  confidenceScore: number; // درجة الثقة الإجمالية
  reason: string; // سبب الرفض أو القبول
}

class TranslationAccuracyChecker {
  async checkAccuracy(translationResult: TranslationResult, targetLanguage: LanguageCode): Promise<TranslationQualityResult> {
    // 1. مقياس التعقيد اللغوي للنص المترجم
    const perplexity = this.calculatePerplexity(translationResult.translatedText, targetLanguage);

    // 2. مقياس التباين بين النص الأصلي والمترجم (باستخدام نموذج محلي خفيف الوزن)
    const divergenceScore = this.calculateDivergence(translationResult.originalText, translationResult.translatedText);

    // 3. مقياس الثقة الإجمالية
    const confidenceScore = this.combineScores(translationResult.latency, perplexity, divergenceScore);

    if (confidenceScore < 0.7) {
      return {
        isAcceptable: false,
        confidenceScore: confidenceScore,
        reason: 'Low linguistic confidence or high divergence from source.',
      };
    }

    return {
      isAcceptable: true,
      confidenceScore: confidenceScore,
      reason: 'Acceptable quality.',
    };
  }

  private calculatePerplexity(text: string, language: LanguageCode): number {
    // منطق حساب التعقيد اللغوي
    return 10; // مثال
  }

  private calculateDivergence(source: string, target: string): number {
    // منطق حساب التباين (مثل BLEU Score مبسط)
    return 0.1; // مثال
  }

  private combineScores(latency: number, perplexity: number, divergenceScore: number): number {
    // منطق دمج المقاييس في درجة ثقة واحدة
    return 0.85; // مثال
  }
}
```

## ⚙️ آلية العمل
تضمن `TranslationAccuracyChecker` أن النص المترجم ذو جودة عالية قبل عرضه للمستخدم، مما يقلل من الأخطاء في الترجمة.

## 🔍 حالات الاستخدام
*   **حالة 1:** `TranslationOrchestrator` يستخدم نتيجة التحقق لتحديد ما إذا كان يجب إعادة محاولة الترجمة باستخدام محرك ترجمة آخر.
*   **حالة 2:** يتم استخدام درجة الثقة في نظام الخبراء لتطبيق قواعد ترجمة خاصة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md ---


--- START OF FILE: 15-تنفيذ-وحدة-التحكم-في-جودة-STT.md ---

# 15-تنفيذ-وحدة-التحكم-في-جودة-STT

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/15-تنفيذ-وحدة-التحكم-في-جودة-STT.md
**التبعيات:** 14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md

## 🎯 الغرض والهدف
تنفيذ وحدة التحكم في جودة STT (`STTQualityController`) التي تقع في `Service Worker`، وهي مسؤولة عن إدارة عملية التعرف على الكلام، بما في ذلك اختيار المحرك، وإعادة المحاولة، وتطبيق قواعد نظام الخبراء.

## 📋 المتطلبات الأساسية
ملف `14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/18-تنفيذ-وحدة-التعرف-على-الكلام.md` الذي يوفر النص المعترف به.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md` - لا يوجد ارتباط مباشر.
- `17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md` - يستخدمها للتبديل بين المحركات.

## 🏗 البنية التفصيلية
### 1. تنفيذ `STTQualityController`
**الوصف:** إنشاء فئة `STTQualityController` في `src/core/controllers/STTQualityController.ts` تستخدم `STTAccuracyChecker` و `ExpertSystemUnit`.

```typescript
// src/core/controllers/STTQualityController.ts
import { STTResult, AudioSegment, LanguageCode } from '../../types/common';
import { STTAccuracyChecker } from '../services/STTAccuracyChecker';
import { ExpertSystemUnit } from '../services/ExpertSystemUnit'; // افتراض وجوده
import { STTEngineController } from './STTEngineController'; // افتراض وجوده

class STTQualityController {
  private accuracyChecker: STTAccuracyChecker;
  private expertSystem: ExpertSystemUnit;
  private engineController: STTEngineController;

  constructor(accuracyChecker: STTAccuracyChecker, expertSystem: ExpertSystemUnit, engineController: STTEngineController) {
    this.accuracyChecker = accuracyChecker;
    this.expertSystem = expertSystem;
    this.engineController = engineController;
  }

  async processAudio(audioSegment: AudioSegment, language: LanguageCode): Promise<STTResult> {
    let sttResult: STTResult;
    let attempts = 0;
    const maxAttempts = 3;

    while (attempts < maxAttempts) {
      // 1. اختيار المحرك الأمثل بناءً على نظام الخبراء
      const engineId = this.expertSystem.selectSTTEngine(language, attempts);
      
      // 2. تنفيذ التعرف على الكلام
      sttResult = await this.engineController.process(audioSegment, engineId);

      // 3. التحقق من الدقة
      const qualityResult = await this.accuracyChecker.checkAccuracy(sttResult, language);

      if (qualityResult.isAcceptable) {
        return sttResult;
      }

      // 4. تطبيق قواعد نظام الخبراء لإعادة المحاولة
      const retryAction = this.expertSystem.determineSTTRetryAction(qualityResult, sttResult);

      if (retryAction.shouldRetry) {
        attempts++;
        // تسجيل سبب إعادة المحاولة
        continue;
      } else {
        // إذا لم يوصي نظام الخبراء بإعادة المحاولة، نخرج بالنتيجة الحالية
        return sttResult;
      }
    }

    return sttResult!; // إرجاع آخر نتيجة حتى لو كانت غير مقبولة
  }
}
```

## ⚙️ آلية العمل
تضمن `STTQualityController` أن يتم استخدام أفضل محرك STT متاح وأن يتم إعادة المحاولة تلقائيًا إذا كانت الجودة غير مقبولة.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استدعاء `STTQualityController.processAudio` من قبل `TranslationOrchestrator`.
*   **حالة 2:** يتم استخدام نظام الخبراء لتحديد ما إذا كان يجب التبديل من محرك سحابي إلى محرك محلي.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 15-تنفيذ-وحدة-التحكم-في-جودة-STT.md ---


--- START OF FILE: 16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md ---

# 16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md
**التبعيات:** 15-تنفيذ-وحدة-التحكم-في-جودة-STT.md

## 🎯 الغرض والهدف
تنفيذ وحدة التحكم في جودة الترجمة (`TranslationQualityController`) التي تقع في `Service Worker`، وهي مسؤولة عن إدارة عملية الترجمة، بما في ذلك اختيار المحرك، وإعادة المحاولة، وتطبيق قواعد نظام الخبراء.

## 📋 المتطلبات الأساسية
ملف `15-تنفيذ-وحدة-التحكم-في-جودة-STT.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/15-تنفيذ-وحدة-الترجمة-المركزية.md` الذي يوفر النص المترجم.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `15-تنفيذ-وحدة-التحكم-في-جودة-STT.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md` - لا يوجد ارتباط مباشر.
- `18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `TranslationQualityController`
**الوصف:** إنشاء فئة `TranslationQualityController` في `src/core/controllers/TranslationQualityController.ts` تستخدم `TranslationAccuracyChecker` و `ExpertSystemUnit`.

```typescript
// src/core/controllers/TranslationQualityController.ts
import { TranslationResult, LanguageCode } from '../../types/common';
import { TranslationAccuracyChecker } from '../services/TranslationAccuracyChecker';
import { ExpertSystemUnit } from '../services/ExpertSystemUnit'; // افتراض وجوده
import { TranslationEngineController } from './TranslationEngineController'; // افتراض وجوده

class TranslationQualityController {
  private accuracyChecker: TranslationAccuracyChecker;
  private expertSystem: ExpertSystemUnit;
  private engineController: TranslationEngineController;

  constructor(accuracyChecker: TranslationAccuracyChecker, expertSystem: ExpertSystemUnit, engineController: TranslationEngineController) {
    this.accuracyChecker = accuracyChecker;
    this.expertSystem = expertSystem;
    this.engineController = engineController;
  }

  async processTranslation(text: string, sourceLang: LanguageCode, targetLang: LanguageCode): Promise<TranslationResult> {
    let translationResult: TranslationResult;
    let attempts = 0;
    const maxAttempts = 3;

    while (attempts < maxAttempts) {
      // 1. اختيار المحرك الأمثل بناءً على نظام الخبراء
      const engineId = this.expertSystem.selectTranslationEngine(sourceLang, targetLang, attempts);
      
      // 2. تنفيذ الترجمة
      translationResult = await this.engineController.translate(text, sourceLang, targetLang, engineId);

      // 3. التحقق من الدقة
      const qualityResult = await this.accuracyChecker.checkAccuracy(translationResult, targetLang);

      if (qualityResult.isAcceptable) {
        return translationResult;
      }

      // 4. تطبيق قواعد نظام الخبراء لإعادة المحاولة
      const retryAction = this.expertSystem.determineTranslationRetryAction(qualityResult, translationResult);

      if (retryAction.shouldRetry) {
        attempts++;
        // تسجيل سبب إعادة المحاولة
        continue;
      } else {
        // إذا لم يوصي نظام الخبراء بإعادة المحاولة، نخرج بالنتيجة الحالية
        return translationResult;
      }
    }

    return translationResult!; // إرجاع آخر نتيجة حتى لو كانت غير مقبولة
  }
}
```

## ⚙️ آلية العمل
تضمن `TranslationQualityController` أن يتم استخدام أفضل محرك ترجمة متاح وأن يتم إعادة المحاولة تلقائيًا إذا كانت الجودة غير مقبولة.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استدعاء `TranslationQualityController.processTranslation` من قبل `TranslationOrchestrator`.
*   **حالة 2:** يتم استخدام نظام الخبراء لتحديد ما إذا كان يجب التبديل من محرك سحابي إلى محرك محلي.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md ---


--- START OF FILE: 17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md ---

# 17-تنفيذ-وحدة-إدارة-المحركات-المحلية

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md
**التبعيات:** 16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة المحركات المحلية (`LocalEngineManager`) التي تقع في `Service Worker`، وهي مسؤولة عن تحميل، تهيئة، وإدارة محركات STT والترجمة التي تعمل محليًا (مثل نماذج WebAssembly).

## 📋 المتطلبات الأساسية
ملف `16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/08-تنفيذ-وحدة-إدارة-الموارد.md` الذي يوفر إدارة الموارد.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md` - لا يوجد ارتباط مباشر.
- `19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `LocalEngineManager`
**الوصف:** إنشاء فئة `LocalEngineManager` في `src/core/managers/LocalEngineManager.ts` تستخدم `ResourceManager` لتهيئة المحركات المحلية.

```typescript
// src/core/managers/LocalEngineManager.ts
import { ISTTEngine, ITranslationEngine } from '../../types/common';
import { ResourceManager } from './ResourceManager'; // افتراض وجوده
import { AdvancedOfflineSTTAdapter } from '../adapters/AdvancedOfflineSTTAdapter'; // افتراض وجوده

class LocalEngineManager {
  private sttEngines: Map<string, ISTTEngine> = new Map();
  private translationEngines: Map<string, ITranslationEngine> = new Map();

  constructor(private resourceManager: ResourceManager) {}

  async initialize(): Promise<void> {
    // 1. تهيئة محرك STT المحلي المتقدم
    const advancedSTT = new AdvancedOfflineSTTAdapter(this.resourceManager);
    await advancedSTT.init();
    this.sttEngines.set(advancedSTT.id, advancedSTT);

    // 2. تهيئة محرك الترجمة المحلي (افتراضي)
    // ...
  }

  getSTTEngine(id: string): ISTTEngine | undefined {
    return this.sttEngines.get(id);
  }

  getTranslationEngine(id: string): ITranslationEngine | undefined {
    return this.translationEngines.get(id);
  }
}
```

## ⚙️ آلية العمل
تضمن `LocalEngineManager` أن المحركات المحلية جاهزة للاستخدام عند الحاجة، مما يقلل من زمن الاستجابة ويعزز الخصوصية.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTQualityController` يطلب محرك STT محليًا من `LocalEngineManager`.
*   **حالة 2:** يتم استخدام `LocalEngineManager` للتحقق من حالة المحركات المحلية (جاهزة، قيد التحميل، إلخ).

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md ---


--- START OF FILE: 18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md ---

# 18-تنفيذ-وحدة-إدارة-المحركات-السحابية

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md
**التبعيات:** 17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة المحركات السحابية (`CloudEngineManager`) التي تقع في `Service Worker`، وهي مسؤولة عن تهيئة، وتكوين، وإدارة محركات STT والترجمة التي تعتمد على الخدمات السحابية (مثل Google, Azure, OpenAI).

## 📋 المتطلبات الأساسية
ملف `17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/07-تنفيذ-وحدة-الأمان-والخصوصية.md` الذي يوفر إدارة المفاتيح.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md` - لا يوجد ارتباط مباشر.
- `20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `CloudEngineManager`
**الوصف:** إنشاء فئة `CloudEngineManager` في `src/core/managers/CloudEngineManager.ts` تستخدم المحولات السحابية.

```typescript
// src/core/managers/CloudEngineManager.ts
import { ISTTEngine, ITranslationEngine } from '../../types/common';
import { GoogleSTTAdapter } from '../adapters/GoogleSTTAdapter';
import { AzureSTTAdapter } from '../adapters/AzureSTTAdapter';
import { WhisperSTTAdapter } from '../adapters/WhisperSTTAdapter';

class CloudEngineManager {
  private sttEngines: Map<string, ISTTEngine> = new Map();
  private translationEngines: Map<string, ITranslationEngine> = new Map();

  constructor() {
    // 1. تهيئة محركات STT السحابية
    const googleSTT = new GoogleSTTAdapter();
    this.sttEngines.set(googleSTT.id, googleSTT);

    const azureSTT = new AzureSTTAdapter();
    this.sttEngines.set(azureSTT.id, azureSTT);

    const whisperSTT = new WhisperSTTAdapter();
    this.sttEngines.set(whisperSTT.id, whisperSTT);

    // 2. تهيئة محركات الترجمة السحابية (افتراضي)
    // ...
  }

  getSTTEngine(id: string): ISTTEngine | undefined {
    return this.sttEngines.get(id);
  }

  getTranslationEngine(id: string): ITranslationEngine | undefined {
    return this.translationEngines.get(id);
  }
}
```

## ⚙️ آلية العمل
تضمن `CloudEngineManager` أن المحركات السحابية جاهزة للاستخدام عند الحاجة، مع إدارة مفاتيح API بشكل آمن.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTQualityController` يطلب محرك STT سحابيًا من `CloudEngineManager`.
*   **حالة 2:** يتم استخدام `CloudEngineManager` لتحديث تكوين المحركات السحابية (مثل تغيير المنطقة).

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md ---


--- START OF FILE: 19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md ---

# 19-تنفيذ-وحدة-إدارة-النماذج-المخصصة

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md
**التبعيات:** 18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة النماذج المخصصة (`CustomModelManager`) التي تقع في `Service Worker`، وهي مسؤولة عن تحميل، وتخزين، وتطبيق النماذج المخصصة للتعرف على الكلام والترجمة (مثل نماذج STT المخصصة للمصطلحات الطبية).

## 📋 المتطلبات الأساسية
ملف `18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/05-تنفيذ-وحدة-إدارة-التخزين.md` الذي يوفر التخزين.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md` - لا يوجد ارتباط مباشر.
- `21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `CustomModelManager`
**الوصف:** إنشاء فئة `CustomModelManager` في `src/core/managers/CustomModelManager.ts` تستخدم `StorageManager` لتخزين النماذج.

```typescript
// src/core/managers/CustomModelManager.ts
import { StorageManager } from './StorageManager'; // افتراض وجوده

export interface CustomModel {
  id: string;
  type: 'STT' | 'TRANSLATION';
  language: string;
  version: string;
  data: ArrayBuffer; // بيانات النموذج
}

class CustomModelManager {
  private activeModels: Map<string, CustomModel> = new Map();

  constructor(private storageManager: StorageManager) {}

  async loadModel(modelId: string): Promise<CustomModel | null> {
    // 1. جلب بيانات النموذج من التخزين
    const modelData = await this.storageManager.get(`custom-model-${modelId}`);
    if (!modelData) return null;

    // 2. تهيئة النموذج وتخزينه في الذاكرة
    const model: CustomModel = JSON.parse(modelData);
    this.activeModels.set(model.id, model);
    return model;
  }

  getActiveModel(type: 'STT' | 'TRANSLATION', language: string): CustomModel | undefined {
    // منطق اختيار النموذج المخصص الأنسب
    return Array.from(this.activeModels.values()).find(m => m.type === type && m.language === language);
  }
}
```

## ⚙️ آلية العمل
تسمح `CustomModelManager` للمستخدمين بتحميل نماذج STT أو الترجمة المخصصة، مما يزيد من دقة النظام في المجالات المتخصصة.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام نموذج STT مخصص للمصطلحات الطبية.
*   **حالة 2:** يتم استخدام نموذج ترجمة مخصص للمصطلحات القانونية.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md ---


--- START OF FILE: 20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md ---

# 20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md
**التبعيات:** 19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة الترخيص والاستخدام (`LicenseManager`) التي تقع في `Service Worker`، وهي مسؤولة عن تتبع استخدام المحركات السحابية والمحلية، والتحقق من صلاحية الترخيص.

## 📋 المتطلبات الأساسية
ملف `19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/07-تنفيذ-وحدة-الأمان-والخصوصية.md` الذي يوفر إدارة المفاتيح.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md` - لا يوجد ارتباط مباشر.
- `22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md` - لا يوجد ارتباط مباشر.

## 🏗 البنية التفصيلية
### 1. تنفيذ `LicenseManager`
**الوصف:** إنشاء فئة `LicenseManager` في `src/core/managers/LicenseManager.ts` تتفاعل مع خادم الترخيص.

```typescript
// src/core/managers/LicenseManager.ts
import { StorageManager } from './StorageManager'; // افتراض وجوده

export interface UsageMetrics {
  cloudSTT: number; // عدد الدقائق المستخدمة
  cloudTranslation: number; // عدد الأحرف المترجمة
  offlineSTT: number; // عدد الدقائق المستخدمة
}

class LicenseManager {
  private licenseKey: string = '';
  private usageMetrics: UsageMetrics = { cloudSTT: 0, cloudTranslation: 0, offlineSTT: 0 };

  constructor(private storageManager: StorageManager) {}

  async loadLicense(): Promise<void> {
    this.licenseKey = await this.storageManager.get('license-key') || '';
    const metrics = await this.storageManager.get('usage-metrics');
    if (metrics) {
      this.usageMetrics = JSON.parse(metrics);
    }
  }

  async checkLicenseValidity(): Promise<boolean> {
    // منطق التحقق من صلاحية الترخيص عبر خادم الترخيص
    return this.licenseKey.length > 0;
  }

  async recordUsage(type: keyof UsageMetrics, amount: number): Promise<void> {
    this.usageMetrics[type] += amount;
    await this.storageManager.set('usage-metrics', JSON.stringify(this.usageMetrics));
    // إرسال تحديث الاستخدام إلى خادم الترخيص
  }

  getRemainingUsage(type: keyof UsageMetrics): number {
    // منطق حساب الاستخدام المتبقي بناءً على خطة الترخيص
    return 1000 - this.usageMetrics[type]; // مثال
  }
}
```

## ⚙️ آلية العمل
تضمن `LicenseManager` أن يتم استخدام المحركات السحابية والمحلية ضمن حدود الترخيص، وتوفر معلومات الاستخدام لنظام الخبراء.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTQualityController` يتحقق من الاستخدام المتبقي قبل استخدام محرك سحابي.
*   **حالة 2:** يتم استخدام معلومات الترخيص في نظام الخبراء لتطبيق قواعد خاصة بالترخيص.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md ---


--- START OF FILE: 21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md ---

# 21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md
**التبعيات:** 20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md

## 🎯 الغرض والهدف
تنفيذ وحدة إدارة التكوين الديناميكي (`DynamicConfigManager`) التي تقع في `Service Worker`، وهي مسؤولة عن جلب وتطبيق التكوينات والتحديثات في الوقت الفعلي من خادم مركزي.

## 📋 المتطلبات الأساسية
ملف `20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/06-تنفيذ-وحدة-إدارة-الإعدادات.md` الذي يوفر الإعدادات.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md` - لا يوجد ارتباط مباشر.
- `DELIVERABLES_PHASE5.md` - ملف تسليمات المرحلة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `DynamicConfigManager`
**الوصف:** إنشاء فئة `DynamicConfigManager` في `src/core/managers/DynamicConfigManager.ts` تتفاعل مع خادم التكوين.

```typescript
// src/core/managers/DynamicConfigManager.ts
import { SettingsManager } from './SettingsManager'; // افتراض وجوده

export interface DynamicConfig {
  sttEngineWeights: { [key: string]: number }; // أوزان المحركات
  translationEngineWeights: { [key: string]: number };
  featureFlags: { [key: string]: boolean }; // ميزات التشغيل/الإيقاف
}

class DynamicConfigManager {
  private config: DynamicConfig = { sttEngineWeights: {}, translationEngineWeights: {}, featureFlags: {} };

  constructor(private settingsManager: SettingsManager) {}

  async fetchConfig(): Promise<void> {
    // 1. جلب التكوين من خادم مركزي
    const response = await fetch('https://config.example.com/api/v1/config');
    const newConfig: DynamicConfig = await response.json();

    // 2. تطبيق التكوين الجديد
    this.config = newConfig;
    
    // 3. تحديث الإعدادات المحلية
    this.settingsManager.updateDynamicSettings(newConfig);
  }

  getSTTWeights(): { [key: string]: number } {
    return this.config.sttEngineWeights;
  }

  isFeatureEnabled(feature: string): boolean {
    return this.config.featureFlags[feature] || false;
  }
}
```

## ⚙️ آلية العمل
تسمح `DynamicConfigManager` بتحديث سلوك النظام دون الحاجة إلى تحديث المتصفح، مما يسهل الصيانة والتحسين.

## 🔍 حالات الاستخدام
*   **حالة 1:** يتم استخدام أوزان المحركات في نظام الخبراء لاختيار المحرك الأمثل.
*   **حالة 2:** يتم استخدام علامات الميزات لتمكين أو تعطيل ميزات جديدة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md ---


--- START OF FILE: 22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md ---

# 22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء

<!-- Metadata -->
**النوع:** تنفيذ
**المرحلة:** 5
**المسار:** المرحلة-5-نظام-التعرف-على-الكلام/22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md
**التبعيات:** 21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md

## 🎯 الغرض والهدف
تنفيذ وحدة التكامل مع نظام الخبراء (`ExpertSystemIntegrationUnit`) التي تقع في `Service Worker`، وهي مسؤولة عن تجميع جميع البيانات السياقية (اللغة، اللهجة، العاطفة، الجودة، الترخيص) وتمريرها إلى نظام الخبراء لاتخاذ قرارات حاسمة.

## 📋 المتطلبات الأساسية
ملف `21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md` (للتسلسل)، و `المرحلة-3-النواة-الأساسية/19-تنفيذ-وحدة-نظام-الخبراء.md` الذي يوفر نظام الخبراء.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md` - لا يوجد ارتباط مباشر.

### الملفات اللاحقة  
- `DELIVERABLES_PHASE5.md` - ملف تسليمات المرحلة.
- `ACCEPTANCE_CRITERIA_PHASE5.md` - ملف معايير القبول للمرحلة.

## 🏗 البنية التفصيلية
### 1. تنفيذ `ExpertSystemIntegrationUnit`
**الوصف:** إنشاء فئة `ExpertSystemIntegrationUnit` في `src/core/services/ExpertSystemIntegrationUnit.ts` تعمل كواجهة بين الوحدات الأخرى ونظام الخبراء.

```typescript
// src/core/services/ExpertSystemIntegrationUnit.ts
import { AudioSegment, STTResult, TranslationResult, LanguageCode } from '../../types/common';
import { ExpertSystemUnit } from '../managers/ExpertSystemUnit'; // افتراض وجوده
import { LicenseManager } from '../managers/LicenseManager';
import { DynamicConfigManager } from '../managers/DynamicConfigManager';
import { STTQualityResult } from './STTAccuracyChecker';
import { TranslationQualityResult } from './TranslationAccuracyChecker';

class ExpertSystemIntegrationUnit {
  private expertSystem: ExpertSystemUnit;
  private licenseManager: LicenseManager;
  private configManager: DynamicConfigManager;

  constructor(expertSystem: ExpertSystemUnit, licenseManager: LicenseManager, configManager: DynamicConfigManager) {
    this.expertSystem = expertSystem;
    this.licenseManager = licenseManager;
    this.configManager = configManager;
  }

  async getSTTDecision(audioSegment: AudioSegment, language: LanguageCode, qualityResult: STTQualityResult): Promise<string> {
    const context = {
      language: language,
      audioLength: audioSegment.data.byteLength,
      licenseUsage: this.licenseManager.getRemainingUsage('cloudSTT'),
      qualityConfidence: qualityResult.confidenceScore,
      engineWeights: this.configManager.getSTTWeights(),
      // ... المزيد من السياق (اللهجة، العاطفة، إلخ)
    };

    return this.expertSystem.decideSTTEngine(context);
  }

  async getTranslationDecision(sttResult: STTResult, targetLang: LanguageCode, qualityResult: TranslationQualityResult): Promise<string> {
    const context = {
      sourceLanguage: sttResult.language,
      targetLanguage: targetLang,
      textLength: sttResult.text.length,
      licenseUsage: this.licenseManager.getRemainingUsage('cloudTranslation'),
      qualityConfidence: qualityResult.confidenceScore,
      engineWeights: this.configManager.getTranslationWeights(),
      // ... المزيد من السياق
    };

    return this.expertSystem.decideTranslationEngine(context);
  }
}
```

## ⚙️ آلية العمل
تضمن `ExpertSystemIntegrationUnit` أن نظام الخبراء يتلقى جميع المعلومات اللازمة لاتخاذ قرارات مستنيرة بشأن اختيار المحرك، وإعادة المحاولة، وتطبيق القواعد.

## 🔍 حالات الاستخدام
*   **حالة 1:** `STTQualityController` يستخدم `ExpertSystemIntegrationUnit` لتحديد المحرك الأمثل.
*   **حالة 2:** يتم استخدام `ExpertSystemIntegrationUnit` لتطبيق قواعد نظام الخبراء على نتائج الجودة.

---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط

--- END OF FILE: 22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md ---


--- START OF FILE: ACCEPTANCE_CRITERIA_PHASE5.md ---

# معايير القبول للمرحلة 5: نظام التعرف على الكلام (STT)

## 🎯 الهدف
تحديد المعايير التي يجب تحقيقها لقبول تسليمات المرحلة الخامسة.

## ✅ معايير القبول
| المعيار | الوصف | حالة التحقق |
| :--- | :--- | :--- |
| **اكتمال الهيكل** | يجب أن تكون جميع الملفات الـ 25 المذكورة في `DELIVERABLES_PHASE5.md` موجودة في مجلد المرحلة. | [ ] مكتمل |
| **التوافق مع الواجهة** | يجب أن تلتزم جميع محولات STT (السحابية والمحلية) بالواجهة الموحدة `ISTTEngine` المحددة في `01-تنفيذ-واجهة-محرك-STT.md`. | [ ] مكتمل |
| **التحكم في الجودة** | يجب أن تكون وحدات `STTQualityController` و `TranslationQualityController` قادرة على استخدام نتائج `AccuracyChecker` ونظام الخبراء لاتخاذ قرارات إعادة المحاولة واختيار المحرك. | [ ] مكتمل |
| **إدارة المحركات** | يجب أن تكون وحدات `LocalEngineManager` و `CloudEngineManager` قادرة على تهيئة وإدارة المحركات الخاصة بها بشكل صحيح. | [ ] مكتمل |
| **التحليل الصوتي المتقدم** | يجب أن تكون وحدات `AudioProcessor`، `SpeakerDiarizationUnit`، `LanguageDetectionUnit`، و `EmotionDetectionUnit` قادرة على معالجة البيانات الصوتية واستخراج المعلومات المطلوبة. | [ ] مكتمل |
| **التكامل مع نظام الخبراء** | يجب أن تكون `ExpertSystemIntegrationUnit` قادرة على تجميع جميع البيانات السياقية اللازمة لنظام الخبراء. | [ ] مكتمل |
| **إدارة التكوين** | يجب أن تكون `DynamicConfigManager` قادرة على جلب وتطبيق التكوينات الديناميكية، ويجب أن تكون `LicenseManager` قادرة على تتبع الاستخدام. | [ ] مكتمل |

## 📝 ملاحظات
يجب أن يتم التحقق من هذه المعايير من قبل فريق ضمان الجودة قبل الانتقال إلى المرحلة التالية.

--- END OF FILE: ACCEPTANCE_CRITERIA_PHASE5.md ---


--- START OF FILE: DELIVERABLES_PHASE5.md ---

# تسليمات المرحلة 5: نظام التعرف على الكلام (STT)

## 🎯 الهدف
تحديد جميع المخرجات والملفات التي يجب تسليمها عند اكتمال المرحلة الخامسة من المشروع.

## 📦 قائمة التسليمات
1.  **واجهة محرك STT الأساسية:**
    *   `01-تنفيذ-واجهة-محرك-STT.md`
2.  **محولات STT السحابية:**
    *   `02-تنفيذ-محول-Google-STT.md`
    *   `03-تنفيذ-محول-Azure-STT.md`
    *   `04-تنفيذ-محول-OpenAI-Whisper.md`
3.  **محول STT المحلي المتقدم:**
    *   `05-تنفيذ-محول-محلي-لـ-STT-متقدم.md`
4.  **وحدات معالجة الصوت واللغة الطبيعية:**
    *   `06-تنفيذ-وحدة-معالجة-الصوت-المتقدمة.md`
    *   `07-تنفيذ-وحدة-التعرف-على-المتحدث.md`
    *   `08-تنفيذ-وحدة-التعرف-على-اللغة.md`
    *   `09-تنفيذ-وحدة-تحديد-المصطلحات-الخاصة.md`
    *   `10-تنفيذ-وحدة-تحديد-اللهجة-واللكنة.md`
    *   `11-تنفيذ-وحدة-تحديد-العواطف-والنبرة.md`
    *   `12-تنفيذ-وحدة-معالجة-اللغة-الطبيعية-المتقدمة.md`
5.  **وحدات التحقق من الجودة والتحكم:**
    *   `13-تنفيذ-وحدة-التحقق-من-دقة-STT.md`
    *   `14-تنفيذ-وحدة-التحقق-من-دقة-الترجمة.md`
    *   `15-تنفيذ-وحدة-التحكم-في-جودة-STT.md`
    *   `16-تنفيذ-وحدة-التحكم-في-جودة-الترجمة.md`
6.  **وحدات إدارة المحركات والتكوين:**
    *   `17-تنفيذ-وحدة-إدارة-المحركات-المحلية.md`
    *   `18-تنفيذ-وحدة-إدارة-المحركات-السحابية.md`
    *   `19-تنفيذ-وحدة-إدارة-النماذج-المخصصة.md`
    *   `20-تنفيذ-وحدة-إدارة-الترخيص-والاستخدام.md`
    *   `21-تنفيذ-وحدة-إدارة-التكوين-الديناميكي.md`
7.  **وحدة التكامل مع نظام الخبراء:**
    *   `22-تنفيذ-وحدة-التكامل-مع-نظام-الخبراء.md`
8.  **ملفات المرحلة الإدارية:**
    *   `DELIVERABLES_PHASE5.md` (هذا الملف)
    *   `ACCEPTANCE_CRITERIA_PHASE5.md`
    *   `SUCCESS_METRICS_PHASE5.md`

## 📝 ملاحظات
يجب أن تكون جميع الملفات المذكورة أعلاه موجودة في مجلد **المرحلة-5-نظام-التعرف-على-الكلام/**.

--- END OF FILE: DELIVERABLES_PHASE5.md ---


--- START OF FILE: SUCCESS_METRICS_PHASE5.md ---

# مقاييس النجاح للمرحلة 5: نظام التعرف على الكلام (STT)

## 🎯 الهدف
تحديد المقاييس الكمية والنوعية التي تقيس نجاح المرحلة الخامسة.

## 📈 مقاييس النجاح
| المقياس | الوصف | القيمة المستهدفة |
| :--- | :--- | :--- |
| **تغطية المحركات** | عدد محولات STT السحابية والمحلية التي تم تنفيذها. | 4 محولات على الأقل |
| **زمن استجابة STT** | متوسط زمن الاستجابة للتعرف على مقطع صوتي مدته 5 ثوانٍ (باستخدام المحرك المحلي). | < 1000 مللي ثانية |
| **دقة التعرف (Word Error Rate)** | معدل خطأ الكلمات (WER) للمحرك المحلي المتقدم على مجموعة بيانات اختبار داخلية. | < 15% |
| **فعالية نظام الجودة** | نسبة الحالات التي تم فيها تحسين جودة STT أو الترجمة من خلال إعادة المحاولة أو التبديل بين المحركات. | > 80% |
| **تغطية التحليل الصوتي** | عدد وحدات التحليل الصوتي المتقدمة (المتحدث، اللغة، العاطفة) التي تم تنفيذها. | 3 وحدات على الأقل |
| **تكامل نظام الخبراء** | نسبة الوحدات التي تتفاعل مع نظام الخبراء لاتخاذ قرارات ديناميكية. | 100% من وحدات التحكم في الجودة |

## 📝 ملاحظات
سيتم استخدام هذه المقاييس لتقييم أداء المرحلة الخامسة وتحديد ما إذا كانت جاهزة للانتقال إلى المرحلة التالية.

--- END OF FILE: SUCCESS_METRICS_PHASE5.md ---

