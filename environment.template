# environment.template

## 🎯 الغرض والهدف
قالب لملف متغيرات البيئة (`.env`) الذي يجب على المطورين نسخه وتعبئته لتشغيل المشروع في بيئة التطوير.

```ini
# ----------------------------------------------------------------
# إعدادات البيئة الأساسية
# ----------------------------------------------------------------
# يجب أن تكون 'development' في بيئة التطوير
NODE_ENV=development
# منفذ تشغيل الخادم الخلفي
PORT=3000
# مستوى التسجيل (debug, info, warn, error)
LOG_LEVEL=debug

# ----------------------------------------------------------------
# إعدادات قاعدة البيانات (لبيئة التطوير المحلية)
# ----------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_USER=stt_user
DB_PASSWORD=secure_dev_password
DB_NAME=stt_translator_db

# ----------------------------------------------------------------
# مفاتيح API لخدمات الطرف الثالث (للتطوير)
# ----------------------------------------------------------------
# مفتاح API لخدمة Google Cloud Speech-to-Text
GOOGLE_API_KEY=
# مفتاح API لخدمة Azure Cognitive Services
AZURE_API_KEY=
# مفتاح API لخدمة DeepL Translation
DEEPL_API_KEY=

# ----------------------------------------------------------------
# إعدادات الأمان
# ----------------------------------------------------------------
# مفتاح سري لتوقيع رموز JWT (يجب أن يكون طويلاً ومعقداً)
JWT_SECRET=
# مفتاح التشفير الرئيسي للبيانات الحساسة
MASTER_ENCRYPTION_KEY=
```
---
**ملاحظات الجودة:**
- [ ] تم التحقق من الهيكل
- [ ] تم التحقق من الروابط
- [ ] تم مراجعة المحتوى
- [ ] تم تحقيق الترابط
