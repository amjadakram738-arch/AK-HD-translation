# وثيقة تكوين وإدارة البيئات والمواصفات للمرحلة العاشرة

---

# 01-إعداد بيئة التطوير

## 🎯 الغرض والهدف
تحديد وتوثيق الإجراءات اللازمة لإعداد بيئة تطوير متكاملة وموحدة لجميع أعضاء فريق المشروع، لضمان التناسق وتقليل أخطاء "يعمل على جهازي".

## 📋 المتطلبات الأساسية
اكتمال جميع ملفات المرحلة 9 (التحسين والأداء).

## 🔗 العلاقات والترابط
### الملفات السابقة
- `المرحلة-9-التحسين-والأداء/10-إنشاء-تقرير-الأداء-النهائي-للمرحلة-9.md` - تقرير الأداء النهائي للمرحلة السابقة.

### الملفات اللاحقة
- `02-إعداد-بيئة-الاختبار.md` - يحدد إعداد بيئة الاختبار.
- `ENVIRONMENT_CONFIGURATION.md` - مرجع لتكوين البيئة.

## 🏗 البنية التفصيلية
### 1. المتطلبات الأساسية للبرمجيات
*   **نظام التشغيل:** يفضل استخدام بيئة Linux (مثل Ubuntu) أو macOS. دعم Windows عبر WSL2.
*   **اللغات:** Node.js (الإصدار 20.x)، Python (الإصدار 3.11).
*   **أدوات الحاويات:** Docker و Docker Compose (للتناسق البيئي).
*   **محرر الكود:** Visual Studio Code مع الإضافات الموصى بها (مثل ESLint، Prettier).

### 2. إعداد البيئة باستخدام Docker
*   **ملف `docker-compose.dev.yml`:** سيتم استخدام هذا الملف (المرحلة 10/04) لتعريف جميع الخدمات المطلوبة (مثل قاعدة البيانات، الخادم الخلفي، الخادم الأمامي) وتشغيلها بضغطة زر واحدة.
*   **الخدمات:** يجب أن تتضمن البيئة حاويات لـ:
    *   قاعدة البيانات (TiDB/MySQL).
    *   الخادم الخلفي (Node.js/FastAPI).
    *   واجهة المستخدم (React/Vite).

### 3. تكوين المتغيرات البيئية
*   **ملف `environment.template`:** يجب على كل مطور إنشاء ملف `.env` خاص به بناءً على هذا القالب (المرحلة 10/06).
*   **المتغيرات:** يجب أن يتضمن التكوين متغيرات لـ:
    *   مفاتيح API المحلية (للتطوير).
    *   منافذ الخدمات.
    *   بيانات اعتماد قاعدة البيانات المحلية.

## ⚙️ آلية العمل
تضمن هذه الوثيقة أن كل مطور يبدأ العمل بنفس الإعدادات، مما يقلل من وقت الإعداد ويزيد من إنتاجية الفريق.

---

# 02-إعداد بيئة الاختبار

## 🎯 الغرض والهدف
تحديد وتوثيق الإجراءات اللازمة لإعداد بيئة اختبار (Staging/Testing Environment) منفصلة عن بيئة التطوير والإنتاج، لضمان أن الاختبارات تتم في بيئة تحاكي الإنتاج قدر الإمكان.

## 📋 المتطلبات الأساسية
ملف `01-إعداد-بيئة-التطوير.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `01-إعداد-بيئة-التطوير.md` - يحدد إعداد بيئة التطوير.

### الملفات اللاحقة
- `03-إعداد-بيئة-الإنتاج.md` - يحدد إعداد بيئة الإنتاج.
- `DEPLOYMENT_SETUP_GUIDE.md` - مرجع لدليل إعداد النشر.

## 🏗 البنية التفصيلية
### 1. متطلبات الفصل البيئي
*   **العزل:** يجب أن تكون بيئة الاختبار معزولة تمامًا عن بيئة الإنتاج، باستخدام قواعد بيانات وخدمات طرف ثالث منفصلة (أو محاكاة لها).
*   **التشابه مع الإنتاج:** يجب أن تستخدم بيئة الاختبار نفس البنية التحتية (مثل نفس نوع الخادم، نفس نظام التشغيل) قدر الإمكان لتقليل مخاطر الاختلافات.

### 2. إعداد البيئة الآلي
*   **النشر المستمر:** يجب أن يتم نشر الكود إلى بيئة الاختبار تلقائيًا عند دمج التغييرات في الفرع الرئيسي (Main Branch) (المرحلة 12).
*   **البيانات الوهمية:** يجب استخدام بيانات وهمية (Mock Data) أو بيانات إنتاج منقحة (Sanitized Production Data) في بيئة الاختبار لضمان الخصوصية.

### 3. تكوين الاختبار
*   **أدوات الاختبار:** يجب تثبيت وتكوين أدوات الاختبار (مثل Jest، Cypress) في هذه البيئة.
*   **متغيرات البيئة:** يجب استخدام مجموعة خاصة من متغيرات البيئة لبيئة الاختبار، بما في ذلك مفاتيح API الخاصة بالاختبار (Test API Keys) لخدمات الطرف الثالث.

## ⚙️ آلية العمل
تضمن هذه الوثيقة أن جميع الاختبارات تتم في بيئة موثوقة ومحاكية للإنتاج، مما يزيد من ثقة الفريق في جودة الكود قبل النشر.

---

# 03-إعداد بيئة الإنتاج

## 🎯 الغرض والهدف
تحديد وتوثيق الإجراءات والمعايير اللازمة لإعداد بيئة الإنتاج (Production Environment) التي سيتم نشر التطبيق عليها، مع التركيز على الأمان، الأداء، وقابلية التوسع.

## 📋 المتطلبات الأساسية
ملف `02-إعداد-بيئة-الاختبار.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `02-إعداد-بيئة-الاختبار.md` - يحدد إعداد بيئة الاختبار.

### الملفات اللاحقة
- `04-إدارة-المفاتيح-و-API.md` - يحدد إدارة المفاتيح و API.
- `API_MANAGEMENT_SPEC.md` - مرجع لمواصفات إدارة API.

## 🏗 البنية التفصيلية
### 1. البنية التحتية للإنتاج
*   **الاستضافة:** يجب استخدام مزود خدمة سحابية موثوق (مثل AWS، Azure، GCP) لضمان التوافرية العالية (High Availability) وقابلية التوسع.
*   **التوزيع:** سيتم استخدام حاويات Docker (ملف `docker-compose.prod.yml` - المرحلة 10/05) مع نظام تنسيق (Orchestration System) مثل Kubernetes أو Docker Swarm.
*   **الشبكة:** يجب أن تكون بيئة الإنتاج محمية بجدار حماية (Firewall) وأن يتم الوصول إليها فقط عبر نقاط نهاية آمنة (HTTPS).

### 2. تكوين الأداء والأمان
*   **التخزين المؤقت (Caching):** يجب تكوين آليات التخزين المؤقت على مستوى التطبيق وعلى مستوى الشبكة (CDN) لتحسين الأداء.
*   **التسجيل والمراقبة:** يجب تكوين نظام تسجيل مركزي (المرحلة 12) لمراقبة صحة وأداء النظام في الوقت الفعلي.
*   **الأمان:** يجب تطبيق جميع متطلبات الأمان المحددة في المرحلة 12، بما في ذلك تشفير البيانات أثناء النقل والتخزين.

### 3. عملية النشر (Deployment)
*   **النشر الآلي:** يجب أن يتم النشر إلى بيئة الإنتاج عبر خط أنابيب آلي (CI/CD Pipeline) (المرحلة 12) لتقليل الأخطاء البشرية.
*   **التحقق من الصحة:** يجب إجراء فحوصات صحية (Health Checks) شاملة بعد كل عملية نشر للتأكد من أن جميع الخدمات تعمل بشكل صحيح.

## ⚙️ آلية العمل
تضمن هذه الوثيقة أن بيئة الإنتاج قوية، آمنة، ومحسّنة للأداء، مما يوفر تجربة مستخدم موثوقة.

---

# 04-إدارة المفاتيح و API

## 🎯 الغرض والهدف
تحديد الإجراءات والتقنيات اللازمة لإدارة مفاتيح الواجهة البرمجية (API Keys) والأسرار الأخرى (Secrets) بشكل آمن عبر جميع البيئات (التطوير، الاختبار، الإنتاج).

## 📋 المتطلبات الأساسية
ملف `03-إعداد-بيئة-الإنتاج.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `03-إعداد-بيئة-الإنتاج.md` - يحدد إعداد بيئة الإنتاج.

### الملفات اللاحقة
- `ENVIRONMENT_CONFIGURATION.md` - مرجع لتكوين البيئة.
- `MONITORING_CONFIGURATION.md` - مرجع لتكوين المراقبة.

## 🏗 البنية التفصيلية
### 1. تخزين المفاتيح والأسرار
*   **الإنتاج:** يجب تخزين جميع مفاتيح API الحساسة (مثل مفاتيح خدمات STT والترجمة) في مخزن أسرار مخصص (Secret Manager) (مثل AWS Secrets Manager، HashiCorp Vault) بدلاً من تخزينها مباشرة في متغيرات البيئة.
*   **التطوير/الاختبار:** يمكن استخدام ملفات تكوين محلية (مثل `api-keys.config.template` - المرحلة 10/07) مع توجيهات واضحة لعدم الالتزام بها في نظام التحكم في الإصدار (Git).

### 2. الوصول إلى المفاتيح
*   **الوصول المقيد:** يجب أن يكون الوصول إلى مخزن الأسرار مقيدًا فقط بالخدمات التي تحتاج إليها، باستخدام مبدأ الحد الأدنى من الامتيازات (Principle of Least Privilege).
*   **التناوب الدوري:** يجب تطبيق سياسة للتناوب الدوري (Rotation) لجميع مفاتيح API لتقليل مخاطر الاختراق.

### 3. إدارة مفاتيح المستخدم
*   **التشفير:** يجب تشفير مفاتيح API التي يقدمها المستخدم لخدمات الطرف الثالث (المرحلة 12/03) قبل تخزينها في قاعدة البيانات.
*   **التدقيق:** يجب تسجيل جميع محاولات الوصول إلى المفاتيح الحساسة لأغراض التدقيق الأمني.

## ⚙️ آلية العمل
تضمن هذه الوثيقة أن المفاتيح والأسرار يتم التعامل معها بأقصى درجات الأمان، مما يحمي النظام من الوصول غير المصرح به إلى الخدمات الخارجية.

---

# API_MANAGEMENT_SPEC

## 🎯 الغرض والهدف
تحديد مواصفات إدارة الواجهات البرمجية (API Management) الخاصة بالنظام، بما في ذلك معدلات الحدود (Rate Limiting)، التوثيق، ونقاط النهاية (Endpoints) الرئيسية.

## 📋 المتطلبات الأساسية
ملف `DEPLOYMENT_SETUP_GUIDE.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `DEPLOYMENT_SETUP_GUIDE.md` - يحدد دليل إعداد النشر.

### الملفات اللاحقة
- `MONITORING_CONFIGURATION.md` - يستخدم هذا المرجع في تكوين المراقبة.

## 🏗 البنية التفصيلية
### 1. نقاط النهاية الرئيسية
| نقطة النهاية | الغرض | المصادقة المطلوبة | معدل الحدود (الافتراضي) |
| :--- | :--- | :--- | :--- |
| `/api/v1/translate` | ترجمة نص في الوقت الفعلي | نعم (Token) | 100 طلب/دقيقة |
| `/api/v1/stt` | تحويل الكلام إلى نص | نعم (Token) | 50 طلب/دقيقة |
| `/api/v1/config` | جلب/تحديث إعدادات المستخدم | نعم (Token) | 10 طلبات/دقيقة |
| `/health` | فحص صحة الخدمة | لا | غير محدود |

### 2. معدلات الحدود (Rate Limiting)
*   **الهدف:** حماية النظام من هجمات الحرمان من الخدمة (DoS) وضمان التوزيع العادل للموارد.
*   **التنفيذ:** سيتم تطبيق معدلات الحدود على مستوى بوابة API (API Gateway) أو على مستوى الخادم الخلفي.
*   **الاستثناءات:** يمكن زيادة معدلات الحدود للمستخدمين ذوي الاشتراكات المدفوعة (المرحلة 12).

### 3. التوثيق (Documentation)
*   **النموذج:** سيتم استخدام مواصفات OpenAPI (Swagger) لتوثيق جميع نقاط النهاية.
*   **التحديث:** يجب تحديث التوثيق تلقائيًا مع كل عملية نشر جديدة.

## ⚙️ آلية العمل
تضمن هذه المواصفات أن الواجهة البرمجية منظمة، موثقة، ومحمية، مما يسهل على المطورين استخدامها ودمجها.

---

# DEPLOYMENT_SETUP_GUIDE

## 🎯 الغرض والهدف
توفير دليل خطوة بخطوة لإعداد بيئات النشر المختلفة (الاختبار والإنتاج)، مع التركيز على استخدام Docker و Docker Compose.

## 📋 المتطلبات الأساسية
ملف `ENVIRONMENT_CONFIGURATION.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `ENVIRONMENT_CONFIGURATION.md` - يحدد متغيرات البيئة.

### الملفات اللاحقة
- `API_MANAGEMENT_SPEC.md` - يستخدم هذا الدليل في إدارة API.

## 🏗 البنية التفصيلية
### 1. إعداد الخادم الأساسي
*   **نظام التشغيل:** تثبيت نظام تشغيل خادم (مثل Ubuntu Server LTS).
*   **البرمجيات الأساسية:** تثبيت Docker و Docker Compose.

### 2. نشر بيئة الاختبار (Staging)
*   **الخطوة 1: سحب الكود:** سحب أحدث إصدار من الفرع الرئيسي.
*   **الخطوة 2: إعداد المتغيرات:** إنشاء ملف `.env.staging` وتعبئته بمتغيرات البيئة الخاصة بالاختبار.
*   **الخطوة 3: التشغيل:** استخدام الأمر `docker-compose -f docker-compose.dev.yml up -d` لتشغيل جميع الخدمات.

### 3. نشر بيئة الإنتاج (Production)
*   **الخطوة 1: سحب الإصدار:** سحب الإصدار المستقر (Tagged Release).
*   **الخطوة 2: إعداد المتغيرات:** استخدام مخزن الأسرار (Secret Manager) لتحميل متغيرات البيئة الحساسة.
*   **الخطوة 3: التشغيل:** استخدام الأمر `docker-compose -f docker-compose.prod.yml up -d` لتشغيل الخدمات مع التكوين المحسن للإنتاج.
*   **الخطوة 4: التحقق من الصحة:** التحقق من حالة الخدمات عبر نقاط النهاية الصحية (Health Endpoints).

## ⚙️ آلية العمل
يضمن هذا الدليل أن عملية النشر موحدة وموثوقة عبر جميع البيئات، مما يقلل من أخطاء النشر.

---

# ENVIRONMENT_CONFIGURATION

## 🎯 الغرض والهدف
توفير مرجع شامل لجميع متغيرات البيئة (Environment Variables) المستخدمة في المشروع، مع تحديد الغرض والقيمة الافتراضية لكل متغير.

## 📋 المتطلبات الأساسية
ملف `04-إدارة-المفاتيح-و-API.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `04-إدارة-المفاتيح-و-API.md` - يحدد إدارة المفاتيح و API.

### الملفات اللاحقة
- `DEPLOYMENT_SETUP_GUIDE.md` - يستخدم هذا المرجع في دليل إعداد النشر.

## 🏗 البنية التفصيلية
### 1. متغيرات البيئة الأساسية
| المتغير | الغرض | القيمة الافتراضية (التطوير) | ملاحظات |
| :--- | :--- | :--- | :--- |
| `NODE_ENV` | تحديد بيئة التشغيل | `development` | `production` في بيئة الإنتاج. |
| `PORT` | منفذ تشغيل الخادم | `3000` | |
| `LOG_LEVEL` | مستوى التسجيل | `debug` | يمكن أن يكون `info`, `warn`, `error`. |

### 2. متغيرات قاعدة البيانات
| المتغير | الغرض | القيمة الافتراضية (التطوير) | ملاحظات |
| :--- | :--- | :--- | :--- |
| `DB_HOST` | مضيف قاعدة البيانات | `localhost` | |
| `DB_PORT` | منفذ قاعدة البيانات | `3306` | |
| `DB_USER` | اسم مستخدم قاعدة البيانات | `root` | |
| `DB_PASSWORD` | كلمة مرور قاعدة البيانات | `password` | يجب أن يكون سراً في الإنتاج. |
| `DB_NAME` | اسم قاعدة البيانات | `stt_translator_db` | |

### 3. متغيرات خدمات الطرف الثالث (STT/Translation)
| المتغير | الغرض | القيمة الافتراضية (التطوير) | ملاحظات |
| :--- | :--- | :--- | :--- |
| `GOOGLE_API_KEY` | مفتاح API لخدمة Google | `YOUR_GOOGLE_API_KEY` | يجب تخزينه بشكل آمن. |
| `AZURE_API_KEY` | مفتاح API لخدمة Azure | `YOUR_AZURE_API_KEY` | يجب تخزينه بشكل آمن. |
| `STT_ENGINE_DEFAULT` | محرك STT الافتراضي | `google` | يمكن أن يكون `azure` أو غيره. |

## ⚙️ آلية العمل
يضمن هذا المرجع أن جميع المكونات تستخدم نفس التكوين البيئي، مما يسهل عملية الصيانة والتصحيح.

---

# MONITORING_CONFIGURATION

## 🎯 الغرض والهدف
تحديد تكوين نظام المراقبة (Monitoring System) لجمع المقاييس (Metrics)، السجلات (Logs)، والتتبع (Tracing) عبر جميع مكونات النظام.

## 📋 المتطلبات الأساسية
ملف `API_MANAGEMENT_SPEC.md`.

## 🔗 العلاقات والترابط
### الملفات السابقة
- `API_MANAGEMENT_SPEC.md` - يحدد مواصفات إدارة API.

### الملفات اللاحقة
- `docker-compose.dev.yml` - يستخدم هذا التكوين في ملفات Docker.

## 🏗 البنية التفصيلية
### 1. المقاييس (Metrics)
*   **الأداة:** سيتم استخدام Prometheus لجمع المقاييس.
*   **المقاييس الرئيسية:**
    *   **الأداء:** زمن استجابة API، معدل الأخطاء (Error Rate)، معدل الطلبات في الثانية (RPS).
    *   **الموارد:** استخدام وحدة المعالجة المركزية (CPU)، الذاكرة، والقرص.
    *   **الأعمال:** عدد عمليات الترجمة الناجحة، عدد عمليات STT الفاشلة.

### 2. السجلات (Logs)
*   **الأداة:** سيتم استخدام نظام تسجيل مركزي (مثل ELK Stack أو Grafana Loki).
*   **التنسيق:** يجب أن تكون جميع السجلات بتنسيق JSON لسهولة التحليل.
*   **المحتوى:** يجب أن تتضمن السجلات معرفات الطلب (Request IDs) لتمكين التتبع عبر الخدمات.

### 3. التنبيهات (Alerts)
*   **الأداة:** سيتم استخدام Alertmanager.
*   **قواعد التنبيه:**
    *   معدل الأخطاء يتجاوز 5% لمدة 5 دقائق.
    *   زمن استجابة API يتجاوز 500 مللي ثانية لمدة 10 دقائق.
    *   استخدام وحدة المعالجة المركزية يتجاوز 80% لمدة 15 دقيقة.

## ⚙️ آلية العمل
يضمن هذا التكوين أن الفريق يمكنه مراقبة صحة النظام وأدائه بشكل استباقي والاستجابة السريعة لأي مشكلات.
