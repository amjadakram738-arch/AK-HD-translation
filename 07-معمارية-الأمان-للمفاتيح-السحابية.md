# 07-معمارية-الأمان-للمفاتيح-السحابية

## 🎯 الغرض والهدف
تحديد معمارية آمنة لإدارة واستخدام مفاتيح API الخاصة بالخدمات السحابية (STT والترجمة) تمنع تسريبها أو كشفها في بيئة العميل (Browser Extension) غير الآمنة.

## 📋 النطاق الوظيفي
*   تشفير مفاتيح API قبل تخزينها في إعدادات الإضافة.
*   منع فك تشفير المفاتيح داخل `Service Worker` أو `Content Script`.
*   استخدام "خادم وسيط" (Proxy Server/Backend) للقيام بالطلبات الحساسة.
*   إدارة صلاحيات الوصول للمفاتيح بناءً على هوية المستخدم.

## 🏗 المسؤوليات
*   **Secure Proxy Client:** المكون داخل الإضافة الذي يرسل الطلبات إلى الخادم الوسيط بدلاً من إرسالها مباشرة إلى Google/Azure.
*   **Backend Proxy (Server-side):** الخادم الذي يحتفظ بالمفاتيح الحقيقية، يستقبل الطلب من الإضافة، يضيف المفتاح، ثم يرسله للخدمة السحابية.
*   **Token Manager:** إدارة رموز الوصول قصيرة المدى (Access Tokens) التي تستخدمها الإضافة للتواصل مع الخادم الوسيط.

## 🚫 ما لا يقع ضمن مسؤوليته
*   تشفير بيانات المستخدم الشخصية (مسؤولية Storage System).
*   إدارة اشتراكات المستخدمين المالية.
*   حماية مفاتيح API التي يدخلها المستخدم بنفسه (User-provided keys) - حيث يتم تشفيرها محلياً فقط.

## 🔗 التفاعل مع المكونات الأخرى (Conceptual)
1.  **مع STT/Translation Adapters:** بدلاً من استدعاء API الخدمة مباشرة، تستدعي هذه المحولات نقطة نهاية الخادم الوسيط.
2.  **مع Settings Manager:** جلب رموز الوصول (Tokens) بدلاً من مفاتيح API الخام.
3.  **مع الخادم الخارجي:** التواصل الآمن عبر HTTPS فقط.

## 🛠 الآلية التقنية المعتمدة
يُمنع منعاً باتاً وجود مفاتيح API الخام في كود الإضافة أو ذاكرتها. يتم استخدام **نمط الوكيل (Proxy Pattern)** حيث يعمل خادم خلفي كبوابة (Gateway) لجميع الطلبات السحابية، مما يضمن بقاء المفاتيح في بيئة خاضعة للسيطرة الكاملة.
